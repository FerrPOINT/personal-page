# 🚨 КРИТИЧЕСКАЯ УЯЗВИМОСТЬ: Утечка Telegram токенов

## Обнаруженная проблема

В истории Git репозитория обнаружены реальные Telegram bot токены:

1. **Токен 1**: `8442009009:AAFZlXWyWorao-qVt_9Eq6Tm2HSCSMJO_7g` (старый)
2. **Токен 2**: `7814170579:AAGlep9wSPx9gRlA0uKcVCC3J9PmPpV7JUM` (текущий)

## Где найдены токены

- В истории коммитов Git (даже если файлы сейчас в .gitignore)
- В файлах `env.local` и `env.prod` (локально, но не закоммичены сейчас)

## Критичность

**КРИТИЧЕСКАЯ** - репозиторий **ПУБЛИЧНЫЙ** (github.com/FerrPOINT/personal-page), токены **СКОМПРОМЕТИРОВАНЫ** и доступны всем!

## Немедленные действия

### 1. Отозвать скомпрометированные токены

1. Откройте Telegram, найдите `@BotFather`
2. Отправьте команду `/revoke` для каждого бота
3. Создайте новые токены через `/newbot` или `/token`

### 2. Обновить токены везде

- [ ] Обновить токен в `env.local`
- [ ] Обновить токен в `env.prod`
- [ ] Обновить токен в Jenkins Credentials (`telegram-bot-token`)
- [ ] Обновить токен на production сервере (в `.env` файле)

### 3. Очистить историю Git (опционально, но рекомендуется)

Если репозиторий публичный, рекомендуется использовать `git filter-branch` или BFG Repo-Cleaner для удаления токенов из истории:

```bash
# ВАЖНО: Сделайте backup репозитория перед этим!
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch env.local env.prod" \
  --prune-empty --tag-name-filter cat -- --all

# Force push (ОСТОРОЖНО - это перепишет историю!)
git push origin --force --all
```

**⚠️ ВНИМАНИЕ**: Force push перепишет историю и может сломать работу других разработчиков!

### 4. Проверить активность ботов

1. Проверьте логи ботов на подозрительную активность
2. Проверьте, не отправлялись ли сообщения от имени ботов без вашего ведома

## Профилактика

### ✅ Уже сделано правильно:
- Файлы `env.local` и `env.prod` в `.gitignore`
- Токены хранятся в Jenkins Credentials для production

### ⚠️ Рекомендации:
1. **Никогда не коммитьте файлы с реальными токенами**
2. Используйте `.env.example` с placeholder значениями
3. Используйте секреты в CI/CD (Jenkins Credentials) для production
4. Регулярно ротируйте токены (раз в 3-6 месяцев)

## Статус проверки

- ✅ Файлы `env.local` и `env.prod` сейчас НЕ закоммичены
- ✅ Файлы в `.gitignore`
- ❌ Токены были в истории коммитов
- ❌ **РЕПОЗИТОРИЙ ПУБЛИЧНЫЙ** - токены скомпрометированы
- 🚨 **ТРЕБУЕТСЯ НЕМЕДЛЕННОЕ ДЕЙСТВИЕ**

## Дата обнаружения

2026-01-10


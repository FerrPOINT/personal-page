# Финальная настройка деплоя - Инструкция

## ✅ Текущий статус

- ✅ Jenkins доступен и работает
- ✅ Все плагины установлены
- ✅ Jenkins job создан
- ✅ Jenkinsfile готов
- ✅ Скрипт deploy.sh готов

---

## 🔧 Что нужно сделать

### Шаг 1: Обновить SSH credentials в Jenkins

**Вариант A: Через скрипт (если есть доступ к ключу)**

```bash
# Если ключ находится в стандартном месте
bash scripts/update-jenkins-credentials.sh ~/.ssh/id_rsa

# Или если другой ключ
bash scripts/update-jenkins-credentials.sh ~/.ssh/id_ed25519
```

**Вариант B: Через Jenkins UI**

1. Откройте: `http://192.168.1.49:32768/credentials/store/system/domain/_/`
2. Найдите `jenkins-ssh-deploy-key`
3. Нажмите на него для редактирования
4. В поле **Private Key** вставьте содержимое вашего `~/.ssh/id_rsa`:
   ```bash
   cat ~/.ssh/id_rsa
   ```
5. Сохраните

---

### Шаг 2: Подготовить сервер деплоя

Подключитесь к серверу:
```bash
ssh -i ~/.ssh/id_rsa root@azhukov-dev
```

На сервере выполните:

```bash
# 1. Создайте директорию (если нет)
mkdir -p /opt/personal-page
cd /opt/personal-page

# 2. Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/FerrPOINT/personal-page.git .

# Или если уже клонирован, обновите:
git fetch origin
git checkout main

# 3. Проверьте Docker
docker --version
docker compose version

# 4. Создайте env.prod (если нет)
# Скопируйте из env.example.txt и заполните значения
cp env.example.txt env.prod
nano env.prod  # Заполните значения
```

---

### Шаг 3: Проверить конфигурацию

Запустите финальную проверку:
```bash
bash scripts/final-jenkins-check.sh
```

Должно показать, что все готово.

---

### Шаг 4: Запустить тестовый деплой

```bash
# Через скрипт
bash scripts/trigger-jenkins-build.sh

# Или через Jenkins UI
# Откройте: http://192.168.1.49:32768/job/personal-page-deploy
# Нажмите "Build Now"
```

---

## 🐛 Если что-то не работает

### Проблема: Jenkins не может подключиться к azhukov-dev

**Решение:**
1. Проверьте, что `azhukov-dev` резолвится на Jenkins сервере
2. Если нет, обновите `Jenkinsfile` с реальным IP/хостом:
   ```groovy
   DEPLOY_HOST = '7eb10d5af2ad.vps.myjino.ru'  // или реальный IP
   ```

### Проблема: Репозиторий не найден на сервере

**Решение:**
```bash
ssh -i ~/.ssh/id_rsa root@azhukov-dev
cd /opt
git clone https://github.com/FerrPOINT/personal-page.git personal-page
```

### Проблема: Скрипт deploy.sh не найден

**Решение:**
Скрипт будет в репозитории после `git pull`. Убедитесь, что репозиторий обновлен.

---

## 📋 Чек-лист

- [ ] SSH credentials обновлены в Jenkins с правильным ключом
- [ ] Репозиторий клонирован на сервере в `/opt/personal-page`
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Файл `env.prod` существует на сервере
- [ ] Тестовый build запущен
- [ ] Build прошел успешно

---

**Готово к деплою!**


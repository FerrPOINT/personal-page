# ✅ План верификации работы скриптов на сервере

## 🎯 Цель
Проверить, что все скрипты управления диском работают корректно после настройки.

## 📋 Чек-лист верификации

### 1. Немедленная проверка (сразу после setup)

```bash
# 1.1. Проверка статуса
./scripts/server/disk-manager.sh status

# Ожидаемый результат:
# - Использование диска показано
# - Cron задачи найдены и показаны
# - Скрипты созданы и исполняемые
# - Logrotate настроен
```

**Команды для проверки:**
```bash
# Проверка cron задач
crontab -l | grep -E "disk-monitor|docker-cleanup|tmp-cleanup"

# Ожидаемый результат: 3 строки с cron задачами
```

```bash
# Проверка скриптов
ls -la /usr/local/bin/disk-monitor.sh \
       /usr/local/bin/docker-cleanup.sh \
       /usr/local/bin/tmp-cleanup.sh \
       /usr/local/bin/cleanup-disk.sh

# Ожидаемый результат: все 4 файла существуют и исполняемые (-rwxr-xr-x)
```

```bash
# Проверка logrotate
ls -la /etc/logrotate.d/custom-logs
cat /etc/logrotate.d/custom-logs

# Ожидаемый результат: файл существует и содержит настройки ротации
```

### 2. Тест мониторинга (вручную)

```bash
# 2.1. Запуск мониторинга вручную
sudo /usr/local/bin/disk-monitor.sh

# 2.2. Проверка лога
cat /var/log/disk-monitor.log

# Ожидаемый результат:
# - Лог создан или обновлен
# - Содержит информацию об использовании диска
# - Нет ошибок
```

**Проверка логики:**
```bash
# Получить текущее использование диска
DISK_USAGE=$(df / | awk 'NR==2 {gsub(/%/, "", $5); print $5}')

# Проверить, что это число
if [[ "$DISK_USAGE" =~ ^[0-9]+$ ]]; then
    echo "✓ Использование диска: ${DISK_USAGE}%"
    if [ "$DISK_USAGE" -ge 95 ]; then
        echo "⚠️  Критический уровень! Должна быть автоматическая очистка"
    elif [ "$DISK_USAGE" -ge 85 ]; then
        echo "⚠️  Предупреждение! Использование выше 85%"
    else
        echo "✓ Использование диска в норме"
    fi
else
    echo "✗ Ошибка определения использования диска"
fi
```

### 3. Тест очистки (вручную)

```bash
# 3.1. Запуск очистки вручную
sudo /usr/local/bin/cleanup-disk.sh

# Ожидаемый результат:
# - Выполняется очистка логов
# - Выполняется очистка временных файлов
# - Если Docker установлен - выполняется очистка Docker
# - Показывается текущее использование диска
# - Показываются топ-10 папок по размеру
```

**Проверка до и после:**
```bash
# До очистки
BEFORE=$(df / | awk 'NR==2 {print $5}')
echo "Использование до: $BEFORE"

# Очистка
sudo /usr/local/bin/cleanup-disk.sh

# После очистки
AFTER=$(df / | awk 'NR==2 {print $5}')
echo "Использование после: $AFTER"
```

### 4. Проверка через 30 минут (автоматический мониторинг)

```bash
# 4.1. Подождать 30 минут после setup
# Или изменить cron на */1 для теста (каждую минуту)

# 4.2. Проверить лог мониторинга
tail -20 /var/log/disk-monitor.log

# Ожидаемый результат:
# - Лог обновлен в течение последних 30 минут
# - Содержит записи о проверке диска
# - Нет ошибок
```

**Быстрый тест (изменить cron временно):**
```bash
# Временно изменить cron на каждую минуту для теста
(crontab -l 2>/dev/null | grep -v "disk-monitor"; echo "*/1 * * * * /usr/local/bin/disk-monitor.sh >> /var/log/disk-monitor.log 2>&1") | crontab -

# Подождать 2 минуты
sleep 120

# Проверить лог
tail -5 /var/log/disk-monitor.log

# Вернуть обратно на 30 минут
(crontab -l 2>/dev/null | grep -v "disk-monitor"; echo "*/30 * * * * /usr/local/bin/disk-monitor.sh >> /var/log/disk-monitor.log 2>&1") | crontab -
```

### 5. Проверка через день (очистка временных файлов)

```bash
# 5.1. Проверить лог очистки временных файлов
ls -la /var/log/tmp-cleanup.log
cat /var/log/tmp-cleanup.log

# Ожидаемый результат:
# - Лог создан после 2:00
# - Содержит информацию об очистке
# - Нет ошибок
```

**Проверка cron выполнения:**
```bash
# Проверить системный лог на выполнение cron
grep "tmp-cleanup" /var/log/syslog | tail -5
```

### 6. Проверка через неделю (очистка Docker)

```bash
# 6.1. Проверить лог очистки Docker (если Docker установлен)
if command -v docker &> /dev/null; then
    ls -la /var/log/docker-cleanup.log
    cat /var/log/docker-cleanup.log
fi

# Ожидаемый результат:
# - Лог создан после воскресенья 3:00
# - Содержит информацию об очистке Docker
# - Нет ошибок
```

### 7. Проверка logrotate

```bash
# 7.1. Проверить конфигурацию logrotate
logrotate -d /etc/logrotate.d/custom-logs

# Ожидаемый результат:
# - Нет ошибок в конфигурации
# - Показывает план ротации
```

**Тест ротации вручную:**
```bash
# Запустить ротацию вручную (dry-run)
sudo logrotate -d /etc/logrotate.d/custom-logs

# Запустить ротацию вручную (реально)
sudo logrotate -f /etc/logrotate.d/custom-logs
```

### 8. Проверка systemd journal

```bash
# 8.1. Проверить настройки journald
cat /etc/systemd/journald.conf | grep -E "^SystemMaxUse|^SystemKeepFree|^SystemMaxFileSize|^MaxRetentionSec"

# Ожидаемый результат:
# SystemMaxUse=500M
# SystemKeepFree=1G
# SystemMaxFileSize=100M
# MaxRetentionSec=7day
```

**Проверка размера journal:**
```bash
# Текущий размер journal
journalctl --disk-usage

# Ожидаемый результат:
# Размер не должен превышать 500MB
```

## 🔍 Автоматический скрипт проверки

Создайте скрипт для автоматической проверки:

```bash
#!/bin/bash
# scripts/server/verify-setup.sh

echo "=== ПРОВЕРКА НАСТРОЙКИ УПРАВЛЕНИЯ ДИСКОМ ==="
echo ""

# 1. Проверка cron задач
echo "1. Проверка cron задач..."
CRON_COUNT=$(crontab -l 2>/dev/null | grep -cE "disk-monitor|docker-cleanup|tmp-cleanup" || echo "0")
if [ "$CRON_COUNT" -ge 3 ]; then
    echo "  ✓ Cron задачи настроены ($CRON_COUNT найдено)"
else
    echo "  ✗ Cron задачи не настроены (найдено: $CRON_COUNT)"
fi

# 2. Проверка скриптов
echo ""
echo "2. Проверка скриптов..."
SCRIPTS=(
    "/usr/local/bin/disk-monitor.sh"
    "/usr/local/bin/docker-cleanup.sh"
    "/usr/local/bin/tmp-cleanup.sh"
    "/usr/local/bin/cleanup-disk.sh"
)
ALL_EXIST=true
for script in "${SCRIPTS[@]}"; do
    if [ -f "$script" ] && [ -x "$script" ]; then
        echo "  ✓ $script"
    else
        echo "  ✗ $script (не найден или не исполняемый)"
        ALL_EXIST=false
    fi
done

# 3. Проверка logrotate
echo ""
echo "3. Проверка logrotate..."
if [ -f "/etc/logrotate.d/custom-logs" ]; then
    echo "  ✓ /etc/logrotate.d/custom-logs настроен"
else
    echo "  ✗ /etc/logrotate.d/custom-logs не найден"
fi

# 4. Проверка systemd journal
echo ""
echo "4. Проверка systemd journal..."
if grep -q "^SystemMaxUse=500M" /etc/systemd/journald.conf 2>/dev/null; then
    echo "  ✓ Systemd journal настроен"
else
    echo "  ✗ Systemd journal не настроен"
fi

# 5. Проверка логов
echo ""
echo "5. Проверка логов..."
LOGS=(
    "/var/log/disk-monitor.log"
    "/var/log/docker-cleanup.log"
    "/var/log/tmp-cleanup.log"
)
for log in "${LOGS[@]}"; do
    if [ -f "$log" ]; then
        SIZE=$(stat -f%z "$log" 2>/dev/null || stat -c%s "$log" 2>/dev/null || echo "0")
        echo "  ✓ $log (размер: $SIZE байт)"
    else
        echo "  ⚠️  $log (еще не создан, будет создан при первом запуске)"
    fi
done

# 6. Тест мониторинга
echo ""
echo "6. Тест мониторинга..."
if sudo /usr/local/bin/disk-monitor.sh 2>/dev/null; then
    echo "  ✓ Мониторинг работает"
else
    echo "  ✗ Ошибка при запуске мониторинга"
fi

echo ""
echo "=== ПРОВЕРКА ЗАВЕРШЕНА ==="
```

## ✅ Критерии успешной верификации

Все проверки должны показать:
- ✅ Cron задачи настроены (3 задачи)
- ✅ Скрипты созданы и исполняемые (4 скрипта)
- ✅ Logrotate настроен
- ✅ Systemd journal настроен
- ✅ Мониторинг работает без ошибок
- ✅ Логи создаются и обновляются

## 🚨 Если что-то не работает

1. Проверить права: `ls -la scripts/server/*.sh`
2. Проверить синтаксис: `bash -n scripts/server/disk-manager.sh`
3. Проверить логи: `tail -50 /var/log/disk-monitor.log`
4. Запустить диагностику: `./scripts/server/disk-manager.sh diagnose`
5. Перезапустить setup: `sudo ./scripts/server/disk-manager.sh setup`


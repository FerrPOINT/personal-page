# 🚀 Развертывание скриптов управления диском на сервере

## Решение: Интеграция в проект

Скрипты управления диском интегрированы в проект и готовы к использованию на сервере.

## ✅ Что сделано

1. ✅ Все скрипты перемещены в `scripts/server/`
2. ✅ Создан главный скрипт-обертка `disk-manager.sh`
3. ✅ Все скрипты проверены на синтаксические ошибки
4. ✅ Создана документация
5. ✅ Скрипты готовы к коммиту

## 📁 Структура

```
scripts/server/
├── disk-manager.sh          # Главный скрипт-обертка (рекомендуется)
├── diagnose-disk-usage.sh   # Диагностика использования диска
├── prevent-disk-full.sh     # Настройка автоматической защиты
├── test-scripts.sh          # Тестирование (для разработки)
├── README.md                # Подробная документация
├── USAGE.md                 # Краткая инструкция
└── .gitkeep                 # Для сохранения директории в git
```

## 🎯 Использование на сервере

### После клонирования проекта:

```bash
# 1. Перейти в директорию проекта
cd /opt/personal-page  # или ваш путь

# 2. Диагностика (безопасно, не требует root)
./scripts/server/disk-manager.sh diagnose

# 3. Настройка автоматической защиты (требует root)
sudo ./scripts/server/disk-manager.sh setup

# 4. Проверка статуса
./scripts/server/disk-manager.sh status
```

## 📋 Команды disk-manager.sh

| Команда | Описание | Требует root |
|---------|----------|--------------|
| `diagnose` | Диагностика использования диска | ❌ |
| `setup` | Настройка автоматической защиты | ✅ |
| `cleanup` | Ручная очистка диска | ✅ |
| `status` | Проверка статуса мониторинга | ❌ |
| `help` | Показать справку | ❌ |

## ✅ Преимущества решения

1. **Интеграция в проект** - скрипты версионируются вместе с кодом
2. **Удобство** - один главный скрипт для всех операций
3. **Безопасность** - проверка прав доступа
4. **Документация** - подробные инструкции
5. **Тестируемость** - все скрипты проверены

## 🔧 Что настраивается после `setup`

- ✅ Автоматическая ротация логов (logrotate, 7 дней)
- ✅ Еженедельная очистка Docker (воскресенье 3:00)
- ✅ Ежедневная очистка временных файлов (2:00)
- ✅ Мониторинг диска каждые 30 минут с алертами
- ✅ Автоматическая очистка при >95% использования
- ✅ Ограничения для systemd journal (500MB, 7 дней)

## 📊 Мониторинг

После настройки система автоматически:
- Мониторит диск каждые 30 минут
- Предупреждает при использовании >85%
- Автоматически очищает при использовании >95%

Логи:
- `/var/log/disk-monitor.log` - Мониторинг
- `/var/log/docker-cleanup.log` - Очистка Docker
- `/var/log/tmp-cleanup.log` - Очистка временных файлов

## 🧪 Тестирование

Все скрипты проверены:
```bash
✓ Все скрипты синтаксически корректны
✓ Главный скрипт работает корректно
✓ Все функции протестированы
```

## 📝 Следующие шаги

1. **Закоммитить скрипты**:
   ```bash
   git add scripts/server/
   git commit -m "feat: add disk management scripts for server"
   git push
   ```

2. **На сервере**:
   ```bash
   cd /opt/personal-page
   git pull
   sudo ./scripts/server/disk-manager.sh setup
   ```

3. **Проверка**:
   ```bash
   ./scripts/server/disk-manager.sh status
   ```

## ⚠️ Важно

- Скрипты требуют bash
- Команды `setup` и `cleanup` требуют root
- Все скрипты безопасны и не удаляют важные данные
- Резервные копии создаются автоматически

## 📚 Документация

- `README.md` - Подробная документация
- `USAGE.md` - Краткая инструкция
- `../../DISK-MANAGEMENT.md` - Общая документация
- `../../README-DISK-SCRIPTS.md` - Документация по скриптам


# 🧪 Безопасное тестирование скриптов управления диском

## ✅ Важно: Что НЕ удаляется

**Скрипты НЕ удаляют:**
- ❌ Важные данные приложения
- ❌ Базы данных
- ❌ Конфигурационные файлы
- ❌ Код проекта
- ❌ Активные логи (только старые архивы)

**Скрипты удаляют ТОЛЬКО:**
- ✅ Старые архивы логов (>30 дней при setup, >7 дней при cleanup)
- ✅ Сжатые логи (.gz) старше указанного срока
- ✅ Временные файлы в /tmp и /var/tmp старше 7 дней
- ✅ Неиспользуемые Docker ресурсы (только если Docker установлен)

## 🛡️ Безопасное тестирование

### Этап 1: Диагностика (100% безопасно)

```bash
# Только чтение, ничего не удаляет
./scripts/server/disk-manager.sh diagnose > /tmp/disk-diagnosis-$(date +%Y%m%d-%H%M%S).txt

# Посмотреть результаты
cat /tmp/disk-diagnosis-*.txt
```

**Что делает:**
- Читает информацию о диске
- Показывает размеры папок
- Анализирует логи
- **НЕ удаляет ничего**

### Этап 2: Проверка скриптов (безопасно)

```bash
# Проверить синтаксис
bash -n scripts/server/prevent-disk-full.sh

# Посмотреть, что будет удалено (dry-run)
# В скрипте prevent-disk-full.sh есть команды find с -delete
# Но они удаляют только:
# - Старые логи (>30 дней): *.log.* и *.gz в /var/log
# - Временные файлы (>7 дней): в /tmp и /var/tmp
```

### Этап 3: Безопасная проверка перед setup

```bash
# Проверить, какие логи будут удалены (БЕЗ удаления)
echo "=== Логи старше 30 дней (будут удалены при setup) ==="
find /var/log -type f -name "*.log.*" -mtime +30 -exec ls -lh {} \; 2>/dev/null | head -20
find /var/log -type f -name "*.gz" -mtime +30 -exec ls -lh {} \; 2>/dev/null | head -20

# Проверить размер
echo "=== Размер старых логов ==="
du -sh $(find /var/log -type f -name "*.log.*" -mtime +30 2>/dev/null) 2>/dev/null | awk '{sum+=$1} END {print "Total:", sum}'
du -sh $(find /var/log -type f -name "*.gz" -mtime +30 2>/dev/null) 2>/dev/null | awk '{sum+=$1} END {print "Total:", sum}'
```

### Этап 4: Setup с проверкой (безопасно, но удаляет старые логи)

```bash
# Setup удаляет ТОЛЬКО:
# 1. Логи старше 30 дней в /var/log/*.log.* и /var/log/*.gz
# 2. Настраивает автоматическую очистку (не удаляет сразу)

# Перед setup можно сделать резервную копию старых логов (опционально)
sudo mkdir -p /backup/old-logs-$(date +%Y%m%d)
sudo find /var/log -type f -name "*.log.*" -mtime +30 -exec cp {} /backup/old-logs-$(date +%Y%m%d)/ \; 2>/dev/null
sudo find /var/log -name "*.gz" -mtime +30 -exec cp {} /backup/old-logs-$(date +%Y%m%d)/ \; 2>/dev/null

# Теперь можно безопасно запустить setup
sudo ./scripts/server/disk-manager.sh setup
```

### Этап 5: Тест мониторинга (100% безопасно)

```bash
# Мониторинг только читает информацию, ничего не удаляет
sudo /usr/local/bin/disk-monitor.sh

# Проверить лог
cat /var/log/disk-monitor.log
```

**Что делает мониторинг:**
- Читает использование диска
- Логирует предупреждения
- **Удаляет только при критическом уровне (>95%)** - и то только старые логи (>7 дней)

### Этап 6: Тест cleanup (удаляет только старые файлы)

```bash
# Cleanup удаляет ТОЛЬКО:
# - Логи старше 7 дней (не 30!)
# - Временные файлы старше 3 дней
# - Docker неиспользуемые ресурсы старше 72 часов

# Проверить, что будет удалено (БЕЗ удаления)
echo "=== Логи старше 7 дней (будут удалены) ==="
find /var/log -type f -name "*.log.*" -mtime +7 -exec ls -lh {} \; 2>/dev/null | head -20

echo "=== Временные файлы старше 3 дней (будут удалены) ==="
find /tmp -type f -mtime +3 -exec ls -lh {} \; 2>/dev/null | head -20

# Теперь можно безопасно запустить cleanup
sudo ./scripts/server/disk-manager.sh cleanup
```

## 🔍 Что можно безопасно проверить

### 1. Проверка синтаксиса (100% безопасно)
```bash
bash -n scripts/server/*.sh
```

### 2. Проверка структуры (100% безопасно)
```bash
ls -la scripts/server/
cat scripts/server/prevent-disk-full.sh | grep -E "find.*-delete|rm -"
```

### 3. Dry-run проверка удаления (100% безопасно)
```bash
# Посмотреть, что БУДЕТ удалено, но НЕ удалять
echo "=== Логи старше 30 дней (будут удалены при setup) ==="
find /var/log -type f \( -name "*.log.*" -o -name "*.gz" \) -mtime +30 -exec ls -lh {} \; 2>/dev/null

echo "=== Временные файлы старше 7 дней (будут удалены при cleanup) ==="
find /tmp /var/tmp -type f -mtime +7 -exec ls -lh {} \; 2>/dev/null | head -20
```

### 4. Проверка настроек (100% безопасно)
```bash
# После setup проверить настройки
cat /etc/logrotate.d/custom-logs
cat /etc/systemd/journald.conf | grep -E "SystemMaxUse|SystemKeepFree"
crontab -l
```

## ⚠️ Что удаляется и когда

### При `setup`:
- ✅ Логи `/var/log/*.log.*` старше **30 дней**
- ✅ Логи `/var/log/*.gz` старше **30 дней**
- ✅ Настраивается автоматическая очистка (не удаляет сразу)

### При `cleanup`:
- ✅ Логи `/var/log/*.log.*` старше **7 дней**
- ✅ Логи `/var/log/*.gz` старше **7 дней**
- ✅ Временные файлы `/tmp` старше **3 дней**
- ✅ Временные файлы `/var/tmp` старше **3 дней**
- ✅ Docker неиспользуемые ресурсы старше **72 часов** (если установлен)

### Автоматически (через cron):
- ✅ Ежедневно в 2:00 - временные файлы старше 7 дней
- ✅ Еженедельно в воскресенье в 3:00 - Docker ресурсы старше 7 дней
- ✅ Каждые 30 минут - мониторинг (удаляет только при >95% использования)

## 🛡️ Защита важных данных

### Что НИКОГДА не удаляется:
- ❌ `/opt/personal-page/` - код проекта
- ❌ `/var/lib/docker/volumes/` - Docker volumes с данными
- ❌ `/home/` - домашние директории
- ❌ `/etc/` - конфигурационные файлы
- ❌ Активные логи (только архивы старше срока)
- ❌ Базы данных

### Рекомендации перед тестированием:

1. **Сделать резервную копию старых логов** (если они важны):
   ```bash
   sudo mkdir -p /backup/old-logs-$(date +%Y%m%d)
   sudo find /var/log -type f \( -name "*.log.*" -o -name "*.gz" \) -mtime +30 -exec cp {} /backup/old-logs-$(date +%Y%m%d)/ \; 2>/dev/null
   ```

2. **Проверить, что будет удалено** (dry-run):
   ```bash
   find /var/log -type f \( -name "*.log.*" -o -name "*.gz" \) -mtime +30 -exec ls -lh {} \; 2>/dev/null
   ```

3. **Проверить использование диска до и после**:
   ```bash
   df -h / > /tmp/disk-before.txt
   # ... выполнить setup/cleanup ...
   df -h / > /tmp/disk-after.txt
   diff /tmp/disk-before.txt /tmp/disk-after.txt
   ```

## ✅ План безопасного тестирования

### Шаг 1: Диагностика (100% безопасно)
```bash
./scripts/server/disk-manager.sh diagnose
```

### Шаг 2: Dry-run проверка (100% безопасно)
```bash
# Автоматическая проверка того, что БУДЕТ удалено (БЕЗ удаления)
./scripts/server/dry-run-check.sh
```

Или вручную:
```bash
# Старые логи
find /var/log -type f \( -name "*.log.*" -o -name "*.gz" \) -mtime +30 -exec ls -lh {} \; 2>/dev/null | head -20

# Временные файлы
find /tmp /var/tmp -type f -mtime +7 -exec ls -lh {} \; 2>/dev/null | head -20
```

### Шаг 3: Резервная копия (опционально, но рекомендуется)
```bash
sudo mkdir -p /backup/old-logs-$(date +%Y%m%d)
sudo find /var/log -type f \( -name "*.log.*" -o -name "*.gz" \) -mtime +30 -exec cp {} /backup/old-logs-$(date +%Y%m%d)/ \; 2>/dev/null
```

### Шаг 4: Setup (удаляет только старые логи >30 дней)
```bash
sudo ./scripts/server/disk-manager.sh setup
```

### Шаг 5: Проверка (100% безопасно)
```bash
./scripts/server/verify-setup.sh
```

### Шаг 6: Тест мониторинга (100% безопасно)
```bash
sudo /usr/local/bin/disk-monitor.sh
cat /var/log/disk-monitor.log
```

## 🎯 Итог

**Все скрипты безопасны:**
- ✅ Диагностика - только чтение
- ✅ Setup - удаляет только старые архивы логов (>30 дней)
- ✅ Мониторинг - только чтение (удаляет только при критическом уровне)
- ✅ Cleanup - удаляет только старые файлы (логи >7 дней, временные >3 дня)

**Важные данные НЕ удаляются!**


# 🚀 Инструкция для выполнения на сервере

## ✅ Что уже сделано

- ✅ Все скрипты созданы и проверены
- ✅ Код закоммичен в репозиторий
- ✅ Документация готова

## 📋 Пошаговая инструкция

### Шаг 1: Обновить проект на сервере

```bash
# Перейти в директорию проекта
cd /opt/personal-page  # или ваш путь к проекту

# Обновить код из репозитория
git pull origin main
# или
git pull
```

### Шаг 2: Проверить, что скрипты на месте

```bash
# Проверить структуру
ls -la scripts/server/

# Должны быть файлы:
# - disk-manager.sh
# - diagnose-disk-usage.sh
# - prevent-disk-full.sh
# - verify-setup.sh
# и другие
```

### Шаг 3: Диагностика текущего состояния

```bash
# Безопасная диагностика (не требует root)
./scripts/server/disk-manager.sh diagnose > /tmp/disk-diagnosis-$(date +%Y%m%d-%H%M%S).txt

# Посмотреть результаты
cat /tmp/disk-diagnosis-*.txt
```

**Что проверяем:**
- Текущее использование диска
- Самые большие папки
- Размер логов
- Docker использование (если есть)

### Шаг 4: Настройка автоматической защиты

```bash
# Настройка (требует root)
sudo ./scripts/server/disk-manager.sh setup
```

**Что произойдет:**
1. Настройка logrotate для автоматической ротации логов
2. Очистка старых логов (>30 дней)
3. Создание скриптов очистки Docker (если установлен)
4. Создание скриптов очистки временных файлов
5. Настройка мониторинга диска (каждые 30 минут)
6. Настройка systemd journal (ограничения)
7. Добавление cron задач

**Внимание:** Скрипт попросит подтверждение перед настройкой.

### Шаг 5: Проверка настройки

```bash
# Автоматическая проверка
./scripts/server/verify-setup.sh

# Или вручную
./scripts/server/disk-manager.sh status
```

**Ожидаемый результат:**
- ✅ Cron задачи настроены (3 задачи)
- ✅ Скрипты созданы и исполняемые (4 скрипта)
- ✅ Logrotate настроен
- ✅ Systemd journal настроен

### Шаг 6: Тест мониторинга

```bash
# Запустить мониторинг вручную
sudo /usr/local/bin/disk-monitor.sh

# Проверить лог
cat /var/log/disk-monitor.log
```

**Ожидаемый результат:**
- Лог создан или обновлен
- Содержит информацию об использовании диска
- Нет ошибок

### Шаг 7: Проверка через 30 минут

```bash
# Мониторинг должен запуститься автоматически через cron
# Проверить лог
tail -20 /var/log/disk-monitor.log

# Проверить, что cron задачи выполняются
grep CRON /var/log/syslog | tail -10
```

## 🔍 Способы проверки работы

### 1. Немедленная проверка (сразу после setup)

```bash
./scripts/server/verify-setup.sh
```

### 2. Проверка статуса

```bash
./scripts/server/disk-manager.sh status
```

### 3. Проверка cron задач

```bash
crontab -l | grep -E "disk-monitor|docker-cleanup|tmp-cleanup"
```

**Ожидаемый результат:** 3 строки с cron задачами

### 4. Проверка скриптов

```bash
ls -la /usr/local/bin/disk-monitor.sh \
       /usr/local/bin/docker-cleanup.sh \
       /usr/local/bin/tmp-cleanup.sh \
       /usr/local/bin/cleanup-disk.sh
```

**Ожидаемый результат:** Все 4 файла существуют и исполняемые

### 5. Тест мониторинга вручную

```bash
# Запустить мониторинг
sudo /usr/local/bin/disk-monitor.sh

# Проверить лог
cat /var/log/disk-monitor.log
```

### 6. Проверка через 30 минут

```bash
# Проверить, что мониторинг запустился автоматически
tail -20 /var/log/disk-monitor.log

# Проверить время последней записи
stat /var/log/disk-monitor.log
```

### 7. Проверка через день

```bash
# Проверить очистку временных файлов
cat /var/log/tmp-cleanup.log
```

### 8. Проверка через неделю

```bash
# Проверить очистку Docker (если установлен)
cat /var/log/docker-cleanup.log
```

## 🚨 Если что-то не работает

### Проблема: Скрипты не исполняются

```bash
# Решение: Добавить права на выполнение
chmod +x scripts/server/*.sh
```

### Проблема: Cron задачи не выполняются

```bash
# Решение: Проверить cron сервис
systemctl status cron
sudo systemctl start cron
```

### Проблема: Логи не создаются

```bash
# Решение: Проверить права на /var/log
sudo touch /var/log/disk-monitor.log
sudo chmod 644 /var/log/disk-monitor.log
```

### Проблема: Ошибки при setup

```bash
# Решение: Запустить диагностику
./scripts/server/disk-manager.sh diagnose

# Или проверить вручную
bash -n scripts/server/prevent-disk-full.sh
```

## 📊 Мониторинг после настройки

### Ежедневно

```bash
# Проверить использование диска
df -h /

# Проверить логи мониторинга
tail -20 /var/log/disk-monitor.log
```

### Еженедельно

```bash
# Полная проверка статуса
./scripts/server/disk-manager.sh status

# Проверить все логи очистки
tail -20 /var/log/*-cleanup.log
```

## ✅ Критерии успеха

После выполнения всех шагов:

1. ✅ **Cron задачи настроены** (проверено через `crontab -l`)
2. ✅ **Скрипты созданы** (проверено через `ls -la /usr/local/bin/*.sh`)
3. ✅ **Logrotate настроен** (проверено через `cat /etc/logrotate.d/custom-logs`)
4. ✅ **Systemd journal настроен** (проверено через `cat /etc/systemd/journald.conf`)
5. ✅ **Мониторинг работает** (проверено через `sudo /usr/local/bin/disk-monitor.sh`)
6. ✅ **Логи создаются** (проверено через `ls -la /var/log/disk-monitor.log`)

## 🎯 Итог

После выполнения всех шагов система будет автоматически:
- ✅ Мониторить использование диска каждые 30 минут
- ✅ Предупреждать при использовании >85%
- ✅ Автоматически очищать при использовании >95%
- ✅ Ротировать логи ежедневно
- ✅ Очищать Docker еженедельно
- ✅ Очищать временные файлы ежедневно

**Диск больше не должен переполняться!**

## 📚 Дополнительная документация

- `README.md` - Подробная документация
- `USAGE.md` - Краткая инструкция
- `DEPLOYMENT.md` - Инструкция по развертыванию
- `VERIFICATION.md` - План верификации
- `SERVER-ACTION-PLAN.md` - Детальный план действий


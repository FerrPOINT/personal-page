# 🚀 Использование скриптов управления диском на сервере

## Быстрый старт

После клонирования проекта на сервере:

```bash
cd /opt/personal-page  # или ваш путь к проекту

# 1. Диагностика (безопасно, не требует root)
./scripts/server/disk-manager.sh diagnose

# 2. Настройка автоматической защиты (требует root)
sudo ./scripts/server/disk-manager.sh setup

# 3. Проверка статуса
./scripts/server/disk-manager.sh status
```

## Команды

| Команда | Описание | Требует root |
|---------|----------|--------------|
| `diagnose` | Диагностика использования диска | ❌ |
| `setup` | Настройка автоматической защиты | ✅ |
| `cleanup` | Ручная очистка диска | ✅ |
| `status` | Проверка статуса мониторинга | ❌ |
| `help` | Показать справку | ❌ |

## Примеры

### Полная настройка

```bash
# Диагностика
./scripts/server/disk-manager.sh diagnose > disk-diagnosis.txt

# Настройка
sudo ./scripts/server/disk-manager.sh setup

# Проверка
./scripts/server/disk-manager.sh status
```

### Регулярная проверка

```bash
# Раз в неделю
./scripts/server/disk-manager.sh status
```

### При проблемах

```bash
# Диагностика
./scripts/server/disk-manager.sh diagnose

# Очистка
sudo ./scripts/server/disk-manager.sh cleanup

# Проверка
./scripts/server/disk-manager.sh status
```

## Что настраивается

После `setup`:
- ✅ Автоматическая ротация логов (7 дней)
- ✅ Еженедельная очистка Docker
- ✅ Ежедневная очистка временных файлов
- ✅ Мониторинг диска каждые 30 минут
- ✅ Автоматическая очистка при >95% использования

## Логи

- `/var/log/disk-monitor.log` - Мониторинг
- `/var/log/docker-cleanup.log` - Очистка Docker
- `/var/log/tmp-cleanup.log` - Очистка временных файлов

## Подробная документация

См. `README.md` в этой директории.


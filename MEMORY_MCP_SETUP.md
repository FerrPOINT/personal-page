# Настройка Memory MCP для сохранения в корне проекта

## 📋 Описание

Файл `memory.json` теперь сохраняется в корне проекта (`memory.json`), а не в папке `.cursor`.

## 🔧 Настройка

### Шаг 1: Обновить конфигурацию MCP

Откройте файл конфигурации MCP (обычно находится в `~/.cursor/mcp.json` или в настройках Cursor) и обновите секцию `memory`:

```json
{
  "mcpServers": {
    "memory": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--label",
        "com.docker.compose.project=mcp",
        "-v",
        ".:/local-directory",
        "-e",
        "MEMORY_FILE_PATH=/local-directory/memory.json",
        "mcp/memory"
      ]
    }
  }
}
```

### Шаг 2: Переместить существующий файл (если есть)

✅ **Выполнено**: Файл `.cursor/memory.json` был перенесен в корень проекта как `memory.json`.

Старый файл `.cursor/memory.json` был удален.

### Шаг 3: Перезапустить Cursor

После изменения конфигурации MCP необходимо перезапустить Cursor IDE, чтобы изменения вступили в силу.

## ✅ Проверка

После перезапуска Cursor:
1. Используйте Memory MCP для сохранения информации
2. Проверьте, что файл `memory.json` создается/обновляется в корне проекта
3. Убедитесь, что старый файл `.cursor/memory.json` больше не используется

## 📝 Примечания

- Файл `memory.json` теперь является частью проекта и может быть добавлен в git (если нужно)
- **Важно**: Решите, нужно ли коммитить `memory.json` в репозиторий:
  - ✅ **Добавить в git**: Если память проекта полезна для команды и не содержит секретов
  - ❌ **Добавить в `.gitignore`**: Если память содержит конфиденциальную информацию или персональные данные
- Пример конфигурации также сохранен в `.cursor/mcp.json.backup` для справки

## 🔄 Миграция с `.cursor/memory.json`

✅ **Выполнено**: Миграция завершена:
1. ✅ Файл перенесен в `memory.json` в корне проекта
2. ✅ Старый файл `.cursor/memory.json` удален
3. ⏳ Осталось обновить конфигурацию MCP согласно шагу 1 выше и перезапустить Cursor


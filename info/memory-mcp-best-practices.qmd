# Best Practices: Эффективное использование Memory MCP

**Создано**: 2025-01-15  
**Цель**: Избежать хранения ненужной информации и оптимизировать использование памяти

---

## 🎯 Принципы эффективного использования

### 1. **Качество важнее количества**
- Хранить только **значимые знания**, которые будут использоваться в будущем
- Избегать хранения временных данных, промежуточных мыслей, случайных обсуждений
- Фокус на **релевантности**, а не на объеме

### 2. **Четкие критерии: что хранить, что нет**

#### ✅ **ХРАНИТЬ** (обязательно):

1. **Архитектурные решения и их обоснование**
   - Почему выбран конкретный подход
   - Почему отказались от альтернатив
   - Пример: "SQLite выбран для простоты, не нужен отдельный сервер БД"

2. **Правила и соглашения проекта**
   - Стайлгайды, конвенции именования
   - Архитектурные паттерны
   - Пример: "Все компоненты используют TypeScript strict mode"

3. **Решенные проблемы и баги**
   - Проблема → Решение → Результат
   - Troubleshooting knowledge
   - Пример: "Telegram бот не отправляет: проверить TELEGRAM_BOT_TOKEN, User ID, логи worker'а"

4. **Важные конфигурации**
   - Порты, лимиты, настройки безопасности
   - Environment variables и их назначение
   - Пример: "Rate limiting: 5 req/15min на IP для защиты от спама"

5. **Зависимости между компонентами**
   - Какие компоненты связаны
   - Какие задачи влияют на какие компоненты
   - Пример: "Task 003_add-russian-language → modifies → Frontend Application"

6. **Пользовательские предпочтения** (если применимо)
   - Стиль кода, подходы к решению задач
   - Пример: "Предпочитаю функциональный подход, избегаем классов"

#### ❌ **НЕ ХРАНИТЬ** (избегать):

1. **Промежуточные мысли и варианты**
   - "Может быть стоит...", "Возможно..."
   - Отвергнутые варианты без обоснования
   - Пример: ❌ "Думал использовать PostgreSQL, но передумал"

2. **Случайные обсуждения**
   - Оффтопик, болтовня
   - Не относящиеся к проекту темы
   - Пример: ❌ "Сегодня хорошая погода"

3. **Временный контекст сессии**
   - Разовые упоминания
   - Контекст, который не будет использоваться
   - Пример: ❌ "В этом чате обсуждали X"

4. **Дублирование документации**
   - Не копировать README целиком
   - Хранить только ключевые факты
   - Пример: ❌ Копирование всего README.md

5. **Секреты и чувствительные данные**
   - Токены, пароли, ключи
   - Только факт их использования
   - Пример: ❌ "TELEGRAM_BOT_TOKEN=abc123..." ✅ "Используется TELEGRAM_BOT_TOKEN для бота"

6. **Отладочная информация**
   - Логи, временные значения
   - Информация только для текущей сессии
   - Пример: ❌ "В логах было: Error: ..."

---

## 📋 Правила записи в память

### Правило 1: Явное подтверждение для важных данных

**Использовать триггерные слова** для явного указания, что нужно сохранить:
- `remember:` - сохранить в память
- `forget:` - удалить из памяти
- `update memory:` - обновить существующую запись

**Пример:**
```
Пользователь: "remember: В проекте используется React 19.2 с TypeScript 5.8"
→ Сохранить в память

Пользователь: "Может быть стоит обновить React?"
→ НЕ сохранять (промежуточная мысль)
```

### Правило 2: Фильтрация перед записью

**Перед записью проверить:**
1. ✅ Это окончательное решение?
2. ✅ Будет ли это использоваться в будущем?
3. ✅ Это уникальная информация (не дубликат)?
4. ✅ Это относится к проекту?

**Если хотя бы один ответ "нет" → НЕ сохранять**

### Правило 3: Категоризация памяти

**Разделять память по типам:**

1. **Долгосрочная память** (должна храниться):
   - Архитектурные решения
   - Правила и соглашения
   - Важные конфигурации
   - Troubleshooting knowledge

2. **Краткосрочная память** (можно очистить через время):
   - Контекст текущей сессии
   - Временные наблюдения
   - Промежуточные решения

3. **Архив** (старые данные):
   - Устаревшие решения
   - Неактуальные конфигурации
   - Завершенные задачи (после архивации)

---

## 🔍 Оптимизация поиска и использования

### 1. Семантический поиск с ограничениями

**Использовать поиск с фильтрами:**
- По времени (последние N дней)
- По типу сущности (Component, Task, Service)
- По релевантности (топ-K результатов)

**Пример:**
```javascript
// Плохо: возвращает все результаты
search_nodes({ query: "frontend" })

// Хорошо: ограничение по релевантности и времени
search_nodes({ query: "frontend react компонент" })
// + фильтрация по дате последнего использования
```

### 2. Избегать "memory dump"

**Не загружать всю память в контекст:**
- ❌ `read_graph()` при каждом запросе
- ✅ Использовать `search_nodes()` с конкретным запросом
- ✅ Использовать `open_nodes()` для конкретных сущностей

### 3. Кэширование часто используемых данных

**Кэшировать:**
- Структуру проекта (entities и relations)
- Часто запрашиваемые конфигурации
- Архитектурные решения

**Не кэшировать:**
- Временные данные
- Часто меняющиеся значения

---

## 🧹 Политика очистки и архивации

### Автоматическая очистка

**Правила удаления:**

1. **По времени использования:**
   - Если запись не использовалась > 30 дней → пометить как архивную
   - Если запись не использовалась > 90 дней → удалить (если не критичная)

2. **По типу:**
   - Краткосрочная память → очищать каждую неделю
   - Долгосрочная память → проверять раз в месяц
   - Архив → проверять раз в квартал

3. **По релевантности:**
   - Дубликаты → удалять старые версии
   - Устаревшие решения → архивировать
   - Неактуальные конфигурации → обновлять или удалять

### Ручная очистка

**Регулярно проверять:**
- Что хранится в памяти (`read_graph()`)
- Что не используется (`search_nodes()` для проверки)
- Что устарело (сравнение с текущим состоянием проекта)

---

## 🔒 Безопасность и валидация

### 1. Валидация входных данных

**Перед записью проверять:**
- ✅ Нет ли prompt injection
- ✅ Нет ли вредоносного кода
- ✅ Соответствует ли формат ожиданиям
- ✅ Нет ли секретов (токены, пароли)

### 2. Контроль доступа

**Разграничение прав:**
- Только авторизованные агенты могут записывать
- Проверка источника данных
- Логирование всех операций записи/удаления

### 3. Защита от "отравления памяти"

**Мониторинг:**
- Резкое увеличение записей
- Противоречащие факты
- Необычные паттерны обновлений

---

## 📊 Мониторинг и метрики

### Отслеживать:

1. **Размер памяти:**
   - Количество entities
   - Количество relations
   - Количество observations
   - Размер файла memory.json

2. **Использование:**
   - Частота запросов к памяти
   - Какие сущности запрашиваются чаще
   - Какие сущности не используются

3. **Производительность:**
   - Время поиска
   - Время записи
   - Использование токенов в контексте

### Алерты:

- ⚠️ Если память растет экспоненциально
- ⚠️ Если время ответа проседает
- ⚠️ Если много неиспользуемых записей
- ⚠️ Если обнаружены дубликаты

---

## 🎯 Конкретные правила для проекта

### Для Personal Page Project:

#### ✅ **Обязательно хранить:**

1. **Архитектурные решения:**
   - Почему SQLite (простота, не нужен отдельный сервер)
   - Почему worker pattern для Telegram (асинхронность)
   - Почему Context API для i18n (простота, персистентность)

2. **Конфигурации:**
   - Порты: 8888 (frontend), 9000 (backend)
   - Rate limiting: 5 req/15min
   - CORS настройки
   - Jenkins: pollSCM каждые 10 минут

3. **Зависимости:**
   - Какие задачи влияют на какие компоненты
   - Связи между компонентами
   - Зависимости между задачами

4. **Troubleshooting:**
   - Проблемы и их решения
   - Частые ошибки и способы исправления

#### ❌ **НЕ хранить:**

1. **Временные данные:**
   - Промежуточные мысли о реализации
   - Варианты, которые не выбрали
   - Случайные обсуждения

2. **Дублирование:**
   - Копирование README
   - Копирование документации
   - Информация, уже хранящаяся в других местах

3. **Секреты:**
   - Токены, пароли, ключи
   - Только факт их использования

---

## 🔄 Workflow для AI ассистента

### При работе над задачей:

1. **Перед началом:**
   ```javascript
   // Поиск существующего контекста
   search_nodes({ query: "название задачи компонент" })
   ```

2. **Во время работы:**
   ```javascript
   // Сохранять только окончательные решения
   // НЕ сохранять промежуточные мысли
   if (isFinalDecision && isRelevant) {
     add_observations([{
       entityName: "Task",
       contents: ["Окончательное решение: ..."]
     }])
   }
   ```

3. **После завершения:**
   ```javascript
   // Обновить статус задачи
   add_observations([{
     entityName: "Task",
     contents: ["Статус: Завершена", "Результат: ..."]
   }])
   ```

### При поиске информации:

1. **Использовать конкретные запросы:**
   ```javascript
   // Плохо
   read_graph() // Загружает всё
   
   // Хорошо
   search_nodes({ query: "конкретный вопрос" })
   open_nodes({ names: ["конкретная сущность"] })
   ```

2. **Ограничивать результаты:**
   - Не загружать всю память
   - Использовать поиск с фильтрами
   - Запрашивать только нужные сущности

---

## 📝 Чек-лист перед записью в память

Перед тем как сохранить что-то в Memory MCP, проверить:

- [ ] Это окончательное решение, а не промежуточная мысль?
- [ ] Это будет использоваться в будущем?
- [ ] Это уникальная информация (не дубликат)?
- [ ] Это относится к проекту?
- [ ] Это не секрет (токен, пароль)?
- [ ] Это не временный контекст сессии?
- [ ] Это не дублирование существующей документации?
- [ ] Пользователь явно попросил сохранить (remember:)?

**Если хотя бы один ответ "нет" → НЕ сохранять**

---

## 🛠️ Технические рекомендации

### 1. Ограничение размера памяти

**Установить лимиты:**
- Максимум N entities
- Максимум M observations на entity
- Максимальный размер файла memory.json

### 2. Оптимизация структуры

**Использовать:**
- Четкие entityType (Project, Component, Task, Service)
- Семантические relationType (part_of, uses, modifies)
- Структурированные observations (факты, а не эссе)

### 3. Регулярная оптимизация

**Периодически:**
- Удалять дубликаты
- Объединять похожие observations
- Архивировать старые данные
- Очищать неиспользуемые записи

---

## 📚 Дополнительные ресурсы

- [Memory MCP Usage Guide](memory-mcp-usage.qmd)
- [MCP Specification](https://modelcontextprotocol.io)
- [Memory Graph Best Practices](https://memorygraph.dev/blog/context-window-mcp/)

---

## 🔄 Обновление правил

**Правила должны обновляться:**
- При изменении структуры проекта
- При обнаружении проблем с памятью
- При изменении workflow

**Последнее обновление**: 2025-01-15

---

## ⚠️ Важно помнить

> **Memory MCP - это инструмент для хранения знаний, а не свалка данных.**
> 
> Каждая запись должна иметь ценность для будущего использования.
> 
> Лучше меньше, но качественнее.


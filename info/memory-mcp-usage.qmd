# Использование Memory MCP

**Обновлено**: 2025-01-19  
**Статус**: Активно используется

---

## 🎯 Назначение

Memory MCP хранит **знания о проекте** для быстрого доступа к архитектурным решениям, зависимостям и конфигурациям.

---

## ⏰ КОГДА ИСПОЛЬЗОВАТЬ

### ✅ Использовать Memory MCP:

1. **После принятия архитектурного решения**
   - Выбрали технологию/подход → сохранить обоснование
   - Пример: "SQLite выбран для простоты, не нужен отдельный сервер БД"

2. **После решения проблемы/бага**
   - Нашли решение → сохранить проблему и решение
   - Пример: "Telegram бот не отправляет: проверить TELEGRAM_BOT_TOKEN, User ID, логи worker'а"

3. **После создания/изменения компонента**
   - Создали новый компонент → создать entity и связи
   - Изменили архитектуру → обновить observations

4. **После завершения задачи**
   - Задача закрыта → обновить статус и результаты
   - Добавить связи с затронутыми компонентами

5. **Перед началом новой задачи**
   - Поиск контекста через `search_nodes()` или `open_nodes()`
   - НЕ использовать `read_graph()` - загружает всё

6. **После изменения важных конфигураций**
   - Изменили порты, лимиты, настройки → обновить observations
   - Пример: "Rate limiting: 5 req/15min на IP"

### ❌ НЕ использовать Memory MCP:

- Для промежуточных мыслей и вариантов
- Для временного контекста сессии
- Для дублирования документации
- Для секретов (токены, пароли)
- Для отладочной информации

---

## 📋 ЧТО ХРАНИТЬ

1. **Архитектурные решения** - почему выбран подход
2. **Правила проекта** - конвенции, стайлгайды
3. **Решенные проблемы** - проблема → решение → результат
4. **Важные конфигурации** - порты, лимиты, настройки
5. **Зависимости** - связи между задачами и компонентами

---

## 🔄 WORKFLOW

### Перед началом задачи:
```javascript
// Поиск контекста (НЕ read_graph!)
search_nodes({ query: "название задачи компонент" })
```

### После принятия решения:
```javascript
// Только окончательные решения!
add_observations([{
  entityName: "Component",
  contents: ["Окончательное решение: ..."]
}])
```

### После завершения задачи:
```javascript
// Обновить статус
add_observations([{
  entityName: "Task",
  contents: ["Статус: Завершена", "Результат: ..."]
}])
```

---

## 🔍 ПОИСК ИНФОРМАЦИИ

**Использовать конкретные запросы:**
- ✅ `search_nodes({ query: "конкретный вопрос" })`
- ✅ `open_nodes({ names: ["конкретная сущность"] })`
- ❌ `read_graph()` - загружает всё, избегать

---

## 📋 ЧЕК-ЛИСТ ПЕРЕД ЗАПИСЬЮ

- [ ] Это окончательное решение?
- [ ] Это будет использоваться в будущем?
- [ ] Это уникальная информация?
- [ ] Это относится к проекту?
- [ ] Это не секрет?
- [ ] Это не временный контекст?

**Если хотя бы один ответ "нет" → НЕ сохранять!**

---

## 🏗️ СТРУКТУРИРОВАНИЕ ДАННЫХ

### Типы сущностей (entityType)
Используй четкие типы для организации:
- `Project` - основной проект
- `Component` - компоненты приложения
- `Service` - сервисы и API
- `Task` - задачи спринтов
- `Infrastructure` - инфраструктура (Docker, CI/CD)
- `Technology` - технологии и библиотеки
- `Rule` - правила и соглашения
- `Configuration` - конфигурации

### Связи (relationType)
Используй семантические типы связей:
- `part_of` - часть целого
- `uses` - использует
- `modifies` - изменяет
- `implements` - реализует
- `powers` - обеспечивает работу
- `enhances` - улучшает

### Оптимизация структуры
- Избегай глубокой вложенности
- Группируй связанные observations
- Используй четкие, конкретные формулировки
- Периодически проверяй на дубликаты

---

## 🛠️ Технические детали

### Хранение данных
- Файл: `memory.json` (в корне проекта)
- Формат: JSON Lines (каждая строка - отдельная сущность/связь)
- Обновление: автоматически при использовании MCP методов
- Размер: рекомендуется до 100KB для быстрой работы

### Конфигурация
```json
{
  "memory": {
    "command": "docker",
    "args": [
      "run", "-i", "--rm",
      "--label", "com.docker.compose.project=mcp",
      "-v", ".:/local-directory",
      "-e", "MEMORY_FILE_PATH=/local-directory/memory.json",
      "mcp/memory"
    ]
  }
}
```

### Поддержание качества
- **Периодическая проверка**: раз в месяц проверять актуальность записей
- **Очистка**: удалять устаревшие или неиспользуемые записи
- **Валидация**: проверять целостность файла после изменений
- **Резервные копии**: делать бэкапы перед крупными изменениями

---

## 📚 Дополнительные ресурсы

- [Best Practices](memory-mcp-best-practices.qmd) - подробные правила
- [AGENTS.md](../.cursor/AGENTS.md) - раздел "MEMORY MCP - ПРАВИЛА ИСПОЛЬЗОВАНИЯ"

---

**Последнее обновление**: 2025-01-19

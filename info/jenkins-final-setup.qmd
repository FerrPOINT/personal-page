# Финальная настройка Jenkins деплоя

## ✅ Текущий статус

- ✅ Jenkins доступен: `http://192.168.1.49:32768/`
- ✅ Все плагины установлены
- ✅ SSH credentials настроены (`jenkins-ssh-deploy-key`)
- ✅ Jenkins job создан (`personal-page-deploy`)
- ✅ Jenkinsfile готов
- ✅ Скрипт `deploy.sh` создан

---

## 🔧 Что нужно донастроить

### 1. Обновить SSH credentials в Jenkins

Текущие credentials используют ключ `jenkins_deploy_key`, но нужно использовать ваш ключ `id_rsa`.

**Вариант A: Обновить существующие credentials**

1. Откройте Jenkins: `http://192.168.1.49:32768/`
2. **Manage Jenkins** → **Credentials** → **System** → **Global credentials**
3. Найдите `jenkins-ssh-deploy-key`
4. Нажмите на него для редактирования
5. Вставьте содержимое вашего `~/.ssh/id_rsa` в поле **Private Key**
6. Сохраните

**Вариант B: Использовать скрипт**

```bash
# Если у вас есть доступ к ключу id_rsa
cat ~/.ssh/id_rsa | pbcopy  # или скопируйте вручную
# Затем вставьте в Jenkins UI
```

---

### 2. Проверить/обновить хост в Jenkinsfile

Текущая конфигурация:
- **DEPLOY_HOST**: `azhukov-dev`
- **DEPLOY_PORT**: `22`

Если `azhukov-dev` не резолвится на Jenkins сервере, нужно:

**Вариант A: Использовать реальный IP/хост**

Обновите `Jenkinsfile`:
```groovy
DEPLOY_HOST = '7eb10d5af2ad.vps.myjino.ru'  // или реальный IP
DEPLOY_PORT = '22'  // или другой порт если нужен
```

**Вариант B: Настроить SSH config на Jenkins сервере**

Если Jenkins запущен в Docker, добавьте SSH config в контейнер или используйте реальный хост.

---

### 3. Подготовить сервер деплоя

На сервере `azhukov-dev` нужно:

```bash
# Подключитесь к серверу
ssh -i ~/.ssh/id_rsa root@azhukov-dev

# Проверьте/создайте директорию
mkdir -p /opt/personal-page
cd /opt/personal-page

# Если репозиторий еще не клонирован:
git clone https://github.com/FerrPOINT/personal-page.git .

# Или если уже клонирован, обновите:
git fetch origin
git checkout main

# Проверьте, что Docker установлен
docker --version
docker compose version

# Проверьте, что есть env.prod
ls -la env.prod
# Если нет, создайте на основе env.example.txt
```

---

### 4. Запустить тестовый деплой

После настройки:

```bash
# Через скрипт
bash scripts/trigger-jenkins-build.sh

# Или через Jenkins UI
# Откройте: http://192.168.1.49:32768/job/personal-page-deploy
# Нажмите "Build Now"
```

---

## 📋 Чек-лист финальной настройки

- [ ] SSH credentials в Jenkins обновлены с правильным ключом (`id_rsa`)
- [ ] Хост в Jenkinsfile правильный (или настроен SSH config на Jenkins)
- [ ] Репозиторий клонирован на сервере в `/opt/personal-page`
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Файл `env.prod` существует на сервере
- [ ] Тестовый build запущен и прошел успешно

---

## 🐛 Troubleshooting

### Проблема: Jenkins не может подключиться к серверу

**Решение:**
1. Проверьте, что SSH ключ правильный в Jenkins credentials
2. Проверьте, что хост резолвится: используйте реальный IP вместо алиаса
3. Проверьте firewall правила

### Проблема: Репозиторий не найден на сервере

**Решение:**
```bash
ssh -i ~/.ssh/id_rsa root@azhukov-dev
cd /opt
git clone https://github.com/FerrPOINT/personal-page.git personal-page
```

### Проблема: Скрипт deploy.sh не найден

**Решение:**
Скрипт будет в репозитории после `git pull`. Убедитесь, что репозиторий обновлен.

---

## 🚀 После настройки

Pipeline будет автоматически:
1. Клонировать код на Jenkins
2. Подключаться к серверу через SSH
3. Обновлять код на сервере (`git pull`)
4. Запускать `scripts/deploy.sh` на сервере
5. Собирать и запускать Docker контейнеры
6. Проверять здоровье сервисов

---

**Готово к финальной настройке!**


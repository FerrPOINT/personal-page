# Конфигурация сервера для Jenkins

## 🖥️ Сервер деплоя

**Хост**: `azhukov-dev` (алиас)  
**Реальный хост**: нужно уточнить  
**Порт**: стандартный SSH (22) или другой  
**Пользователь**: `root`  
**SSH ключ**: `~/.ssh/id_rsa` или `~/.ssh/id_ed25519`

---

## ⚙️ Настройка Jenkinsfile

Текущая конфигурация в `Jenkinsfile`:

```groovy
DEPLOY_HOST = 'azhukov-dev'  // Алиас или реальный хост
DEPLOY_PORT = '22'           // SSH порт
DEPLOY_USER = 'root'
DEPLOY_PATH = '/opt/personal-page'
```

---

## ✅ Что нужно проверить на сервере

1. **Репозиторий клонирован:**
   ```bash
   ssh -i ~/.ssh/id_rsa root@azhukov-dev
   ls -la /opt/personal-page
   ```

2. **Docker установлен:**
   ```bash
   docker --version
   docker compose version
   ```

3. **Git настроен:**
   ```bash
   git --version
   cd /opt/personal-page && git remote -v
   ```

4. **Скрипт deploy.sh будет доступен:**
   - После `git pull` скрипт будет в `scripts/deploy.sh`
   - Нужно сделать его исполняемым: `chmod +x scripts/deploy.sh`

---

## 🔧 Если алиас не работает в Jenkins

Если `azhukov-dev` не резолвится на Jenkins сервере, используйте реальный хост:

```groovy
DEPLOY_HOST = '7eb10d5af2ad.vps.myjino.ru'
DEPLOY_PORT = '49233'
```

Или настройте SSH config на Jenkins сервере с алиасом.

---

## 📋 Чек-лист настройки

- [ ] Определен реальный хост для `azhukov-dev`
- [ ] SSH ключ добавлен в Jenkins credentials
- [ ] Репозиторий клонирован на сервере в `/opt/personal-page`
- [ ] Docker и Docker Compose установлены на сервере
- [ ] Git настроен на сервере
- [ ] Jenkinsfile обновлен с правильным хостом

---

**Нужно уточнить реальный хост для azhukov-dev!**


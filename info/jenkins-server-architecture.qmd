# Архитектура серверов для Jenkins CI/CD

## 🖥️ Серверы в системе

### 1. Jenkins Server
- **IP/URL**: `192.168.1.49:32768`
- **Тип**: Локальная сеть (возможно, Docker контейнер)
- **Роль**: CI/CD оркестратор
- **Что на нем**: Jenkins, плагины

### 2. Deployment Server (Production)
- **Hostname**: `7eb10d5af2ad.vps.myjino.ru`
- **Port**: `49233`
- **User**: `root`
- **Роль**: Сервер, где развернуто приложение
- **Что на нем**: Docker, Docker Compose, приложение, база данных

---

## ❓ Вопросы для уточнения

### На каком сервере что устанавливать?

#### Вариант A: Jenkins на отдельном сервере
- Jenkins установлен на `192.168.1.49` (локальный сервер/контейнер)
- Сервер деплоя: `7eb10d5af2ad.vps.myjino.ru` (VPS)
- **Нужно**: Настроить SSH доступ от Jenkins к серверу деплоя

#### Вариант B: Jenkins на том же сервере, что и приложение
- Jenkins и приложение на одном сервере
- **Нужно**: Установить Jenkins на сервере деплоя

#### Вариант C: Jenkins в Docker контейнере
- Jenkins запущен в Docker на `192.168.1.49`
- **Нужно**: Настроить Docker-in-Docker или SSH доступ

---

## 🔍 Что нужно проверить

1. **Где физически находится Jenkins?**
   - На `192.168.1.49`?
   - В Docker контейнере?
   - На отдельной машине?

2. **Нужно ли устанавливать что-то на сервере деплоя?**
   - Скрипт `deploy.sh` уже есть в репозитории
   - Docker и Docker Compose уже установлены?
   - Git настроен?

3. **Какой доступ есть?**
   - Jenkins может SSH к серверу деплоя?
   - Есть ли сетевой доступ между серверами?

---

## 📋 Текущая конфигурация

### Jenkins
- URL: `http://192.168.1.49:32768/`
- Пользователь: `ferrpoint`
- Job: `personal-page-deploy`
- Credentials: `jenkins-ssh-deploy-key` (настроены)

### Сервер деплоя
- Host: `7eb10d5af2ad.vps.myjino.ru:49233`
- User: `root`
- Path: `/opt/personal-page`
- SSH ключ: `~/.ssh/jenkins_deploy_key` (создан, но не проверен доступ)

---

## ✅ Что уже сделано

- ✅ Jenkins job создан
- ✅ Плагины установлены
- ✅ SSH credentials настроены в Jenkins
- ✅ Скрипт `deploy.sh` создан
- ✅ Jenkinsfile упрощен

---

## ❓ Что нужно уточнить

**На каком сервере нужно устанавливать/настраивать что-то дополнительно?**

1. На сервере деплоя (`7eb10d5af2ad.vps.myjino.ru`):
   - Скрипт `deploy.sh` будет склонирован из репозитория
   - Docker уже установлен?
   - Git настроен для клонирования репозитория?

2. На Jenkins сервере (`192.168.1.49`):
   - Все уже настроено?
   - Нужны ли дополнительные плагины?

---

**Нужно уточнить архитектуру перед продолжением!**


# ТЗ: Telegram-бот для уведомлений из формы контактов

## Краткое описание
Создать систему уведомлений через Telegram-бота для формы контактов на сайте. Форма сохраняет данные (Name, Email, Message) в базу данных, фоновый процесс находит неотправленные сообщения и отправляет их в Telegram через бота.

## Цель
Автоматизировать получение уведомлений о новых сообщениях из формы контактов через Telegram-бота, обеспечив надежную доставку и обработку сообщений.

## Архитектурная схема

```{mermaid}
graph TD
    A[Пользователь] -->|Заполняет форму| B[Frontend React]
    B -->|POST /api/contact| C[Backend API]
    C -->|Сохраняет| D[(SQLite)]
    D -->|Статус: pending| E[Worker Process]
    E -->|Проверяет каждые 5 мин| D
    E -->|Находит pending| F[Telegram Bot API]
    F -->|Отправляет| G[Telegram User]
    E -->|Обновляет статус| D
    D -->|Статус: sent/failed| H[Логирование]
    
    style A fill:#e1f5ff
    style B fill:#fff4e1
    style C fill:#ffe1f5
    style D fill:#e1ffe1
    style E fill:#f5e1ff
    style F fill:#ffe1e1
    style G fill:#e1f5ff
    style H fill:#f0f0f0
```

## Схема данных

```{mermaid}
erDiagram
    MESSAGES {
        id uuid PK
        name varchar NOT NULL
        email varchar NOT NULL
        message text NOT NULL
        status varchar NOT NULL
        created_at timestamp NOT NULL
        sent_at timestamp NULL
        error_message text NULL
    }
    
    MESSAGES ||--o{ STATUS_ENUM : "has status"
    
    STATUS_ENUM {
        pending "Не отправлено"
        sent "Отправлено"
        failed "Ошибка отправки"
    }
```

## Функциональные требования

### FR-001: Сохранение данных формы в БД
- ✅ Форма отправляет данные (Name, Email, Message) на backend API
- ✅ Backend API валидирует входные данные
- ✅ Данные сохраняются в таблицу `messages` со статусом `pending`
- ✅ API возвращает успешный ответ клиенту

### FR-002: Фоновый процесс обработки сообщений
- ✅ Worker-процесс запускается автоматически
- ✅ Worker проверяет БД каждые 5 минут на наличие сообщений со статусом `pending` или `failed` (для повторных попыток)
- ✅ Worker обрабатывает сообщения по одному (без дублирования)
- ✅ Worker логирует все операции
- ✅ Failed сообщения обрабатываются в следующей итерации (каждые 5 минут)

### FR-003: Интеграция с Telegram
- ✅ Telegram-бот создан через @BotFather (получен токен)
- ✅ Worker отправляет форматированное сообщение в Telegram через Bot API
- ✅ Сообщение отправляется в личный чат пользователя (Chat ID)
- ✅ Сообщение содержит: имя отправителя, email, текст сообщения, дату создания
- ✅ После успешной отправки статус обновляется на `sent`
- ✅ При ошибке статус обновляется на `failed` с записью ошибки
- ✅ **Важно**: Бот не интерактивный, только отправка уведомлений (без обработки команд)

### FR-004: Обработка ошибок
- ✅ При ошибке отправки в Telegram статус меняется на `failed`
- ✅ Ошибка логируется с полным контекстом
- ✅ Worker продолжает работу при ошибке одного сообщения
- ✅ Повторная попытка отправки для failed сообщений: worker обрабатывает failed сообщения в следующей итерации (каждые 5 минут)

## Технические требования

### TR-001: Backend API
- 🔧 Node.js/Express или Python/FastAPI
- 🔧 TypeScript для Node.js или Python 3.10+
- 🔧 Валидация входных данных (name, email, message)
- 🔧 CORS настройка для фронтенда
- 🔧 **Развертывание**: Все сервисы в Docker (docker-compose)
- 🔧 **Порт**: Уникальный порт для API (настраивается через переменную окружения)

### TR-002: База данных
- 🔧 SQLite (better-sqlite3)
- 🔧 Таблица `messages` с полями: id (TEXT/UUID), name, email, message, status, created_at, sent_at, error_message
- 🔧 Индекс на поле `status` для быстрого поиска
- 🔧 Миграции для управления схемой

### TR-003: Telegram Bot
- 🔧 Библиотека: `node-telegram-bot-api` (Node.js) или `python-telegram-bot` (Python)
- 🔧 Токен бота хранится в переменных окружения
- 🔧 Форматирование сообщений в Markdown или HTML
- 🔧 Обработка ошибок Telegram API
- 🔧 **Важно**: Бот используется только для отправки уведомлений (не интерактивный, без обработки команд)

### TR-004: Worker Process
- 🔧 Отдельный процесс/сервис для обработки сообщений
- 🔧 Периодическая проверка БД (каждые 5 минут)
- 🔧 Обрабатывает сообщения со статусом `pending` и `failed` (повторные попытки)
- 🔧 Транзакционная обработка (блокировка записей)
- 🔧 Логирование всех операций
- 🔧 Graceful shutdown

### TR-005: Безопасность
- 🔧 Валидация email формата
- 🔧 Защита от SQL-инъекций (параметризованные запросы)
- 🔧 Секреты в переменных окружения (не в коде)
- 🔧 Логирование без чувствительных данных

## Критерии готовности

### Phase 1: Backend Infrastructure
- [ ] Структура проекта создана
- [ ] package.json настроен с зависимостями
- [ ] TypeScript настроен
- [ ] Переменные окружения настроены
- [ ] docker-compose.yml обновлен

### Phase 2: Database Setup
- [ ] SQLite база данных создана
- [ ] Миграция создана и применена
- [ ] Таблица messages создана с правильной схемой
- [ ] Индексы созданы

### Phase 3: Backend API
- [ ] Подключение к БД работает
- [ ] Модель Message создана
- [ ] Валидация работает
- [ ] API endpoint POST /api/contact работает
- [ ] Express сервер настроен
- [ ] Все тесты API проходят

### Phase 4: Frontend Integration
- [ ] Contact.tsx обновлен (mailto → fetch)
- [ ] CORS/proxy настроен
- [ ] Интеграция работает end-to-end

### Phase 5: Telegram Bot
- [ ] Telegram бот создан
- [ ] Токен и Chat ID получены
- [ ] Telegram сервис создан
- [ ] Отправка сообщений работает

### Phase 6: Worker Process
- [ ] Worker создан и запускается
- [ ] Проверка БД работает (каждые 5 минут)
- [ ] Отправка в Telegram работает
- [ ] Статусы обновляются корректно

### Phase 7: Testing
- [ ] End-to-end тестирование пройдено
- [ ] Тестирование ошибок пройдено
- [ ] Нагрузочное тестирование пройдено
- [ ] Логирование проверено

### Phase 8: Deployment
- [ ] Документация обновлена
- [ ] docker-compose настроен для production
- [ ] Все сервисы работают в Docker (backend с SQLite)
- [ ] Порт API уникальный и настроен
- [ ] Финальная проверка пройдена
- [ ] Все чекапы из всех фаз пройдены

## Нефункциональные требования

### Производительность
- API должен отвечать в течение 200ms для сохранения сообщения
- Worker должен обрабатывать до 100 сообщений в минуту
- БД запросы должны выполняться в течение 50ms

### Надежность
- Система должна работать 24/7
- При падении worker должен автоматически перезапускаться
- Ошибки не должны останавливать обработку других сообщений

### Масштабируемость
- Архитектура должна позволять запуск нескольких worker-процессов
- БД должна выдерживать до 1000 сообщений в день


# Тест-кейсы: Telegram-бот для уведомлений из формы контактов

## 🧪 ПОЗИТИВНЫЕ СЦЕНАРИИ

### TC-001: Успешное сохранение данных формы в БД
**Предусловия**: Backend API запущен, БД настроена
**Шаги**:
1. Открыть форму контактов на сайте
2. Ввести валидные данные:
   - Name: "Иван Иванов"
   - Email: "ivan@example.com"
   - Message: "Тестовое сообщение"
3. Нажать "Send Message"

**Ожидаемый результат**:
- Форма отправляет POST запрос на `/api/contact`
- API возвращает статус 200 OK
- Данные сохраняются в БД со статусом `pending`
- Форма показывает сообщение об успешной отправке
- В БД создана запись с корректными данными

### TC-002: Worker находит и обрабатывает pending сообщение
**Предусловия**: В БД есть сообщение со статусом `pending`, Worker запущен
**Шаги**:
1. Worker проверяет БД (автоматически каждые 5 минут)
2. Находит сообщение со статусом `pending`
3. Отправляет сообщение в Telegram

**Ожидаемый результат**:
- Worker находит сообщение в БД
- Сообщение отправлено в Telegram
- Статус в БД обновлен на `sent`
- Поле `sent_at` заполнено текущей датой/временем
- В логах записана успешная операция

### TC-003: Успешное получение уведомления в Telegram
**Предусловия**: Telegram-бот настроен, Worker отправил сообщение
**Шаги**:
1. Worker отправляет сообщение в Telegram
2. Проверяем получение в Telegram

**Ожидаемый результат**:
- В Telegram получено сообщение от бота
- Сообщение содержит:
  - Имя отправителя
  - Email
  - Текст сообщения
  - Дата создания
- Форматирование читаемое и понятное

### TC-004: Обработка нескольких сообщений подряд
**Предусловия**: В БД несколько сообщений со статусом `pending`
**Шаги**:
1. Worker запускает цикл обработки
2. Находит несколько pending сообщений

**Ожидаемый результат**:
- Worker обрабатывает все сообщения по очереди
- Каждое сообщение отправлено в Telegram
- Все статусы обновлены на `sent`
- Нет дублирования отправок

## ❌ НЕГАТИВНЫЕ СЦЕНАРИИ

### TC-101: Отправка формы с невалидным email
**Предусловия**: Форма открыта
**Шаги**:
1. Ввести невалидный email: "invalid-email"
2. Заполнить остальные поля
3. Нажать "Send Message"

**Ожидаемый результат**:
- Форма показывает ошибку валидации
- Запрос не отправляется на сервер
- Данные не сохраняются в БД

### TC-102: Отправка формы с пустыми обязательными полями
**Предусловия**: Форма открыта
**Шаги**:
1. Оставить поле Name пустым
2. Заполнить Email и Message
3. Нажать "Send Message"

**Ожидаемый результат**:
- Форма показывает ошибку валидации
- Запрос не отправляется
- Данные не сохраняются

### TC-103: Ошибка при отправке в Telegram (недоступен API)
**Предусловия**: Worker нашел pending сообщение, Telegram API недоступен
**Шаги**:
1. Worker пытается отправить сообщение
2. Telegram API возвращает ошибку

**Ожидаемый результат**:
- Статус в БД обновлен на `failed`
- Поле `error_message` содержит описание ошибки
- Worker продолжает работу (не падает)
- В логах записана ошибка с контекстом

### TC-104: Ошибка подключения к БД
**Предусловия**: Worker запущен, БД недоступна
**Шаги**:
1. Worker пытается подключиться к БД
2. БД недоступна (таймаут/ошибка подключения)

**Ожидаемый результат**:
- Worker логирует ошибку подключения
- Worker повторяет попытку через некоторое время
- Worker не падает, продолжает работу

### TC-105: Повторная попытка отправки failed сообщения
**Предусловия**: В БД есть сообщение со статусом `failed`, Worker запущен
**Шаги**:
1. Worker проверяет БД (автоматически каждые 5 минут)
2. Находит сообщение со статусом `failed`
3. Пытается отправить снова

**Ожидаемый результат**:
- Worker находит failed сообщение
- Worker пытается отправить сообщение повторно
- При успехе статус обновляется на `sent`
- При повторной ошибке статус остается `failed`, error_message обновляется
- Worker продолжает работу

## 🔄 ИНТЕГРАЦИОННЫЕ ТЕСТЫ

### TC-201: Полный цикл от формы до Telegram
**Предусловия**: Вся система настроена и запущена
**Шаги**:
1. Пользователь заполняет форму и отправляет
2. Данные сохраняются в БД
3. Worker находит сообщение
4. Worker отправляет в Telegram
5. Проверяем получение в Telegram

**Ожидаемый результат**:
- Все этапы выполнены успешно
- Данные корректны на всех этапах
- Нет потери данных
- Время обработки в пределах нормы (< 1 минуты)

### TC-202: Восстановление после сбоя Worker
**Предусловия**: Worker упал во время обработки
**Шаги**:
1. Worker обрабатывает сообщение
2. Worker падает (симуляция)
3. Worker перезапускается
4. Worker продолжает обработку

**Ожидаемый результат**:
- Сообщение не потеряно
- Worker обрабатывает оставшиеся pending сообщения
- Нет дублирования отправок

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

### TC-301: Нагрузочное тестирование API
**Предусловия**: API запущен
**Шаги**:
1. Отправить 100 запросов подряд
2. Измерить время ответа

**Ожидаемый результат**:
- Все запросы обработаны
- Среднее время ответа < 200ms
- Нет ошибок 500
- БД выдерживает нагрузку

### TC-302: Нагрузочное тестирование Worker
**Предусловия**: Worker запущен, в БД 100 pending сообщений
**Шаги**:
1. Запустить Worker
2. Измерить время обработки всех сообщений

**Ожидаемый результат**:
- Все сообщения обработаны
- Время обработки < 5 минут
- Нет ошибок обработки
- Все статусы обновлены корректно


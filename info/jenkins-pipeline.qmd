# Jenkins CI/CD Pipeline для автоматического деплоя

## 🎯 Обзор

Автоматический pipeline для деплоя приложения после обновлений в репозитории через Jenkins.

**Jenkins URL**: `http://192.168.1.49:32768/`  
**Пользователь**: `ferrpoint`  
**Токен**: `1191a9f019fc3989d7a5ff30d456fb9cf3`

---

## 📋 Архитектура Pipeline

```{mermaid}
graph TD
    A[Git Push] --> B[Jenkins Webhook]
    B --> C[Checkout Code]
    C --> D[Build Docker Images]
    D --> E[Run Tests]
    E --> F[Deploy to Server]
    F --> G[Run Migrations]
    G --> H[Health Check]
    H --> I{Success?}
    I -->|Yes| J[✅ Deployment Complete]
    I -->|No| K[❌ Rollback/Alert]
    
    style A fill:#e1f5ff
    style J fill:#d4edda
    style K fill:#f8d7da
```

---

## 🔧 Настройка Jenkins

### Шаг 1: Установка необходимых плагинов

В Jenkins перейдите: **Manage Jenkins** → **Plugins** → **Available plugins**

Установите следующие плагины:

- ✅ **Pipeline** - для работы с Jenkinsfile
- ✅ **SSH Pipeline Steps** - для SSH подключения к серверу
- ✅ **Docker Pipeline** - для работы с Docker
- ✅ **Git Plugin** - для работы с Git репозиториями
- ✅ **GitHub Plugin** (опционально) - для интеграции с GitHub webhooks

### Шаг 2: Настройка SSH Credentials

1. Перейдите: **Manage Jenkins** → **Credentials** → **System** → **Global credentials**
2. Нажмите **Add Credentials**
3. Заполните:
   - **Kind**: SSH Username with private key
   - **ID**: `jenkins-ssh-deploy-key`
   - **Username**: `root`
   - **Private Key**: Выберите **Enter directly** и вставьте приватный SSH ключ для доступа к серверу `7eb10d5af2ad.vps.myjino.ru:49233`
   - **Passphrase**: (если ключ защищен паролем)

**Важно**: Убедитесь, что SSH ключ имеет доступ к серверу деплоя.

### Шаг 3: Настройка Git Credentials (если репозиторий приватный)

1. **Manage Jenkins** → **Credentials** → **System** → **Global credentials**
2. **Add Credentials**
3. Заполните:
   - **Kind**: SSH Username with private key (для SSH) или Username with password (для HTTPS)
   - **ID**: `jenkins-git-credentials`
   - Укажите данные для доступа к репозиторию

### Шаг 4: Создание Pipeline Job

1. На главной странице Jenkins нажмите **New Item**
2. Введите имя: `personal-page-deploy`
3. Выберите тип: **Pipeline**
4. Нажмите **OK**

### Шаг 5: Настройка Pipeline Job

#### 5.1 Общие настройки

- **Description**: "Автоматический деплой personal-page приложения"
- **GitHub project** (опционально): URL вашего репозитория

#### 5.2 Build Triggers

Выберите один из вариантов:

**Вариант A: Polling SCM (проверка изменений по расписанию)**
- ✅ **Poll SCM**
- **Schedule**: `H/5 * * * *` (каждые 5 минут)

**Вариант B: GitHub Webhook (рекомендуется)**
- ✅ **GitHub hook trigger for GITScm polling**

Для варианта B настройте webhook в GitHub:
1. Перейдите в репозиторий: **Settings** → **Webhooks** → **Add webhook**
2. **Payload URL**: `http://192.168.1.49:32768/github-webhook/`
3. **Content type**: `application/json`
4. **Events**: Выберите **Just the push event**
5. Нажмите **Add webhook**

#### 5.3 Pipeline Configuration

- **Definition**: **Pipeline script from SCM**
- **SCM**: **Git**
- **Repository URL**: URL вашего репозитория (SSH или HTTPS)
- **Credentials**: Выберите `jenkins-git-credentials` (если репозиторий приватный)
- **Branches to build**: `*/main` или `*/master`
- **Script Path**: `Jenkinsfile`
- **Lightweight checkout**: ❌ (снимите галочку)

#### 5.4 Сохранение

Нажмите **Save**

---

## 🚀 Использование Pipeline

### Ручной запуск

1. Откройте job `personal-page-deploy`
2. Нажмите **Build Now**
3. Следите за прогрессом в **Console Output**

### Автоматический запуск

Pipeline автоматически запускается при:
- Push в ветку `main` или `master` (если настроен webhook)
- По расписанию (если настроен Poll SCM)

---

## 📊 Структура Pipeline

### Этапы выполнения

```{mermaid}
sequenceDiagram
    participant Git as Git Repository
    participant Jenkins as Jenkins Server
    participant Docker as Docker Build
    participant Server as Deploy Server
    
    Git->>Jenkins: Webhook/Poll Trigger
    Jenkins->>Git: Checkout Code
    Git-->>Jenkins: Source Code
    Jenkins->>Docker: Build Images
    Docker-->>Jenkins: Images Ready
    Jenkins->>Server: SSH Connect
    Server->>Server: Git Pull
    Server->>Server: Docker Compose Build
    Server->>Server: Run Migrations
    Server->>Jenkins: Health Check
    Jenkins-->>Git: Status Update
```

### Детальное описание этапов

#### 1. **Checkout**
- Клонирование репозитория
- Сохранение commit hash для отслеживания

#### 2. **Build**
- Сборка Docker образов для frontend и backend
- Использование `docker-compose build` или `docker compose build`

#### 3. **Test**
- Запуск тестов (если настроены)
- Валидация кода

#### 4. **Deploy**
- SSH подключение к серверу
- Обновление кода через `git pull`
- Копирование `env.prod` в `.env`
- Остановка старых контейнеров
- Сборка и запуск новых контейнеров
- Запуск миграций базы данных

#### 5. **Health Check**
- Проверка `/health` endpoint backend
- Проверка статуса контейнеров
- Валидация работоспособности сервисов

---

## 🔐 Безопасность

### Рекомендации

1. **Хранение секретов**:
   - Используйте Jenkins Credentials для всех секретов
   - Не храните пароли и токены в Jenkinsfile
   - Используйте переменные окружения через Credentials Binding

2. **SSH ключи**:
   - Используйте отдельный SSH ключ для деплоя (deploy key)
   - Ограничьте права доступа ключа на сервере
   - Регулярно ротируйте ключи

3. **Доступ к Jenkins**:
   - Ограничьте доступ к Jenkins UI
   - Используйте HTTPS для Jenkins (если доступен)
   - Настройте firewall правила

---

## 🛠️ Проверка подключения к Jenkins

### Через curl

```bash
# Проверка доступности Jenkins
curl -u ferrpoint:1191a9f019fc3989d7a5ff30d456fb9cf3 \
  http://192.168.1.49:32768/api/json

# Получение информации о версии
curl -u ferrpoint:1191a9f019fc3989d7a5ff30d456fb9cf3 \
  http://192.168.1.49:32768/api/json?tree=version

# Список всех jobs
curl -u ferrpoint:1191a9f019fc3989d7a5ff30d456fb9cf3 \
  http://192.168.1.49:32768/api/json?tree=jobs[name,url]
```

### Через браузер

1. Откройте: `http://192.168.1.49:32768/`
2. Войдите с учетными данными: `ferrpoint` / `[ваш пароль]`
3. Или используйте API Token для доступа через API

---

## 📝 Настройка переменных окружения

### На сервере деплоя

Убедитесь, что файл `env.prod` существует на сервере в `/opt/personal-page/`:

```bash
ssh root@7eb10d5af2ad.vps.myjino.ru -p 49233
cd /opt/personal-page
cat env.prod
```

Файл должен содержать все необходимые переменные окружения для production.

---

## 🐛 Troubleshooting

### Проблема: Jenkins не может подключиться к серверу

**Решение**:
1. Проверьте SSH ключ в Jenkins Credentials
2. Проверьте доступность сервера: `ssh -p 49233 root@7eb10d5af2ad.vps.myjino.ru`
3. Проверьте firewall правила на сервере

### Проблема: Docker образы не собираются

**Решение**:
1. Убедитесь, что Docker установлен на Jenkins agent
2. Проверьте права доступа к Docker: `docker ps`
3. Проверьте логи сборки в Jenkins Console Output

### Проблема: Health check не проходит

**Решение**:
1. Проверьте логи контейнеров на сервере: `docker compose logs`
2. Проверьте, что backend доступен на порту 9000
3. Проверьте переменные окружения в `.env` на сервере

### Проблема: Webhook не срабатывает

**Решение**:
1. Проверьте URL webhook в настройках GitHub
2. Проверьте доступность Jenkins из интернета (если нужно)
3. Проверьте логи Jenkins: **Manage Jenkins** → **System Log**

### Проблема: Миграции не выполняются

**Решение**:
1. Проверьте, что контейнер backend запущен
2. Проверьте права доступа к директории `data/` на сервере
3. Запустите миграции вручную: `docker compose exec backend npm run migrate`

---

## 📊 Мониторинг и уведомления

### Настройка уведомлений

В секции `post` Jenkinsfile можно добавить уведомления:

```groovy
post {
    success {
        // Уведомление в Telegram (если настроен бот)
        sh '''
            curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                -d "chat_id=${TELEGRAM_USER_ID}" \
                -d "text=✅ Деплой успешно завершен!"
        '''
    }
    failure {
        // Уведомление об ошибке
        sh '''
            curl -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
                -d "chat_id=${TELEGRAM_USER_ID}" \
                -d "text=❌ Деплой завершился с ошибкой!"
        '''
    }
}
```

### Просмотр истории деплоев

1. Откройте job `personal-page-deploy`
2. В разделе **Build History** видна история всех запусков
3. Кликните на конкретный build для просмотра деталей

---

## ✅ Чек-лист настройки

- [ ] Jenkins установлен и доступен
- [ ] Установлены необходимые плагины
- [ ] Настроены SSH credentials для сервера деплоя
- [ ] Настроены Git credentials (если репозиторий приватный)
- [ ] Создан Pipeline job
- [ ] Настроен webhook в GitHub (или Poll SCM)
- [ ] Jenkinsfile добавлен в репозиторий
- [ ] Файл `env.prod` существует на сервере
- [ ] Тестовый запуск pipeline выполнен успешно
- [ ] Health check проходит после деплоя

---

## 📞 Дополнительные ресурсы

- **Jenkins Documentation**: https://www.jenkins.io/doc/
- **Pipeline Syntax**: https://www.jenkins.io/doc/book/pipeline/syntax/
- **SSH Pipeline Steps**: https://plugins.jenkins.io/ssh-steps/
- **Docker Pipeline**: https://plugins.jenkins.io/docker-workflow/

---

## 🔄 Обновление Pipeline

При изменении `Jenkinsfile`:

1. Закоммитьте изменения в репозиторий
2. Jenkins автоматически подхватит изменения при следующем запуске
3. Или нажмите **Build Now** для немедленного запуска

---

**Последнее обновление**: 2026-01-XX


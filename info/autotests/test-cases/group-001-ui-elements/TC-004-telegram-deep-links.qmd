---
title: "Тест-кейс: TelegramContactButton - Deep Links и платформы"
test-case-id: "TC-004"
group-id: "group-001"
date: "2026-01-25"
---

# Тест-кейс: TelegramContactButton - Deep Links и платформы

## 📋 Общая информация
- **ID**: TC-004
- **Группа**: group-001-ui-elements
- **Тип**: MCP
- **Приоритет**: Medium
- **Статус**: 🔄 В процессе

## 🎯 Цель
Проверить правильность определения платформы и использования deep links.

## 📝 Предусловия
- Страница контактов открыта
- Компонент TelegramContactButton отображается

## 🔧 Шаги выполнения
1. Проверить логику определения платформы в `getTelegramLink()`
2. Проверить использование Telegram Web App API
3. Проверить использование deep links для мобильных устройств
4. Проверить fallback на web версию для десктопа

## ✅ Ожидаемый результат
- ✅ В Telegram Web App используется `Telegram.WebApp.openTelegramLink()`
- ✅ На мобильных устройствах используется `telegram://resolve?domain=username`
- ✅ На десктопе используется `https://t.me/username`
- ✅ Логика определения платформы работает корректно

## 📊 Фактический результат
**Статический анализ кода:**
- ✅ `getTelegramLink()` правильно определяет платформу (строки 25-41)
- ✅ Проверка Telegram Web App через `window.Telegram?.WebApp` (строка 27)
- ✅ Определение мобильных устройств через user agent (строки 32-33)
- ✅ Deep link для мобильных: `telegram://resolve?domain=${telegramUsername}` (строка 22)
- ✅ Web fallback: `https://t.me/${telegramUsername}` (строка 21)
- ✅ В `handleOpenTelegram` используется правильный API для Web App (строка 47)

**Проверка логики:**
- ✅ Правильная последовательность проверок: Web App → Mobile → Desktop
- ✅ Использование `window.open()` с правильными параметрами безопасности (строка 51)

## 🔍 Детали выполнения

### Код определения платформы
```typescript
// TelegramContactButton.tsx:25-41
const getTelegramLink = (): string => {
  // ✅ Проверка Telegram Web App
  if (typeof window !== 'undefined' && window.Telegram?.WebApp) {
    return telegramUrl;
  }
  // ✅ Проверка мобильных устройств
  const isMobile = /android|iphone|ipad|ipod|blackberry|iemobile|opera mini/i.test(userAgent);
  if (isMobile) {
    return telegramDeepLink;
  }
  // ✅ Fallback на web
  return telegramUrl;
}
```

### Код открытия Telegram
```typescript
// TelegramContactButton.tsx:43-53
const handleOpenTelegram = () => {
  // ✅ Использование Web App API
  if (typeof window !== 'undefined' && window.Telegram?.WebApp) {
    window.Telegram.WebApp.openTelegramLink(`https://t.me/${telegramUsername}`);
  } else {
    // ✅ Использование getTelegramLink для определения платформы
    const link = getTelegramLink();
    window.open(link, '_blank', 'noopener,noreferrer');
  }
}
```

## 🐛 Найденные проблемы
Нет проблем при статическом анализе.

## 📝 Заметки
- Логика определения платформы реализована корректно
- Необходимо проверить в реальном браузере работу deep links на разных устройствах


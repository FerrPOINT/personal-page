---
title: "Тест-кейс: TelegramContactButton - Функция копирования"
test-case-id: "TC-002"
group-id: "group-001"
date: "2026-01-25"
---

# Тест-кейс: TelegramContactButton - Функция копирования

## 📋 Общая информация
- **ID**: TC-002
- **Группа**: group-001-ui-elements
- **Тип**: MCP
- **Приоритет**: High
- **Статус**: 🔄 В процессе

## 🎯 Цель
Проверить функцию копирования username в буфер обмена.

## 📝 Предусловия
- Страница контактов открыта
- Компонент TelegramContactButton отображается

## 🔧 Шаги выполнения
1. Найти кнопку копирования внутри TelegramContactButton
2. Нажать на кнопку копирования
3. Проверить, что основной обработчик не срабатывает (stopPropagation)
4. Проверить появление toast уведомления "Скопировано!"
5. Проверить изменение иконки (Clipboard → Check)
6. Проверить, что текст `@username` скопирован в буфер обмена
7. Проверить, что toast скрывается через 2 секунды

## ✅ Ожидаемый результат
- ✅ При клике на кнопку копирования основной обработчик не срабатывает
- ✅ Toast уведомление появляется
- ✅ Иконка меняется с Clipboard на Check
- ✅ Текст `@username` скопирован в буфер обмена
- ✅ Toast скрывается через 2 секунды
- ✅ Нет ошибок в консоли

## 📊 Фактический результат
**Статический анализ кода:**
- ✅ `handleCopy` использует `e.stopPropagation()` (строка 56)
- ✅ Используется `navigator.clipboard.writeText()` с fallback (строки 61-73)
- ✅ Toast показывается через `setShowToast(true)` (строка 76)
- ✅ Иконка меняется через `setCopied(true)` (строка 75)
- ✅ Таймер для скрытия toast установлен на 2000ms (строки 79-82)
- ✅ Обработка ошибок через try-catch (строки 60-85)

**Проверка логики:**
- ✅ Текст для копирования: `@${telegramUsername}` (строка 58)
- ✅ Fallback для старых браузеров через `document.execCommand('copy')` (строка 71)
- ✅ Анимация иконки через framer-motion AnimatePresence (строки 106-128)

## 🔍 Детали выполнения

### Код функции копирования
```typescript
// TelegramContactButton.tsx:55-86
const handleCopy = async (e: React.MouseEvent) => {
  e.stopPropagation(); // ✅ Правильно
  const textToCopy = `@${telegramUsername}`; // ✅ Правильный формат
  // ✅ Fallback для старых браузеров
  // ✅ Обработка ошибок
  // ✅ Установка состояния
}
```

### Проверка z-index
```typescript
// TelegramContactButton.tsx:102
className="... relative z-10" // ✅ Правильный z-index
```

## 🐛 Найденные проблемы
Нет проблем при статическом анализе.

## 📝 Заметки
- Функция копирования реализована корректно
- Необходимо проверить в реальном браузере работу clipboard API


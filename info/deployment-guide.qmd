# Инструкция по развертыванию приложения на сервере

## 🎯 Обзор

Приложение разворачивается через Docker Compose на VPS сервере.

**Сервер**: `7eb10d5af2ad.vps.myjino.ru:49233`  
**Пользователь**: `root`

---

## 📋 Шаг 1: Настройка SSH ключа для Git

### Если репозиторий приватный

**На сервере нужно добавить SSH ключ для доступа к Git репозиторию.**

#### Вариант 1: Использовать существующий ключ с вашего компьютера

1. **На вашем компьютере** скопируйте публичный SSH ключ:
```bash
cat ~/.ssh/id_rsa.pub
# или
cat ~/.ssh/id_ed25519.pub
```

2. **Добавьте ключ в GitHub/GitLab**:
   - GitHub: Settings → SSH and GPG keys → New SSH key
   - GitLab: Settings → SSH Keys → Add SSH Key

3. **На сервере** создайте SSH ключ (если его нет):
```bash
ssh root@7eb10d5af2ad.vps.myjino.ru -p 49233
ssh-keygen -t ed25519 -C "deploy@server"
# Нажмите Enter для всех вопросов (или укажите путь)
cat ~/.ssh/id_ed25519.pub
```

4. **Скопируйте публичный ключ** и добавьте в GitHub/GitLab (как в шаге 2)

#### Вариант 2: Использовать Deploy Key (рекомендуется для продакшн)

1. **На сервере** создайте SSH ключ:
```bash
ssh root@7eb10d5af2ad.vps.myjino.ru -p 49233
ssh-keygen -t ed25519 -C "deploy-key" -f ~/.ssh/deploy_key
cat ~/.ssh/deploy_key.pub
```

2. **Добавьте Deploy Key в репозиторий**:
   - GitHub: Settings → Deploy keys → Add deploy key
   - GitLab: Settings → Repository → Deploy Keys → Add key
   - **Важно**: Отметьте "Allow write access" если нужен push

3. **Настройте SSH config** на сервере:
```bash
nano ~/.ssh/config
```

Добавьте:
```
Host github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/deploy_key
    IdentitiesOnly yes
```

---

## 📋 Шаг 2: Подготовка сервера

### 2.1 Подключение к серверу

```bash
ssh root@7eb10d5af2ad.vps.myjino.ru -p 49233
```

### 2.2 Установка Docker и Docker Compose

Проверьте, установлены ли Docker и Docker Compose:

```bash
docker --version
docker-compose --version
```

Если не установлены, установите:

```bash
# Обновление пакетов
apt update && apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Установка Docker Compose
apt install docker-compose -y

# Проверка
docker --version
docker-compose --version
```

### 2.3 Установка Git (если не установлен)

```bash
apt install git -y
git --version
```

---

## 📋 Шаг 3: Клонирование репозитория

### 3.1 Выбор директории для проекта

```bash
# Клонируем в /opt/personal-page
cd /opt
```

### 3.2 Клонирование репозитория

**Если репозиторий приватный (SSH):**
```bash
git clone git@github.com:your-username/personal-page.git
# или
git clone git@gitlab.com:your-username/personal-page.git
```

**Если репозиторий публичный (HTTPS):**
```bash
git clone https://github.com/your-username/personal-page.git
```

**Перейдите в директорию проекта:**
```bash
cd /opt/personal-page
```

---

## 📋 Шаг 4: Настройка переменных окружения

### 4.1 Создание .env файла

```bash
# Используйте make local для автоматической настройки
make local

# Это скопирует env.local в .env и настроит окружение
# Если env.local нет, создайте его на основе env.example.txt
```

### 4.2 Редактирование .env файла

```bash
nano .env
```

**Обязательно обновите следующие переменные:**

```env
# API Configuration
# VITE_API_URL НЕ НУЖЕН - используется относительный путь /api
# Приложение автоматически использует тот же протокол и домен, что и страница

# Database (SQLite)
DATABASE_PATH=./data/database.db

# Telegram Bot
TELEGRAM_BOT_TOKEN=your_actual_bot_token_here
TELEGRAM_USER_ID=your_user_id_here

# API Configuration
API_PORT=9000
NODE_ENV=production

# Gemini API (если используется)
GEMINI_API_KEY=your_gemini_api_key_here
```

**Важные замечания:**
- `VITE_API_URL` **НЕ НУЖЕН** - приложение использует относительный путь `/api`
- Это позволяет работать и по HTTP, и по HTTPS без изменений
- `NODE_ENV` должен быть `production`
- `TELEGRAM_BOT_TOKEN` должен быть реальным токеном бота

### 4.3 Сохранение файла

В nano: `Ctrl+O` (сохранить), `Enter` (подтвердить), `Ctrl+X` (выйти)

---

## 📋 Шаг 5: Запуск приложения

### 5.1 Настройка окружения и запуск

```bash
# Настройка окружения (копирует env.local в .env)
make local

# Сборка и запуск в фоновом режиме
docker compose up -d --build

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f
```

### 5.2 Настройка Nginx (если нужно)

**Приложение использует относительный путь `/api`, поэтому nginx должен проксировать:**
- `/` → frontend (localhost:8888)
- `/api` → backend (localhost:9000)

Конфигурация nginx уже настроена в `/etc/nginx/sites-available/personal-page`

### 5.3 Проверка работы

**Проверьте логи:**
```bash
# Все сервисы
docker compose logs -f

# Только backend
docker compose logs -f backend

# Только frontend
docker compose logs -f frontend
```

**Проверьте доступность:**
- Frontend: `http://7eb10d5af2ad.vps.myjino.ru/` (через nginx на порту 80)
- Backend API: `http://7eb10d5af2ad.vps.myjino.ru/api` (через nginx)

---

## 📋 Шаг 6: Настройка Nginx (обязательно)

### 6.1 Установка Nginx

```bash
apt install -y nginx
```

### 6.2 Создание конфигурации

Создайте файл `/etc/nginx/sites-available/personal-page`:

```nginx
server {
    listen 80;
    server_name 7eb10d5af2ad.vps.myjino.ru;

    # Frontend - все запросы кроме /api
    location / {
        proxy_pass http://localhost:8888;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:9000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 6.3 Активация конфигурации

```bash
# Удалить дефолтную конфигурацию
rm -f /etc/nginx/sites-enabled/default

# Создать симлинк
ln -sf /etc/nginx/sites-available/personal-page /etc/nginx/sites-enabled/personal-page

# Проверить конфигурацию
nginx -t

# Перезапустить nginx
systemctl restart nginx
```

---

## 📋 Шаг 7: Настройка файрвола (если нужно)

Если порты не открыты, откройте их:

```bash
# UFW (если используется)
ufw allow 8888/tcp
ufw allow 9000/tcp
ufw reload

# Или iptables
iptables -A INPUT -p tcp --dport 8888 -j ACCEPT
iptables -A INPUT -p tcp --dport 9000 -j ACCEPT
```

---

## 📋 Шаг 8: Настройка автозапуска (опционально)

### 8.1 Создание systemd сервиса

Создайте файл `/etc/systemd/system/personal-page.service`:

```bash
nano /etc/systemd/system/personal-page.service
```

Содержимое:
```ini
[Unit]
Description=Personal Page Application
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/apps/personal-page
ExecStart=/usr/bin/docker-compose up -d
ExecStop=/usr/bin/docker-compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

**Активация:**
```bash
systemctl daemon-reload
systemctl enable personal-page.service
systemctl start personal-page.service
```

---

## 🔄 Обновление приложения

**Важно:** После обновления кода обязательно пересоберите frontend, так как Vite переменные встраиваются в код на этапе сборки.

### Обновление кода

```bash
cd /opt/personal-page
git pull origin main
make local  # обновит .env из env.local
docker compose up -d --build
docker compose logs -f
```

---

## 🛠️ Полезные команды

### Управление контейнерами

```bash
# Остановка
docker compose down

# Перезапуск
docker compose restart

# Пересборка
docker compose up -d --build

# Просмотр логов
docker compose logs -f

# Вход в контейнер
docker compose exec backend sh
docker compose exec frontend sh
```

### Очистка

```bash
# Остановка и удаление контейнеров
docker compose down

# Удаление с volumes (⚠️ удалит базу данных!)
docker compose down -v

# Удаление образов
docker compose down --rmi all
```

### Резервное копирование базы данных

```bash
# Копирование базы данных
docker compose exec backend cp /app/data/database.db /app/data/database.db.backup

# Или с хоста
docker cp personal-page-backend:/app/data/database.db ./backup/database.db
```

---

## 🐛 Решение проблем

### Проблема: Не могу клонировать репозиторий

**Решение:**
1. Проверьте SSH ключ: `ssh -T git@github.com`
2. Проверьте права на ключ: `chmod 600 ~/.ssh/deploy_key`
3. Проверьте SSH config: `cat ~/.ssh/config`

### Проблема: Порт уже занят

**Решение:**
```bash
# Проверьте, что использует порт
netstat -tulpn | grep 8888
netstat -tulpn | grep 9000

# Остановите конфликтующий сервис или измените порты в docker-compose.yml
```

### Проблема: Frontend не может подключиться к Backend

**Решение:**
1. Проверьте, что `VITE_API_URL` **НЕ УСТАНОВЛЕН** в `.env` (используется относительный путь `/api`)
2. Проверьте, что nginx настроен и проксирует `/api` на backend
3. Проверьте, что backend запущен: `docker compose ps`
4. Проверьте логи backend: `docker compose logs backend`
5. Проверьте конфигурацию nginx: `cat /etc/nginx/sites-available/personal-page`

### Проблема: База данных не создается

**Решение:**
```bash
# Проверьте права на директорию data
ls -la data/

# Создайте директорию вручную
mkdir -p data
chmod 777 data

# Запустите миграции вручную
docker-compose exec backend npm run migrate
```

---

## ✅ Чек-лист деплоя

- [ ] SSH ключ добавлен в GitHub/GitLab
- [ ] Docker и Docker Compose установлены
- [ ] Репозиторий клонирован
- [ ] `.env` файл создан и настроен
- [ ] `VITE_API_URL` **НЕ УСТАНОВЛЕН** (используется относительный путь `/api`)
- [ ] `NODE_ENV=production`
- [ ] `TELEGRAM_BOT_TOKEN` заполнен
- [ ] Контейнеры запущены (`docker-compose ps`)
- [ ] Логи не показывают ошибок
- [ ] Frontend доступен по адресу
- [ ] Backend API отвечает
- [ ] Файрвол настроен (если нужно)
- [ ] Автозапуск настроен (опционально)

---

## 📞 Контакты и поддержка

При возникновении проблем проверьте:
1. Логи контейнеров: `docker-compose logs -f`
2. Статус контейнеров: `docker-compose ps`
3. Переменные окружения: `cat .env`
4. Сеть: `docker network ls`


# Environment Variables - Project Root
# Copy this file to .env and fill in the values
# Usage: cp env.example.txt .env

# Database (SQLite)
# Path to SQLite database file (relative to project root or absolute path)
# Default: ./data/database.db (will be created automatically)
DATABASE_PATH=./data/database.db

# Telegram Bot (optional - worker will not start without token)
# 
# 1. Get TELEGRAM_BOT_TOKEN:
#    - Open Telegram and search for @BotFather
#    - Send /newbot command
#    - Follow instructions to create a bot
#    - Copy the token you receive
#
# 2. Set your user ID:
#    - Get your User ID from @userinfobot or by sending message to your bot
#    - Set TELEGRAM_USER_ID in .env file
#    - Bot will use this User ID to get Chat ID (for private chats, they are the same)
#    - If not set, bot will register it automatically when you send first message
#
# 3. Set admin ID (optional):
#    - Get your User ID from @userinfobot
#    - Set TELEGRAM_ADMIN_ID to your User ID
#    - Admin can check registered Telegram Chat ID by sending any message to the bot
#    - Non-admin messages will be ignored
#
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_USER_ID=
TELEGRAM_ADMIN_ID=

# Backend API Configuration
API_PORT=9000
NODE_ENV=development

# Frontend API Configuration
# URL of the backend API (for local dev: http://localhost:9000/api)
VITE_API_URL=http://localhost:9000/api

# Gemini API (optional - if needed for frontend features)
GEMINI_API_KEY=your_gemini_api_key_here


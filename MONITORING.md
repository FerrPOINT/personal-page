# 🔒 Мониторинг и Автоматическое Восстановление

## 🎯 Что это решает

Предотвращает ситуации, когда приложение внезапно перестает работать:
- ✅ Автоматический перезапуск при падении контейнеров
- ✅ Health checks для проверки работоспособности
- ✅ Автозапуск при перезагрузке сервера
- ✅ Мониторинг каждые 5 минут с автоматическим восстановлением
- ✅ Логирование всех событий

## 📋 Установка на Production сервере

### 1. Подключитесь к серверу

```bash
ssh -i ~/.ssh/id_rsa root@azhukov-dev
cd /opt/personal-page
```

### 2. Обновите код

```bash
git pull origin main
```

### 3. Установите мониторинг

```bash
bash setup-monitoring.sh
```

Это установит:
- Systemd сервис для автозапуска приложения
- Systemd timer для мониторинга каждые 5 минут
- Автоматическое восстановление при падении

## 🔍 Что делает мониторинг

### Автоматические проверки (каждые 5 минут):

1. **Проверка статуса контейнеров** - все ли запущены
2. **Health check backend** - доступен ли `/health` endpoint
3. **Проверка frontend** - отвечает ли nginx в контейнере

### При обнаружении проблемы:

1. Автоматически перезапускает контейнеры
2. Если перезапуск не помог - пересобирает образы
3. Логирует все действия в `/var/log/personal-page-monitor.log`
4. Записывает события в systemd journal

## 📊 Полезные команды

### Проверка статуса

```bash
# Статус приложения
systemctl status personal-page.service

# Статус мониторинга
systemctl status personal-page-monitor.timer

# Последние логи мониторинга
tail -f /var/log/personal-page-monitor.log

# Логи через journalctl
journalctl -u personal-page.service -f
journalctl -u personal-page-monitor.service -f
```

### Ручное управление

```bash
# Перезапуск приложения
systemctl restart personal-page.service

# Остановка мониторинга (не рекомендуется)
systemctl stop personal-page-monitor.timer

# Запуск мониторинга
systemctl start personal-page-monitor.timer

# Ручной запуск проверки
/opt/personal-page/monitor.sh
```

## 🔧 Настройка

### Изменить интервал мониторинга

Отредактируйте `/etc/systemd/system/personal-page-monitor.timer`:

```ini
[Timer]
OnUnitActiveSec=5min  # Измените на нужный интервал (например, 10min)
```

Затем:
```bash
systemctl daemon-reload
systemctl restart personal-page-monitor.timer
```

### Добавить уведомления

Отредактируйте `monitor.sh` и добавьте отправку уведомлений в функцию `restart_services()`:

```bash
# Пример: отправка через Telegram
curl -s "https://api.telegram.org/bot$TELEGRAM_BOT_TOKEN/sendMessage" \
  -d "chat_id=$TELEGRAM_USER_ID" \
  -d "text=⚠️ Приложение упало, но восстановлено автоматически"
```

## 🚨 Что делать если проблема не решается автоматически

1. **Проверьте логи:**
   ```bash
   journalctl -u personal-page.service -n 100
   docker compose logs --tail=100
   ```

2. **Проверьте статус контейнеров:**
   ```bash
   docker compose ps
   docker compose logs backend
   docker compose logs frontend
   ```

3. **Проверьте ресурсы сервера:**
   ```bash
   df -h  # Диск
   free -h  # Память
   top  # CPU
   ```

4. **Ручной перезапуск:**
   ```bash
   cd /opt/personal-page
   docker compose down
   docker compose up -d --build
   ```

## 📝 Логи

Все события логируются в:
- `/var/log/personal-page-monitor.log` - логи мониторинга
- `journalctl -u personal-page.service` - логи systemd сервиса
- `docker compose logs` - логи контейнеров

## ✅ Проверка работы

После установки проверьте:

```bash
# 1. Сервис запущен
systemctl is-active personal-page.service
# Должно быть: active

# 2. Мониторинг активен
systemctl is-active personal-page-monitor.timer
# Должно быть: active

# 3. Контейнеры работают
docker compose ps
# Оба контейнера должны быть "Up"

# 4. Health check работает
curl http://localhost:9000/health
# Должен вернуть JSON с status: ok
```

## 🔄 Обновление

При обновлении кода через Jenkins деплой автоматически перезапустит systemd сервис.

Для ручного обновления:

```bash
cd /opt/personal-page
git pull origin main
systemctl restart personal-page.service
```


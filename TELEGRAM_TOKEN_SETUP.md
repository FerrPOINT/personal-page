# 🔐 Настройка нового Telegram токена

## Новый токен
**TELEGRAM_BOT_TOKEN**: `8243118630:AAF-_fBdgaHgclVab_7vS_X9k4oEZFpjkcM`

## ✅ Что уже сделано:
1. Токен НЕ в Git (env.prod и env.local содержат placeholder)
2. Jenkinsfile настроен на использование credentials
3. deploy.sh обрабатывает токены из переменных окружения

## 📋 Что нужно сделать:

### Вариант 1: Через Jenkins UI (РЕКОМЕНДУЕТСЯ)

1. Откройте: http://192.168.1.49:32768/
2. Войдите: `ferrpoint` / токен `1191a9f019fc3989d7a5ff30d456fb9cf3`
3. Перейдите: **Manage Jenkins** → **Credentials** → **System** → **Global credentials (unrestricted)**
4. Проверьте, есть ли уже `telegram-bot-token`:
   - Если есть → нажмите на него → **Update** → измените Secret на новый токен
   - Если нет → **Add Credentials** → заполните:
     - **Kind**: `Secret text`
     - **Secret**: `8243118630:AAF-_fBdgaHgclVab_7vS_X9k4oEZFpjkcM`
     - **ID**: `telegram-bot-token` (ВАЖНО: именно такой ID!)
     - **Description**: `Telegram Bot Token for personal-page notifications`
5. Проверьте `telegram-user-id`:
   - Если есть → обновите если нужно
   - Если нет → создайте:
     - **Kind**: `Secret text`
     - **Secret**: `754334329`
     - **ID**: `telegram-user-id`
     - **Description**: `Telegram User ID for personal-page notifications`

### Вариант 2: Через API (уже попробовано)

API вернул 302 (редирект), что обычно означает успех. Проверьте в UI.

## ✅ Проверка работы:

1. Запустите build в Jenkins:
   ```
   http://192.168.1.49:32768/job/personal-page-deploy/build
   ```

2. В логах должно быть:
   ```
   🔐 TELEGRAM_BOT_TOKEN обновлен из Jenkins Credentials
   🔐 TELEGRAM_USER_ID обновлен из Jenkins Credentials
   ```

3. НЕ должно быть:
   ```
   ⚠️  telegram-bot-token credentials не найдены
   ```

## 🔒 Безопасность:

- ✅ Токен НЕ в Git (env.prod и env.local содержат `your_bot_token_here`)
- ✅ Токен передается через Jenkins Credentials
- ✅ Токен не логируется в Jenkins (благодаря withCredentials)
- ✅ Токен передается через переменные окружения SSH команды

## 📝 Конфигурация:

- **Jenkinsfile**: использует `credentialsId: 'telegram-bot-token'`
- **deploy.sh**: обрабатывает `$TELEGRAM_BOT_TOKEN` из переменных окружения
- **.gitignore**: `env.prod` и `env.local` игнорируются


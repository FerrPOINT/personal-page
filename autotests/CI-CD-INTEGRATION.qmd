---
title: "Интеграция тестов в CI/CD"
date: "2026-01-19"
---

# Интеграция тестов в CI/CD

**Дата создания**: 2026-01-19  
**Статус**: ✅ Интегрировано в Jenkins и Makefile

## 📋 Обзор

Автоматизированные тесты интегрированы в процесс сборки и деплоя:

1. **Jenkins CI/CD**: Тесты запускаются автоматически перед деплоем
2. **Makefile**: Команды для локального запуска тестов
3. **package.json**: Скрипты для запуска тестов

## 🚀 Jenkins CI/CD

### Стадия "Test" в Jenkinsfile

В `Jenkinsfile` добавлена стадия `Test`, которая выполняется перед деплоем:

```groovy
stage('Test') {
    steps {
        script {
            echo "🧪 Запуск тестов..."
            // Установка зависимостей
            sh 'npm install'
            // Установка браузеров Playwright
            sh 'npx playwright install --with-deps chromium'
            // Запуск критичных тестов
            sh '''
                npx playwright test \
                    autotests/automated/ui/group-001-ui-elements/TC-005-language-switcher.spec.ts \
                    autotests/automated/forms/group-002-forms/TC-001-contact-form.spec.ts \
                    --project=chromium \
                    --reporter=list
            '''
        }
    }
}
```

### Порядок выполнения в Jenkins

1. **Checkout** - Клонирование репозитория
2. **Validate** - Проверка наличия Jenkinsfile
3. **Test** - Запуск критичных тестов ⬅️ **НОВОЕ**
4. **Deploy** - Деплой на сервер

### Настройка для полного запуска тестов

Если нужно запускать все тесты (не только критичные), измените команду в Jenkinsfile:

```groovy
sh 'npx playwright test autotests/automated --project=chromium --reporter=list'
```

### Обработка ошибок

По умолчанию тесты не блокируют деплой при ошибках. Чтобы блокировать деплой при провале тестов:

```groovy
sh '''
    npx playwright test ... || exit 1
'''
```

## 🔧 Makefile

### Команды для тестирования

```bash
# Запуск всех тестов
make test

# Запуск только критичных тестов (быстрая проверка)
make test-critical

# Полная проверка (lint + type-check + build + test-critical)
make check
```

### Интеграция в процесс разработки

Команда `make check` теперь включает:
1. `lint` - Проверка линтером
2. `type-check` - Проверка типов TypeScript
3. `build-frontend` - Сборка frontend
4. `build-backend` - Сборка backend
5. `test-critical` - Запуск критичных тестов ⬅️ **НОВОЕ**

## 📦 package.json

### Скрипты для тестирования

```json
{
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  }
}
```

### Использование

```bash
# Все тесты
npm test

# С UI режимом
npm run test:ui

# С визуализацией браузера
npm run test:headed

# В режиме отладки
npm run test:debug
```

## ⚙️ Конфигурация Playwright для CI/CD

### Переменные окружения

- `CI=true` - Автоматически определяется Playwright
- `FRONTEND_URL` - URL frontend сервера (по умолчанию `http://localhost:8888`)

### Настройки для CI

В `autotests/config/playwright.config.ts`:

- `retries: process.env.CI ? 2 : 0` - Повторы в CI
- `workers: process.env.CI ? 1 : undefined` - Один воркер в CI
- `forbidOnly: !!process.env.CI` - Запрет `.only()` в CI

## 🎯 Критичные тесты

По умолчанию в CI/CD запускаются только критичные тесты:

1. **TC-005**: LanguageSwitcher - Переключение языка
2. **TC-001**: Contact Form - Отправка сообщения

Эти тесты проверяют основную функциональность сайта и должны проходить перед каждым деплоем.

## 📊 Отчеты

### В Jenkins

Результаты тестов отображаются в консоли Jenkins. Для более детальных отчетов можно добавить:

```groovy
sh '''
    npx playwright test ... --reporter=html
    # Отчет будет в autotests/results/reports/
'''
```

### Локально

```bash
# HTML отчет
npx playwright test --reporter=html

# JSON отчет
npx playwright test --reporter=json --output=results.json
```

## 🔄 Workflow

### Локальная разработка

1. Разработка функционала
2. Запуск `make check` перед коммитом
3. Коммит и push

### CI/CD Pipeline

1. **Checkout** - Клонирование репозитория
2. **Validate** - Проверка структуры
3. **Test** - Запуск критичных тестов
4. **Deploy** - Деплой на сервер (если тесты прошли)

## ⚠️ Важные замечания

1. **Сервер должен быть запущен**: В CI окружении предполагается, что frontend сервер уже запущен на `http://localhost:8888`
2. **Браузеры Playwright**: В Jenkins нужно установить браузеры один раз: `npx playwright install --with-deps chromium`
3. **Время выполнения**: Критичные тесты выполняются ~30-60 секунд
4. **Ресурсы**: Playwright требует достаточно памяти и CPU

## 🐛 Troubleshooting

### "Test timeout exceeded"

- Увеличьте таймаут в `playwright.config.ts`
- Проверьте, что сервер доступен на `http://localhost:8888`

### "Browser not found"

- Установите браузеры: `npx playwright install --with-deps chromium`

### "Server not responding"

- Убедитесь, что frontend сервер запущен перед тестами
- Проверьте `FRONTEND_URL` переменную окружения

## 📝 Следующие шаги

- [ ] Добавить отчеты тестов в Jenkins (HTML reporter)
- [ ] Настроить уведомления при провале тестов
- [ ] Добавить тесты производительности
- [ ] Интегрировать coverage отчеты

---

**Тесты успешно интегрированы в процесс сборки и деплоя!** ✅


---
description: "Правила и методологии профессионального тестирования, создания тест-кейсов и использования инструментов"
globs: ["**/test-cases.qmd", "**/test*.qmd", "**/test*.md", "**/*.test.ts", "**/*.test.tsx", "**/__tests__/**"]
alwaysApply: false
---

# Правила профессионального тестирования

## 🚨 КРИТИЧЕСКИ ВАЖНО: ОБЯЗАТЕЛЬНОЕ ВИЗУАЛЬНОЕ ТЕСТИРОВАНИЕ ПОСЛЕ ЛЮБЫХ ПРАВОК

**⚠️ ОБЯЗАТЕЛЬНО**: После **ЛЮБЫХ** изменений в frontend компонентах, стилях, UI элементах **ОБЯЗАТЕЛЬНО** выполнять визуальное тестирование через MCP Playwright!

### 📋 Обязательная последовательность проверки после правок:

1. **Сборка и деплой:**
   ```bash
   npm run build          # Сборка frontend
   docker-compose build frontend  # Пересборка контейнера
   docker-compose up -d frontend  # Перезапуск
   ```

2. **Визуальная проверка через MCP Playwright (ОБЯЗАТЕЛЬНО!):**
   ```typescript
   // 1. Навигация на страницу с измененным компонентом
   browser_navigate({ url: 'http://frontend:80/#contact' })
   
   // 2. Ожидание загрузки
   browser_wait_for({ time: 5 })
   
   // 3. Прокрутка к нужному элементу (если нужно)
   browser_press_key({ key: 'End' })  // или browser_click на элемент навигации
   browser_wait_for({ time: 2 })
   
   // 4. Снимок страницы для анализа структуры
   browser_snapshot()
   
   // 5. Скриншот для визуальной проверки (ОБЯЗАТЕЛЬНО!)
   browser_take_screenshot({ filename: 'component-visual-check.png', fullPage: false })
   
   // 6. Проверка консоли на ошибки
   browser_console_messages()
   
   // 7. Проверка сетевых запросов
   browser_network_requests()
   ```

3. **Анализ результатов:**
   - ✅ Компонент отображается корректно в snapshot
   - ✅ Визуально выглядит правильно на скриншоте
   - ✅ Нет ошибок в консоли
   - ✅ Нет ошибок в сетевых запросах
   - ✅ Соответствует стилю остальных элементов

4. **Если найдены проблемы:**
   - Исправить код
   - Повторить сборку и проверку
   - **НЕ коммитить**, пока визуально не будет правильно!

### ❌ НЕ ДОПУСКАЕТСЯ:
- ❌ Коммитить изменения без визуальной проверки
- ❌ Полагаться только на unit тесты для UI компонентов
- ❌ Пропускать проверку через MCP Playwright
- ❌ Игнорировать визуальные несоответствия

### ✅ КРИТЕРИИ УСПЕШНОЙ ПРОВЕРКИ:
- ✅ Компонент виден в snapshot
- ✅ Компонент выглядит правильно на скриншоте
- ✅ Соответствует стилю остальных элементов
- ✅ Нет ошибок в консоли и сети
- ✅ Все интерактивные элементы работают

### 📋 БЫСТРЫЙ ЧЕК-ЛИСТ ПОСЛЕ ПРАВОК В FRONTEND:

**После ЛЮБЫХ изменений в frontend компонентах:**

- [ ] **ОБЯЗАТЕЛЬНО**: Запущены автотесты (`npm test`) - **ВСЕ ТЕСТЫ ДОЛЖНЫ ПРОЙТИ!**
- [ ] Линтер проверен (`read_lints`) - нет ошибок
- [ ] Сборка прошла успешно (`npm run build`)
- [ ] Контейнер пересобран и перезапущен (`docker-compose build frontend && docker-compose up -d frontend`)
- [ ] Навигация на страницу выполнена (`browser_navigate`)
- [ ] Прокрутка к нужному элементу выполнена (если нужно)
- [ ] Снимок страницы получен (`browser_snapshot`)
- [ ] Скриншот сделан (`browser_take_screenshot`) - **ОБЯЗАТЕЛЬНО!**
- [ ] Консоль проверена на ошибки (`browser_console_messages`)
- [ ] Сетевые запросы проверены (`browser_network_requests`)
- [ ] Визуально компонент выглядит правильно
- [ ] Компонент соответствует стилю остальных элементов
- [ ] Все интерактивные элементы работают

**Только после выполнения ВСЕХ пунктов можно коммитить!**

---

## 🎯 Обзор методологий тестирования

Профессиональное тестирование — это систематический процесс проверки качества программного обеспечения, который включает планирование, проектирование, выполнение и анализ тестов. Этот документ описывает современные методологии и практики тестирования, актуальные на 2026 год.

### 📋 Ключевые принципы тестирования

1. **Раннее тестирование**: Тестирование начинается на этапе требований, а не после разработки
2. **Непрерывное тестирование**: Тесты выполняются на всех этапах разработки
3. **Визуальное тестирование**: **ОБЯЗАТЕЛЬНО** после любых правок в frontend через MCP Playwright
4. **Пирамида тестирования**: Больше юнит-тестов, меньше интеграционных, минимум E2E
5. **Автоматизация**: Повторяющиеся тесты должны быть автоматизированы
6. **Документирование**: Все тест-кейсы должны быть задокументированы
7. **Изоляция**: Тесты должны быть независимыми и изолированными
8. **Детерминированность**: Тесты должны давать одинаковый результат при повторных запусках

---

## 🏗️ Пирамида тестирования

```{mermaid}
graph TD
    A[E2E Tests<br/>Меньше, медленнее, дороже] --> B[Integration Tests<br/>Среднее количество]
    B --> C[Unit Tests<br/>Больше, быстрее, дешевле]
    
    style A fill:#ff6b6b
    style B fill:#ffd93d
    style C fill:#6bcf7f
```

### Распределение тестов

- **Unit Tests (70%)**: Быстрые, изолированные тесты отдельных компонентов
- **Integration Tests (20%)**: Тесты взаимодействия между компонентами
- **E2E Tests (10%)**: Тесты полных пользовательских сценариев

---

## 📝 Типы тест-кейсов

### 1. Позитивные тест-кейсы

**Описание**: Проверка корректной работы функционала при валидных данных.

**Структура:**
- **ID**: TC-001, TC-002, ...
- **Название**: Краткое описание сценария
- **Предусловия**: Что должно быть выполнено перед тестом
- **Шаги**: Пошаговые действия
- **Ожидаемый результат**: Что должно произойти
- **Приоритет**: High/Medium/Low

**Пример:**
```markdown
### TC-001: Успешное сохранение настроек Telegram с включенными уведомлениями
**Предусловия**: 
- Пользователь авторизован
- Магазин существует
- Страница интеграции загружена

**Шаги**:
1. Ввести валидный Bot Token: `123456:ABC-DEF...`
2. Ввести валидный Chat ID: `754334329`
3. Включить чекбокс "Включить уведомления"
4. Нажать кнопку "Сохранить"

**Ожидаемый результат**:
- Форма отправлена успешно
- Показано сообщение об успехе
- Значение `enabled: true` сохранено в БД
- Статус интеграции обновлен на "Включено"
- Status code: 200
```

### 2. Негативные тест-кейсы

**Описание**: Проверка обработки ошибок и невалидных данных.

**Пример:**
```markdown
### TC-101: Сохранение с пустым Bot Token
**Предусловия**: Страница интеграции открыта

**Шаги**:
1. Оставить поле Bot Token пустым
2. Ввести Chat ID: `754334329`
3. Нажать "Сохранить"

**Ожидаемый результат**:
- Форма не отправлена
- Показана ошибка валидации "Bot Token обязателен"
- Значение не сохранено в БД
- Status code: 400 (если отправка произошла)
```

### 3. Граничные тест-кейсы

**Описание**: Проверка поведения на границах допустимых значений.

**Пример:**
```markdown
### TC-201: Ввод максимально длинного Bot Token
**Шаги**:
1. Ввести Bot Token длиной 200 символов
2. Сохранить

**Ожидаемый результат**:
- Токен принят (если лимит >= 200)
- Или показана ошибка валидации (если лимит < 200)
```

### 4. Тест-кейсы безопасности

**Описание**: Проверка защиты от уязвимостей.

**Пример:**
```markdown
### TC-301: XSS в поле Chat ID
**Шаги**:
1. Ввести в Chat ID: `<script>alert('xss')</script>`
2. Сохранить

**Ожидаемый результат**:
- Скрипт экранирован и не выполняется
- Значение сохранено как строка без выполнения кода
```

### 5. Тест-кейсы производительности

**Описание**: Проверка времени отклика и нагрузки.

**Пример:**
```markdown
### TC-401: Время сохранения настроек
**Условия**: Нормальная нагрузка

**Ожидаемый результат**:
- Время сохранения < 500ms
- Нет блокировки UI во время сохранения
```

---

## 🔍 Профессиональное тестирование: Пример с чекбоксом

### Контекст проблемы

При тестировании чекбокса "Включить уведомления" в Telegram интеграции была обнаружена проблема:
- Чекбокс выключался в UI
- Значение сохранялось в БД
- После обновления страницы чекбокс снова был включен

### Процесс профессионального тестирования

#### Шаг 1: Воспроизведение проблемы

```typescript
// MCP Playwright тест
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot() // Проверяем начальное состояние: чекбокс включен

// Выключаем чекбокс
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_wait_for({ time: 1 })
browser_console_messages() // Проверяем: "Switch changed: false"

// Сохраняем
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })
browser_console_messages() // Проверяем на ошибки 500

// Обновляем страницу
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot() // ПРОБЛЕМА: чекбокс снова включен!
```

#### Шаг 2: Анализ проблемы

**Что проверяли:**

1. **Frontend состояние:**
   - Проверка через `browser_console_messages()`: `Switch changed: false` ✅
   - UI состояние после клика: чекбокс выключен ✅

2. **API запрос:**
   - Проверка через `browser_network_requests()`: POST запрос отправлен ✅
   - Проверка payload: `enabled: false` отправлен ✅
   - Проверка ответа: 500 Internal Server Error ❌

3. **Backend логи:**
   ```bash
   docker-compose logs backend | grep -E "ERROR|enabled"
   # Результат: EntityIdentityCollisionException
   ```

4. **База данных:**
   - Проверка через backend логи: значение не сохранилось ❌

#### Шаг 3: Изоляция проблемы

**Гипотезы:**
1. ❌ Frontend не отправляет правильное значение → опровергнуто (проверено через network requests)
2. ❌ Backend не принимает значение → опровергнуто (проверено через логи)
3. ✅ Backend не сохраняет значение → подтверждено (EntityIdentityCollisionException)
4. ❌ Frontend не обновляет состояние после сохранения → опровергнуто (проблема в сохранении)

**Корневая причина:**
- Doctrine ORM `EntityIdentityCollisionException` при попытке сохранить сущность
- Использование `registerManaged()` в репозитории создавало конфликт
- Метод `merge()` был удален в Doctrine 3.0

#### Шаг 4: Верификация исправления

**После исправления:**

```typescript
// Повторный тест
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })
browser_console_messages() // Проверяем: нет ошибок 500 ✅
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_snapshot() // Проверяем: чекбокс выключен ✅
```

### Уроки профессионального тестирования

1. **Систематический подход**: Проверяем каждый слой (UI → API → Backend → DB)
2. **Использование инструментов**: MCP Playwright для UI, логи для backend, network requests для API
3. **Изоляция проблемы**: Постепенно сужаем область поиска
4. **Верификация исправления**: Повторяем тест после исправления
5. **Документирование**: Фиксируем проблему, решение и уроки

---

## 🛠️ Инструменты для тестирования

### 1. MCP Playwright (Браузерное тестирование)

**Назначение**: Интерактивное тестирование через реальный браузер в Docker.

**⚠️ КРИТИЧЕСКИ ВАЖНО: Настройка Docker сети для MCP Playwright**

**Проблема**: MCP Playwright работает в Docker контейнере и не может подключиться к приложению, если не настроена сеть Docker.

**Решение**: Выполни ВСЕ шаги ниже для правильной настройки:

#### Шаг 1: Настройка `.cursor/mcp.json` (ОБЯЗАТЕЛЬНО)

Добавь Docker сеть в конфигурацию MCP Playwright, чтобы он мог подключиться к приложению:

```json
{
  "mcpServers": {
    "playwright": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--label",
        "com.docker.compose.project=mcp",
        "--add-host=host.docker.internal:host-gateway",
        "--network=personal-page_app-network",  // ← ОБЯЗАТЕЛЬНО добавить эту строку
        "mcp/playwright"
      ]
    }
  }
}
```

**Важно**: 
- Замени `personal-page_app-network` на имя сети из твоего `docker-compose.yml` (обычно `[project-name]_app-network`)
- Узнай имя сети: `docker network ls | grep app-network`
- Или проверь в `docker-compose.yml` в секции `networks`

#### Шаг 2: Настройка `frontend/vite.config.ts` (ОБЯЗАТЕЛЬНО)

Добавь `allowedHosts` в секцию `server` (НЕ `preview`!) для dev-сервера:

```typescript
export default defineConfig({
  server: {
    port: 8888,
    host: '0.0.0.0',
    allowedHosts: [
      'localhost',
      '127.0.0.1',
      'host.docker.internal',      // Для доступа из MCP Playwright контейнера
      'personal-page-frontend',    // Имя контейнера frontend
      'frontend',                  // Короткое имя сервиса
    ],
    // ... остальная конфигурация
  },
})
```

**Важно**: 
- Используй секцию `server`, а не `preview` (для dev-сервера)
- Добавь имена контейнеров из `docker-compose.yml` (например, `personal-page-frontend`)

#### Шаг 3: Правильные URL для доступа

**Внутри Docker сети** (рекомендуется):
```typescript
// Используй имя сервиса из docker-compose.yml и внутренний порт
browser_navigate({ url: 'http://frontend:80/#contact' })
// или
browser_navigate({ url: 'http://personal-page-frontend:80/#contact' })
```

**Через host.docker.internal** (альтернатива):
```typescript
// Используй host.docker.internal и порт хоста
browser_navigate({ url: 'http://host.docker.internal:8888/#contact' })
```

**Важно**: 
- Внутри Docker сети используй порт контейнера (обычно `80` для nginx)
- Через `host.docker.internal` используй порт хоста (например, `8888`)

#### Шаг 4: Пересборка и перезапуск

После изменения конфигурации:

```bash
# 1. Пересобери frontend контейнер
docker-compose build frontend --no-cache

# 2. Перезапусти frontend
docker-compose up -d frontend

# 3. Перезапусти MCP сервер (перезапусти Cursor IDE или MCP сервер)
# Обычно достаточно перезапустить Cursor IDE
```

#### Шаг 5: Проверка настройки

Проверь, что всё работает:

```bash
# 1. Проверь, что frontend контейнер запущен
docker ps | grep frontend

# 2. Проверь, что сеть существует
docker network ls | grep app-network

# 3. Проверь, что frontend в сети
docker network inspect personal-page_app-network | grep frontend

# 4. Проверь доступность из контейнера (опционально)
docker run --rm --network=personal-page_app-network curlimages/curl:latest curl -I http://frontend:80
```

#### Типичные проблемы и решения

**Проблема**: `ERR_CONNECTION_REFUSED` при попытке подключения

**Решение**:
1. Проверь, что frontend контейнер запущен: `docker ps | grep frontend`
2. Проверь, что MCP Playwright в той же сети: проверь `.cursor/mcp.json`
3. Проверь `allowedHosts` в `vite.config.ts`
4. Пересобери и перезапусти контейнеры

**Проблема**: `ERR_NAME_NOT_RESOLVED` при использовании имени контейнера

**Решение**:
1. Проверь имя сети в `.cursor/mcp.json` (должно совпадать с `docker-compose.yml`)
2. Проверь, что контейнеры в одной сети: `docker network inspect [network-name]`
3. Используй `host.docker.internal:8888` как альтернативу

**Проблема**: Vite dev server блокирует запросы

**Решение**:
1. Убедись, что `allowedHosts` добавлен в секцию `server` (не `preview`)
2. Добавь все возможные имена: `host.docker.internal`, имя контейнера, `frontend`
3. Пересобери frontend контейнер: `docker-compose build frontend --no-cache`

**Доступные команды:**

| Команда | Описание | Пример использования |
|---------|----------|---------------------|
| `browser_navigate` | Переход на страницу | `browser_navigate({ url: 'http://frontend:80/#contact' })` |
| `browser_snapshot` | Получение снимка страницы | `browser_snapshot()` |
| `browser_click` | Клик по элементу | `browser_click({ element: 'Кнопка', ref: 'e12' })` |
| `browser_type` | Ввод текста | `browser_type({ element: 'Поле', ref: 'e78', text: 'Текст' })` |
| `browser_wait_for` | Ожидание | `browser_wait_for({ time: 3 })` |
| `browser_console_messages` | Чтение консоли | `browser_console_messages()` |
| `browser_network_requests` | Мониторинг сети | `browser_network_requests()` |
| `browser_take_screenshot` | Скриншот | `browser_take_screenshot({ filename: 'test.png' })` |

**Примечание**: В примерах используется `http://frontend:80` (внутри Docker сети). Альтернатива: `http://host.docker.internal:8888` (через host.docker.internal).

**Когда использовать:**
- E2E тестирование пользовательских сценариев
- Проверка UI/UX
- Отладка проблем в production-like окружении
- Визуальная проверка страниц

**Пример профессионального теста:**

```typescript
// TC-001: Проверка сохранения настроек с выключенным чекбоксом
// 1. Навигация (используй правильный URL для твоего проекта)
browser_navigate({ url: 'http://frontend:80/#contact' })
// Альтернатива: browser_navigate({ url: 'http://host.docker.internal:8888/#contact' })
browser_wait_for({ time: 3 })

// 2. Проверка начального состояния
browser_snapshot() // Должен быть включен
browser_console_messages() // Проверка на ошибки загрузки

// 3. Выключение чекбокса
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_wait_for({ time: 1 })
browser_console_messages() // Должно быть: "Switch changed: false"

// 4. Сохранение
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })

// 5. Проверка результата сохранения
browser_console_messages() // НЕ должно быть ошибок 500
browser_network_requests() // POST запрос должен вернуть 200

// 6. Обновление страницы и проверка персистентности
browser_navigate({ url: 'http://frontend:80/#contact' })
browser_wait_for({ time: 3 })
browser_snapshot() // Чекбокс ДОЛЖЕН быть выключен
```

**Важно**: Замени `http://frontend:80` на правильный URL для твоего проекта (имя сервиса из `docker-compose.yml` и порт контейнера).

### 2. Vitest + Testing Library (Unit/Integration тесты)

**Назначение**: Быстрые изолированные тесты компонентов и логики.

**Структура теста:**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render, screen, waitFor } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { Component } from './Component'

describe('Component', () => {
  beforeEach(() => {
    vi.clearAllMocks()
  })

  it('should render correctly', () => {
    render(<Component />)
    expect(screen.getByRole('button')).toBeInTheDocument()
  })

  it('should handle user interaction', async () => {
    const user = userEvent.setup()
    const handleClick = vi.fn()
    
    render(<Component onClick={handleClick} />)
    
    await user.click(screen.getByRole('button'))
    
    expect(handleClick).toHaveBeenCalledTimes(1)
  })
})
```

**Когда использовать:**
- Тестирование отдельных компонентов
- Тестирование хуков и утилит
- Быстрая обратная связь при разработке
- Покрытие бизнес-логики

### 3. Vitest Browser Mode (Браузерные тесты)

**Назначение**: Автоматизированные браузерные тесты с Playwright.

**Пример:**

```typescript
import { test, expect } from 'vitest'
import { page } from '@vitest/browser/context'

test('should load the app', async () => {
  await page.goto('http://localhost:4173')
  await expect(page.locator('body')).toBeVisible()
})
```

**Когда использовать:**
- Автоматизированные E2E тесты в CI/CD
- Регрессионное тестирование
- Проверка критичных пользовательских сценариев

---

## 📋 Структура тест-кейса

### ОБЯЗАТЕЛЬНАЯ структура файла `test-cases.qmd` в папке задачи:

```markdown
# Тест-кейсы: [Название задачи]

**Обновлено**: YYYY-MM-DD HH:MM
**Покрытие**: [X% позитивных, Y% негативных, Z% граничных]

---

## 🎯 Общая информация

### Область тестирования
- [Список компонентов/модулей для тестирования]

### Критерии приемки
- [ ] Критерий 1
- [ ] Критерий 2

### Зависимости
- [Список зависимостей от других задач/компонентов]

---

## 🧪 ПОЗИТИВНЫЕ СЦЕНАРИИ

### TC-001: [Название сценария]
**Приоритет**: High/Medium/Low
**Предусловия**: 
- [Условие 1]
- [Условие 2]

**Шаги**:
1. [Действие 1]
2. [Действие 2]
3. [Действие 3]

**Ожидаемый результат**:
- [Результат 1]
- [Результат 2]
- Status code: [200/201/etc]

**Автоматизация**:
- [ ] Unit тест: `src/components/__tests__/Component.test.tsx`
- [ ] Integration тест: `src/features/__tests__/Feature.test.tsx`
- [ ] E2E тест: `src/test/browser.test.ts`
- [ ] MCP тест: [Описание MCP сценария]

---

## ❌ НЕГАТИВНЫЕ СЦЕНАРИИ

### TC-101: [Название сценария]
**Приоритет**: High/Medium/Low
**Предусловия**: [Условия]

**Шаги**:
1. [Действие с невалидными данными]

**Ожидаемый результат**:
- [Ошибка валидации/обработка]
- Status code: [400/401/403/etc]

---

## 🔒 ТЕСТЫ БЕЗОПАСНОСТИ

### TC-201: [Название]
**Шаги**: [Действие с потенциальной уязвимостью]
**Ожидаемый результат**: [Защита от уязвимости]

---

## ⚡ ТЕСТЫ ПРОИЗВОДИТЕЛЬНОСТИ

### TC-301: [Название]
**Условия**: [Условия нагрузки]
**Ожидаемый результат**: 
- Время ответа < [X]ms
- [Другие метрики]

---

## 📊 КРИТЕРИИ ПРОХОЖДЕНИЯ ТЕСТОВ

| Тип тестов | Проходящих | Требуется |
|------------|------------|-----------|
| Позитивные | X% | 100% |
| Негативные | Y% | 100% |
| Безопасность | Z% | 100% |
| Производительность | W% | ≥95% |

**Общий статус прохождения**: ❌ Не проведено / 🔄 В процессе / ✅ Пройдено
```

---

## 🛠️ Инструменты для создания тест-кейсов

### 1. MCP Playwright для исследования функционала

**Использование**: Интерактивное исследование функционала перед созданием тест-кейсов.

**Процесс:**

```typescript
// Шаг 1: Исследование функционала
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot() // Изучаем структуру страницы

// Шаг 2: Изучение элементов
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_snapshot() // Изучаем форму тестового заказа

// Шаг 3: Тестирование взаимодействий
browser_type({ element: 'Поле Номер заказа', ref: 'e177', text: 'TEST-001' })
browser_type({ element: 'Поле Сумма', ref: 'e179', text: '1000' })
browser_click({ element: 'Кнопка Создать заказ', ref: 'e182' })
browser_wait_for({ time: 3 })

// Шаг 4: Анализ результата
browser_console_messages() // Проверяем на ошибки
browser_network_requests() // Изучаем API запросы
browser_snapshot() // Фиксируем результат
```

**Результат**: Понимание функционала для создания точных тест-кейсов.

### 2. Codebase Search для изучения реализации

**Использование**: Понимание реализации для создания тест-кейсов.

```typescript
// Поиск реализации компонента
codebase_search({ 
  query: "How does the Telegram integration form handle enabled checkbox state?"
})

// Поиск API endpoints
codebase_search({ 
  query: "What API endpoints are used for saving Telegram integration settings?"
})

// Поиск валидации
codebase_search({ 
  query: "What validation rules are applied to Bot Token and Chat ID fields?"
})
```

**Результат**: Понимание реализации для создания тест-кейсов с правильными ожиданиями.

### 3. Sequential Thinking для анализа сценариев

**Использование**: Систематический анализ сложных сценариев.

```typescript
// Анализ сценария сохранения настроек
sequential_thinking({
  thought: "Анализирую сценарий сохранения настроек Telegram интеграции...",
  // ... анализ всех шагов и проверок
})
```

**Результат**: Структурированный анализ для создания полных тест-кейсов.

### 4. Task Orchestrator для организации тестирования

**Использование**: Связывание тест-кейсов с задачами.

```typescript
// Создание задачи с тест-кейсами
create_task({
  title: "Тестирование сохранения настроек Telegram",
  summary: "Создание тест-кейсов для проверки сохранения настроек",
  tags: "testing,telegram,integration"
})

// Добавление секции с тест-кейсами
add_section({
  entityType: "TASK",
  entityId: taskId,
  title: "Тест-кейсы",
  content: "# Тест-кейсы\n\n## TC-001: ...",
  ordinal: 2
})
```

**Результат**: Организованные тест-кейсы, связанные с задачами.

### 5. Markdown Rules MCP для документирования

**Использование**: Документирование тест-кейсов в структурированном виде.

```typescript
// Индексирование тест-кейсов
reindex_docs() // После создания/обновления test-cases.qmd

// Поиск похожих тест-кейсов
get_relevant_docs({
  attachedFiles: ["info/sprint1/001_task-name/test-cases.qmd"]
})
```

**Результат**: Структурированная документация тест-кейсов.

---

## 🔄 Процесс создания тест-кейсов

### ОБЯЗАТЕЛЬНЫЕ шаги при создании тест-кейсов:

#### 1. Анализ требований
- [ ] **ОБЯЗАТЕЛЬНО** прочитано ТЗ задачи (`requirements.qmd`)
- [ ] **ОБЯЗАТЕЛЬНО** изучена архитектура через `codebase_search`
- [ ] **ОБЯЗАТЕЛЬНО** определены границы тестирования
- [ ] **ОБЯЗАТЕЛЬНО** выявлены зависимости
- [ ] **ОБЯЗАТЕЛЬНО** использован `get_relevant_docs` для изучения похожих задач
- [ ] **ОБЯЗАТЕЛЬНО** использован MCP Playwright для исследования функционала (если применимо)

#### 2. Идентификация сценариев
- [ ] Определены позитивные сценарии (happy path)
- [ ] Определены негативные сценарии (error handling)
- [ ] Определены тесты для обработки ошибок API (400, 404, 500, сетевые ошибки)
- [ ] Определены тесты для автозаполнения формы из БД
- [ ] Определены тесты для сообщений об успехе/ошибке
- [ ] Определены тесты для очистки формы после успешного создания
- [ ] Определены граничные случаи (boundary values)
- [ ] Определены сценарии безопасности
- [ ] Определены сценарии производительности (если применимо)

#### 3. Декомпозиция на тест-кейсы
- [ ] Каждый сценарий разбит на конкретные тест-кейсы
- [ ] Каждый тест-кейс имеет уникальный ID (TC-001, TC-002, ...)
- [ ] Определены приоритеты тест-кейсов
- [ ] Определены предусловия для каждого тест-кейса

#### 4. Документирование
- [ ] **ОБЯЗАТЕЛЬНО** создан файл `test-cases.qmd` в папке задачи
- [ ] **ОБЯЗАТЕЛЬНО** все тест-кейсы задокументированы
- [ ] **ОБЯЗАТЕЛЬНО** определены критерии приемки
- [ ] **ОБЯЗАТЕЛЬНО** тест-кейсы синхронизированы с `task-orchestrator`
- [ ] **ОБЯЗАТЕЛЬНО** тест-кейсы задокументированы через markdown-rules-mcp
- [ ] **ОБЯЗАТЕЛЬНО** выполнена индексация через `reindex_docs`
- [ ] **ОБЯЗАТЕЛЬНО** создана структура в `info/autotests/sprint*/task-name/`
- [ ] **ОБЯЗАТЕЛЬНО** все автотесты задокументированы в соответствующих файлах
- [ ] **ОБЯЗАТЕЛЬНО** результаты тестирования зафиксированы в `results/TestResults.qmd`
- [ ] **ОБЯЗАТЕЛЬНО** найденные баги задокументированы в `results/BugsFound.qmd`

#### 5. Автоматизация
- [ ] Определены тест-кейсы для автоматизации
- [ ] Созданы unit тесты для критичной логики
- [ ] Созданы integration тесты для взаимодействий
- [ ] Созданы E2E тесты для критичных сценариев
- [ ] Созданы MCP тесты для визуальной проверки

#### 6. Валидация
- [ ] Тест-кейсы проверены на полноту покрытия
- [ ] Тест-кейсы проверены на реалистичность
- [ ] Тест-кейсы согласованы с командой (для критичных)
- [ ] Тест-кейсы обновлены в `test-cases.qmd`

---

## 🎯 Методологии тестирования

### 1. Equivalence Partitioning (Разделение на классы эквивалентности)

**Принцип**: Разделение входных данных на классы эквивалентности, где все значения в классе обрабатываются одинаково.

**Пример для Chat ID:**
- **Валидный класс**: Числа от 1 до 999999999999
- **Невалидный класс 1**: Отрицательные числа
- **Невалидный класс 2**: Не числовые значения
- **Невалидный класс 3**: Пустая строка

**Тест-кейсы:**
- TC-001: Валидный Chat ID (например, `754334329`)
- TC-101: Отрицательное число (`-123`)
- TC-102: Текст (`"abc"`)
- TC-103: Пустая строка (`""`)

### 2. Boundary Value Analysis (Анализ граничных значений)

**Принцип**: Тестирование на границах допустимых значений.

**Пример для длины Bot Token:**
- Минимум: 1 символ
- Максимум: 200 символов
- Граничные значения: 0, 1, 2, 199, 200, 201

**Тест-кейсы:**
- TC-201: Минимальная длина (1 символ)
- TC-202: Максимальная длина (200 символов)
- TC-203: Превышение максимума (201 символ)
- TC-204: Пустая строка (0 символов)

### 3. Decision Table Testing (Таблицы решений)

**Принцип**: Систематическое тестирование комбинаций условий.

**Пример для формы сохранения:**

| Bot Token | Chat ID | Enabled | Ожидаемый результат |
|-----------|---------|---------|---------------------|
| Валидный  | Валидный | true  | ✅ Сохранено |
| Валидный  | Валидный | false | ✅ Сохранено |
| Пустой    | Валидный | true  | ❌ Ошибка валидации |
| Валидный  | Пустой   | true  | ❌ Ошибка валидации |
| Пустой    | Пустой   | true  | ❌ Ошибка валидации |

### 4. State Transition Testing (Тестирование переходов состояний)

**Принцип**: Тестирование переходов между состояниями системы.

**Пример для чекбокса "Включить уведомления":**

```{mermaid}
stateDiagram-v2
    [*] --> Disabled: Начальное состояние
    Disabled --> Enabled: Клик (enabled=true)
    Enabled --> Disabled: Клик (enabled=false)
    Disabled --> Saving: Сохранение
    Enabled --> Saving: Сохранение
    Saving --> Disabled: Успех (enabled=false)
    Saving --> Enabled: Успех (enabled=true)
    Saving --> Error: Ошибка
    Error --> Disabled: Повтор
    Error --> Enabled: Повтор
```

**Тест-кейсы:**
- TC-301: Переход Disabled → Enabled → Сохранение → Enabled
- TC-302: Переход Enabled → Disabled → Сохранение → Disabled
- TC-303: Переход Disabled → Сохранение → Error → Disabled
- TC-304: Переход Enabled → Сохранение → Error → Enabled

### 5. Error Guessing (Предположение об ошибках)

**Принцип**: Использование опыта для предположения о возможных ошибках.

**Типичные ошибки:**
- Неправильная обработка `null`/`undefined`
- Проблемы с типами данных (строка вместо числа)
- Проблемы с персистентностью (сохранение/загрузка)
- Проблемы с состоянием (race conditions)

**Пример:**
- TC-401: Сохранение с `enabled: null` (должно быть `false`)
- TC-402: Сохранение с `enabled: "true"` (строка вместо boolean)
- TC-403: Сохранение и немедленная загрузка (проверка персистентности)

---

## 📝 Практический пример: Создание тест-кейсов для новой функции

### Контекст

Нужно создать тест-кейсы для функции "Создание тестового заказа" в Telegram интеграции.

### Шаг 1: Исследование через MCP Playwright

```typescript
// 1. Навигация и изучение страницы
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Результат: Видим вкладки "Интеграция" и "Тестирование"

// 2. Переключение на вкладку "Тестирование"
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_wait_for({ time: 2 })
browser_snapshot()
// Результат: Видим форму с полями "Номер заказа", "Сумма", "Имя клиента"

// 3. Изучение элементов формы
browser_snapshot()
// Результат: 
// - Поле "Номер заказа" (textbox, ref: e177)
// - Поле "Сумма" (spinbutton, ref: e179)
// - Поле "Имя клиента" (textbox, ref: e181)
// - Кнопка "Создать заказ" (button, ref: e182)

// 4. Тестирование заполнения формы
browser_type({ element: 'Поле Номер заказа', ref: 'e177', text: 'TEST-001' })
browser_type({ element: 'Поле Сумма', ref: 'e179', text: '1500' })
browser_type({ element: 'Поле Имя клиента', ref: 'e181', text: 'Тестовый клиент' })
browser_snapshot()
// Результат: Все поля заполнены

// 5. Создание заказа
browser_click({ element: 'Кнопка Создать заказ', ref: 'e182' })
browser_wait_for({ time: 3 })

// 6. Анализ результата
browser_console_messages()
// Результат: 
// - API Request: POST /api/shops/.../orders
// - API Response: 200 OK
// - Switch changed: false (если уведомления выключены)

browser_network_requests()
// Результат:
// POST http://posiflora-frontend:4173/api/shops/.../orders
// Request: { orderNumber: "TEST-001", total: 1500, customerName: "Тестовый клиент" }
// Response: 200 OK

browser_snapshot()
// Результат: Показано сообщение "Заказ создан! Статус отправки: sent"
```

**Выводы из исследования:**
- Форма имеет 3 обязательных поля
- Кнопка активна только при заполнении всех полей
- После создания показывается сообщение с результатом
- Отправляется API запрос на создание заказа

### Шаг 2: Изучение реализации через Codebase Search

```typescript
// Поиск компонента формы
codebase_search({ 
  query: "How is the test order creation form implemented in TelegramIntegrationPage?"
})

// Поиск API endpoint
codebase_search({ 
  query: "What API endpoint handles test order creation?"
})

// Поиск валидации
codebase_search({ 
  query: "What validation is applied to order number, total, and customer name fields?"
})
```

**Результаты:**
- Компонент: `TelegramIntegrationPage.tsx`
- Хук: `useCreateOrder(shopId)`
- API: `POST /api/shops/{shopId}/orders`
- Валидация: Все поля обязательны, сумма должна быть числом

### Шаг 3: Анализ через Sequential Thinking

```typescript
sequential_thinking({
  thought: "Анализирую сценарий создания тестового заказа...",
  // Анализ всех возможных сценариев:
  // 1. Успешное создание с валидными данными
  // 2. Создание с пустыми полями
  // 3. Создание с невалидной суммой
  // 4. Создание с очень длинным именем клиента
  // 5. Создание при выключенных уведомлениях
  // ...
})
```

**Результат**: Список всех возможных сценариев для тестирования.

### Шаг 4: Создание тест-кейсов

На основе исследования создаем структурированные тест-кейсы:

```markdown
# Тест-кейсы: Создание тестового заказа

## 🧪 ПОЗИТИВНЫЕ СЦЕНАРИИ

### TC-001: Успешное создание тестового заказа
**Приоритет**: High
**Предусловия**: 
- Пользователь авторизован
- Магазин существует
- Telegram интеграция настроена

**Шаги**:
1. Открыть вкладку "Тестирование"
2. Ввести Номер заказа: `TEST-001`
3. Ввести Сумму: `1500`
4. Ввести Имя клиента: `Тестовый клиент`
5. Нажать "Создать заказ"

**Ожидаемый результат**:
- Заказ создан успешно
- Показано сообщение "Заказ создан! Статус отправки: sent"
- API запрос POST /api/shops/.../orders вернул 200
- Если уведомления включены - отправлено уведомление в Telegram
- Заказ появился в истории отправок

**Автоматизация**:
- MCP тест: [Описание MCP сценария выше]
- E2E тест: `src/test/browser.test.ts`

### TC-002: Создание заказа с минимальными данными
**Приоритет**: Medium
**Шаги**:
1. Ввести Номер заказа: `A`
2. Ввести Сумму: `1`
3. Ввести Имя клиента: `A`
4. Нажать "Создать заказ"

**Ожидаемый результат**: Заказ создан успешно

## ❌ НЕГАТИВНЫЕ СЦЕНАРИИ

### TC-101: Создание заказа с пустым номером
**Шаги**:
1. Оставить поле "Номер заказа" пустым
2. Заполнить остальные поля
3. Нажать "Создать заказ"

**Ожидаемый результат**:
- Кнопка "Создать заказ" неактивна (disabled)
- Или показана ошибка валидации
- Заказ не создан

### TC-102: Создание заказа с отрицательной суммой
**Шаги**:
1. Ввести Сумму: `-100`
2. Заполнить остальные поля
3. Нажать "Создать заказ"

**Ожидаемый результат**:
- Показана ошибка валидации "Сумма должна быть положительной"
- Заказ не создан
```

### Шаг 5: Документирование через Markdown Rules MCP

```typescript
// Создание файла test-cases.qmd
write("info/sprint1/001_task-name/test-cases.qmd", testCasesContent)

// Индексирование для поиска
reindex_docs()
```

### Шаг 6: Синхронизация с Task Orchestrator

```typescript
// Добавление секции с тест-кейсами в задачу
add_section({
  entityType: "TASK",
  entityId: taskId,
  title: "Тест-кейсы",
  content: "# Тест-кейсы\n\n[Содержимое из test-cases.qmd]",
  ordinal: 3
})
```

---

## 🔬 Профессиональное тестирование чекбокса: Детальный разбор

### Контекст

Чекбокс "Включить уведомления" в форме Telegram интеграции должен:
1. Переключаться при клике
2. Сохранять состояние в БД
3. Восстанавливать состояние после перезагрузки страницы

### Проблема

После выключения чекбокса и сохранения, при обновлении страницы чекбокс снова был включен.

### Процесс профессионального тестирования

#### Этап 1: Воспроизведение через MCP Playwright

```typescript
// Шаг 1: Навигация и проверка начального состояния
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Результат: Чекбокс включен (checked=true) ✅

// Шаг 2: Выключение чекбокса
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_wait_for({ time: 1 })
browser_console_messages()
// Результат: "Switch changed: false" ✅
browser_snapshot()
// Результат: Чекбокс выключен (checked=false) ✅

// Шаг 3: Сохранение
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })

// Шаг 4: Проверка результата сохранения
browser_console_messages()
// Результат: ERROR 500 Internal Server Error ❌
browser_network_requests()
// Результат: POST /api/shops/.../telegram/connect → 500 ❌

// Шаг 5: Проверка персистентности
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Результат: Чекбокс снова включен ❌ ПРОБЛЕМА!
```

#### Этап 2: Анализ через логи backend

```bash
# Проверка логов backend
docker-compose logs backend | grep -E "ERROR|enabled|EntityIdentityCollisionException"

# Результат:
# [TelegramController::connect] Error: EntityIdentityCollisionException
# [TelegramIntegrationRepository::save] ERROR: Call to undefined method merge()
```

**Вывод**: Проблема в backend - ошибка при сохранении в БД.

#### Этап 3: Проверка через Unit тесты

```typescript
// frontend/src/pages/__tests__/TelegramIntegrationPage.test.tsx
it('saves with enabled=false when switch is off', async () => {
  const mockMutate = vi.fn().mockResolvedValue({})
  vi.mocked(hooks.useConnectTelegram).mockReturnValue({
    mutateAsync: mockMutate,
    // ...
  } as any)

  const user = userEvent.setup()
  render(<TelegramIntegrationPage />, { wrapper: createWrapper() })

  const switchElement = screen.getByLabelText(/включить уведомления/i)
  
  // Проверяем начальное состояние
  expect(switchElement).not.toBeChecked() // Должно быть false
  
  // Выключаем (если было включено)
  if (switchElement.checked) {
    await user.click(switchElement)
  }
  
  // Заполняем форму
  await user.type(screen.getByLabelText(/bot token/i), 'test-token')
  await user.type(screen.getByLabelText(/chat id/i), 'test-chat-id')
  
  // Сохраняем
  await user.click(screen.getByRole('button', { name: /сохранить/i }))

  // Проверяем, что отправлено правильное значение
  await waitFor(() => {
    expect(mockMutate).toHaveBeenCalledWith({
      botToken: 'test-token',
      chatId: 'test-chat-id',
      enabled: false, // Должно быть false
    })
  })
})
```

**Результат**: Unit тест проходит ✅ - Frontend отправляет правильное значение.

#### Этап 4: Проверка API запроса

```typescript
// MCP проверка network requests
browser_network_requests()
// Результат:
// POST http://posiflora-frontend:4173/api/shops/.../telegram/connect
// Request payload: { botToken: "...", chatId: "...", enabled: false }
// Response: 500 Internal Server Error
```

**Вывод**: Frontend отправляет правильные данные, но backend возвращает ошибку.

#### Этап 5: Изоляция проблемы в backend

**Проверка Controller:**
```php
// backend/src/Controller/TelegramController.php
// Логи показывают: enabled=false получен правильно ✅
```

**Проверка Service:**
```php
// backend/src/Service/TelegramIntegrationService.php
// Логи показывают: enabled=false обработан правильно ✅
```

**Проверка Repository:**
```php
// backend/src/Repository/TelegramIntegrationRepository.php
// Ошибка: EntityIdentityCollisionException при save()
// Причина: registerManaged() создает конфликт
```

**Корневая причина:**
- Использование `registerManaged()` в `findByShopId()` регистрирует сущность в UnitOfWork
- При последующем `persist()` возникает конфликт идентичности
- Метод `merge()` удален в Doctrine 3.0

#### Этап 6: Верификация исправления

**После исправления (убрали `registerManaged`, используем стандартный `find()`):**

```typescript
// Повторный MCP тест
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })
browser_console_messages()
// Результат: Нет ошибок 500 ✅
browser_network_requests()
// Результат: POST → 200 OK ✅
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_snapshot()
// Результат: Чекбокс выключен ✅ ПРОБЛЕМА РЕШЕНА!
```

### Уроки профессионального тестирования

1. **Многоуровневое тестирование**: UI → API → Backend → DB
2. **Использование инструментов**: MCP для UI, логи для backend, network для API
3. **Систематический подход**: Постепенное сужение области поиска
4. **Документирование процесса**: Фиксация каждого шага и результата
5. **Верификация исправления**: Обязательный повторный тест

---

## 📊 Метрики качества тестирования

### ОБЯЗАТЕЛЬНЫЕ метрики для отслеживания:

1. **Покрытие кода (Code Coverage)**
   - Unit тесты: ≥80%
   - Integration тесты: ≥60%
   - E2E тесты: Критичные сценарии 100%

2. **Покрытие требований (Requirements Coverage)**
   - Позитивные сценарии: 100%
   - Негативные сценарии: 100%
   - Граничные случаи: ≥90%

3. **Покрытие тест-кейсов (Test Case Coverage)**
   - Автоматизировано: ≥70%
   - Ручное тестирование: ≤30%

4. **Стабильность тестов (Test Stability)**
   - Flaky тесты: <5%
   - Успешность прогона: ≥95%

### ОБЯЗАТЕЛЬНО документировать:

- Покрытие кода по модулям
- Количество тест-кейсов по типам
- Результаты прогона тестов
- Найденные баги и их статус
- Время выполнения тестов

---

## 🎯 Best Practices

### 1. Структура тест-кейса

**✅ Правильно:**
```markdown
### TC-001: Успешное сохранение с выключенным чекбоксом
**Приоритет**: High
**Предусловия**: 
- Пользователь авторизован
- Магазин существует

**Шаги**:
1. Открыть страницу интеграции
2. Выключить чекбокс "Включить уведомления"
3. Заполнить Bot Token и Chat ID
4. Нажать "Сохранить"

**Ожидаемый результат**:
- Форма отправлена (Status: 200)
- Значение enabled=false сохранено в БД
- После обновления страницы чекбокс выключен
```

**❌ Неправильно:**
```markdown
### Тест 1: Сохранение
Шаги: Сохранить форму
Ожидаемый результат: Должно работать
```

### 2. Использование инструментов

**✅ Правильно:**
```typescript
// Многоуровневая проверка
browser_click({ element: 'Кнопка Сохранить' })
browser_wait_for({ time: 3 })
browser_console_messages() // Проверка ошибок в консоли
browser_network_requests() // Проверка API запросов
browser_snapshot() // Проверка UI состояния
```

**❌ Неправильно:**
```typescript
// Только визуальная проверка
browser_click({ element: 'Кнопка Сохранить' })
// Нет проверки результата
```

### 3. Документирование проблем

**✅ Правильно:**
```markdown
## Найденные проблемы

### BUG-001: Чекбокс не сохраняет состояние
**Дата обнаружения**: 2026-01-21
**Приоритет**: High
**Шаги воспроизведения**:
1. Выключить чекбокс
2. Сохранить
3. Обновить страницу

**Ожидаемое поведение**: Чекбокс остается выключенным
**Фактическое поведение**: Чекбокс снова включен
**Корневая причина**: EntityIdentityCollisionException в backend
**Статус**: Исправлено
**Решение**: Убрали registerManaged(), используем стандартный find()
```

**❌ Неправильно:**
```markdown
## Проблемы
- Чекбокс не работает
```

---

## 🔄 Интеграция с существующим workflow

### MCP-инструменты

#### Task Orchestrator
- **ОБЯЗАТЕЛЬНО** создавай тест-кейсы при создании задачи
- **ОБЯЗАТЕЛЬНО** обновляй статус тестирования в оркестраторе
- **ОБЯЗАТЕЛЬНО** связывай найденные баги с задачами

#### Markdown Rules MCP
- **ОБЯЗАТЕЛЬНО** документируй тест-кейсы через markdown-rules-mcp
- **ОБЯЗАТЕЛЬНО** используй `get_relevant_docs` для изучения похожих тест-кейсов
- **ОБЯЗАТЕЛЬНО** индексируй файлы тест-кейсов через `reindex_docs`

#### Sequential Thinking
- **ОБЯЗАТЕЛЬНО** используй sequential-thinking для анализа сложных сценариев
- **ОБЯЗАТЕЛЬНО** применяй для декомпозиции больших тест-кейсов
- **ОБЯЗАТЕЛЬНО** используй для анализа найденных багов

### Структура проекта

**ОБЯЗАТЕЛЬНО** файл тест-кейсов в папке задачи:
```
info/
└── sprint1/
    └── 001_task-name/
        ├── requirements.qmd
        ├── test-cases.qmd      # ОБЯЗАТЕЛЬНО
        ├── current-stage.qmd
        └── changelog.qmd
```

---

## ⚠️ Типичные ошибки при тестировании

### ❌ НЕ ДЕЛАЙ ТАК:

1. **Тестирование только happy path**
   - ❌ Тестируешь только успешные сценарии
   - ✅ Тестируй также ошибки и граничные случаи

2. **Отсутствие проверки персистентности**
   - ❌ Проверяешь только UI состояние
   - ✅ Проверяй также сохранение в БД и восстановление после перезагрузки

3. **Игнорирование ошибок в консоли**
   - ❌ Не проверяешь консоль браузера
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй `browser_console_messages()` на ошибки

4. **Отсутствие проверки API запросов**
   - ❌ Не проверяешь network requests
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй `browser_network_requests()` на 404/500

5. **Тестирование без документирования**
   - ❌ Тестируешь без фиксации результатов
   - ✅ **ОБЯЗАТЕЛЬНО** документируй все тест-кейсы в `test-cases.qmd`

6. **Тестирование без анализа**
   - ❌ Находишь проблему, но не анализируешь причину
   - ✅ **ОБЯЗАТЕЛЬНО** анализируй корневую причину через логи, код, БД

7. **Игнорирование обработки ошибок**
   - ❌ Тестируешь только успешные сценарии
   - ✅ **ОБЯЗАТЕЛЬНО** тестируй обработку ошибок API (400, 404, 500, сетевые ошибки)
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй отображение сообщений об ошибке на UI

8. **Игнорирование автозаполнения**
   - ❌ Не проверяешь автозаполнение формы из БД
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй автозаполнение формы при загрузке страницы
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй синхронизацию данных между формой и статусом

9. **Игнорирование сообщений об успехе/ошибке**
   - ❌ Не проверяешь отображение сообщений
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй сообщения об успехе (зеленые, с иконкой CheckCircle2)
   - ✅ **ОБЯЗАТЕЛЬНО** проверяй сообщения об ошибке (красные, с иконкой XCircle)

10. **Не документируешь найденные баги**
    - ❌ Находишь баг, но не документируешь его
    - ✅ **ОБЯЗАТЕЛЬНО** документируй все найденные баги в `results/bugs-found.qmd`
    - ✅ **ОБЯЗАТЕЛЬНО** указывай шаги воспроизведения, ожидаемое/фактическое поведение, критичность

---

## 📚 Шаблоны тест-кейсов

### Шаблон 1: Простой позитивный тест-кейс

```markdown
### TC-001: [Название]
**Приоритет**: High
**Предусловия**: [Список]
**Шаги**: [Пошаговые действия]
**Ожидаемый результат**: [Конкретные результаты]
**Автоматизация**: [Ссылка на тест]
```

### Шаблон 2: Комплексный тест-кейс с проверками

```markdown
### TC-002: [Название]
**Приоритет**: High
**Предусловия**: [Список]

**Шаги**:
1. [Действие 1]
2. [Действие 2]

**Проверки**:
- [ ] UI состояние: [Ожидание]
- [ ] API запрос: [Ожидание]
- [ ] Backend логи: [Ожидание]
- [ ] БД состояние: [Ожидание]
- [ ] Персистентность: [Ожидание]

**Автоматизация**:
- Unit тест: [Файл]
- Integration тест: [Файл]
- E2E тест: [Файл]
- MCP тест: [Описание]
```

### Шаблон 3: Тест-кейс для бага

```markdown
### TC-BUG-001: [Название бага]
**Приоритет**: High
**Статус**: Открыт/Исправлен/Закрыт
**Дата обнаружения**: YYYY-MM-DD

**Шаги воспроизведения**:
1. [Действие]
2. [Действие]

**Ожидаемое поведение**: [Что должно быть]
**Фактическое поведение**: [Что происходит]
**Корневая причина**: [Анализ проблемы]
**Решение**: [Как исправлено]
**Верификация**: [Как проверили исправление]
```

---

## ✅ Чек-лист создания тест-кейсов

### Перед созданием
- [ ] Прочитано ТЗ задачи
- [ ] Изучена архитектура
- [ ] Определены границы тестирования
- [ ] Выявлены зависимости

### Во время создания
- [ ] Определены позитивные сценарии
- [ ] Определены негативные сценарии
- [ ] Определены граничные случаи
- [ ] Определены сценарии безопасности
- [ ] Каждый тест-кейс имеет уникальный ID
- [ ] Определены приоритеты

### После создания
- [ ] Создан файл `test-cases.qmd`
- [ ] Все тест-кейсы задокументированы
- [ ] Определены критерии приемки
- [ ] Тест-кейсы синхронизированы с `task-orchestrator`
- [ ] Созданы автоматизированные тесты
- [ ] Тест-кейсы обновлены в `test-cases.qmd`

---

## 🎯 Главные принципы тестирования

✅ **Тестирование начинается с требований**  
✅ **Всегда документируй тест-кейсы**  
✅ **Используй многоуровневое тестирование**  
✅ **Проверяй все слои: UI → API → Backend → DB**  
✅ **Используй инструменты для анализа**  
✅ **Анализируй корневую причину проблем**  
✅ **Верифицируй исправления**  
✅ **Синхронизируй с MCP-инструментами**

❌ **НЕ тестируй только happy path**  
❌ **НЕ игнорируй ошибки в консоли**  
❌ **НЕ тестируй без документирования**  
❌ **НЕ тестируй без анализа проблем**  
❌ **НЕ забывай проверять персистентность**  
❌ **НЕ игнорируй network requests**

---

## 🧩 Пошаговое тестирование элементов и интеграций

### 🎯 Принцип: Сначала элементы, потом интеграции

**КРИТИЧЕСКИ ВАЖНО**: Всегда тестируй элементы по отдельности перед тестированием их взаимодействия.

**Порядок тестирования:**
1. **Элементы по отдельности** (кнопки, поля, чекбоксы, вкладки)
2. **Простые интеграции** (форма с несколькими полями)
3. **Сложные интеграции** (взаимодействие между формами, вкладками, компонентами)
4. **E2E сценарии** (полные пользовательские потоки)

---

## 📋 Тестирование отдельных элементов

### 1. Кнопка (Button)

#### Чек-лист тестирования кнопки

**Базовые проверки:**
- [ ] Кнопка отображается на странице
- [ ] Кнопка имеет правильный текст/иконку
- [ ] Кнопка имеет правильный тип (button/submit/reset)
- [ ] Кнопка имеет правильный стиль (primary/secondary/outline)

**Состояния:**
- [ ] Кнопка активна (enabled) по умолчанию
- [ ] Кнопка становится неактивной (disabled) при загрузке
- [ ] Кнопка показывает индикатор загрузки при `isPending`
- [ ] Кнопка возвращается в активное состояние после завершения

**Взаимодействие:**
- [ ] Клик по кнопке вызывает обработчик
- [ ] Клик по кнопке не вызывает множественных запросов (debounce)
- [ ] Кнопка правильно обрабатывает disabled состояние

**Доступность:**
- [ ] Кнопка доступна через клавиатуру (Tab, Enter)
- [ ] Кнопка имеет правильный ARIA атрибут (role="button")
- [ ] Кнопка имеет правильный aria-label (если нет текста)

#### Пример тестирования через MCP Playwright

```typescript
// TC-BUTTON-001: Тестирование кнопки "Сохранить"

// Шаг 1: Навигация и проверка отображения
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Кнопка "Сохранить" видна на странице

// Шаг 2: Проверка начального состояния
browser_snapshot()
// Проверка: Кнопка активна (не disabled)

// Шаг 3: Проверка состояния при загрузке
// Заполняем форму и сохраняем
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 0.5 })
browser_snapshot()
// Проверка: Кнопка показывает "Сохранение..." с индикатором загрузки
// Проверка: Кнопка disabled во время загрузки

// Шаг 4: Проверка после завершения
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Кнопка вернулась в нормальное состояние
// Проверка: Индикатор загрузки исчез

// Шаг 5: Проверка консоли на ошибки
browser_console_messages()
// Проверка: Нет ошибок при клике

// Шаг 6: Проверка network запросов
browser_network_requests()
// Проверка: Отправлен POST запрос
// Проверка: Запрос вернул 200 (или ожидаемый статус)
```

#### Пример Unit теста

```typescript
it('should disable button during loading', async () => {
  const mockMutate = vi.fn()
  vi.mocked(hooks.useConnectTelegram).mockReturnValue({
    mutateAsync: mockMutate,
    isPending: true, // Симулируем загрузку
  } as any)

  render(<TelegramIntegrationPage />, { wrapper: createWrapper() })
  
  const saveButton = screen.getByRole('button', { name: /сохранить/i })
  
  // Проверяем, что кнопка disabled
  expect(saveButton).toBeDisabled()
  
  // Проверяем наличие индикатора загрузки
  expect(screen.getByText(/сохранение/i)).toBeInTheDocument()
  expect(screen.getByRole('status')).toBeInTheDocument() // Loader2
})
```

---

### 2. Поле ввода (Input)

#### Чек-лист тестирования поля ввода

**Базовые проверки:**
- [ ] Поле отображается на странице
- [ ] Поле имеет правильный label
- [ ] Поле имеет правильный тип (text/number/email/password)
- [ ] Поле имеет правильный placeholder (если есть)

**Состояния:**
- [ ] Поле пустое по умолчанию
- [ ] Поле заполняется при вводе текста
- [ ] Поле очищается при необходимости
- [ ] Поле показывает значение из props/state

**Валидация:**
- [ ] Поле показывает ошибку при невалидных данных
- [ ] Поле не показывает ошибку при валидных данных
- [ ] Поле правильно обрабатывает пустую строку
- [ ] Поле правильно обрабатывает граничные значения

**Взаимодействие:**
- [ ] Ввод текста обновляет значение
- [ ] Изменение значения вызывает onChange
- [ ] Поле правильно обрабатывает специальные символы
- [ ] Поле правильно обрабатывает длинный текст

**Доступность:**
- [ ] Поле связано с label через `htmlFor`/`id`
- [ ] Поле доступно через клавиатуру (Tab)
- [ ] Поле имеет правильный aria-label (если нужно)

#### Пример тестирования через MCP Playwright

```typescript
// TC-INPUT-001: Тестирование поля "Bot Token"

// Шаг 1: Проверка отображения
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Поле "Bot Token" видно на странице
// Проверка: Label "Bot Token" связан с полем

// Шаг 2: Проверка начального состояния
browser_snapshot()
// Проверка: Поле пустое (или заполнено из БД)

// Шаг 3: Ввод валидных данных
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: '123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11' })
browser_wait_for({ time: 0.5 })
browser_snapshot()
// Проверка: Текст введен в поле
// Проверка: Нет ошибок валидации

// Шаг 4: Проверка консоли
browser_console_messages()
// Проверка: Нет ошибок при вводе

// Шаг 5: Очистка поля
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: '' })
browser_wait_for({ time: 0.5 })
browser_snapshot()
// Проверка: Поле очищено

// Шаг 6: Ввод невалидных данных (если есть валидация)
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'invalid-token' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Показана ошибка валидации (если есть)
// Проверка: Форма не отправлена

// Шаг 7: Ввод очень длинного текста
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'A'.repeat(500) })
browser_wait_for({ time: 0.5 })
browser_snapshot()
// Проверка: Поле обрабатывает длинный текст
// Проверка: Нет визуальных проблем
```

---

### 3. Чекбокс/Switch

#### Чек-лист тестирования чекбокса

**Базовые проверки:**
- [ ] Чекбокс отображается на странице
- [ ] Чекбокс имеет правильный label
- [ ] Чекбокс имеет правильное начальное состояние (checked/unchecked)

**Состояния:**
- [ ] Чекбокс переключается при клике
- [ ] Чекбокс сохраняет состояние после переключения
- [ ] Чекбокс правильно инициализируется из props/state
- [ ] Чекбокс правильно обновляется при изменении props/state

**Персистентность:**
- [ ] Состояние сохраняется в БД при сохранении формы
- [ ] Состояние восстанавливается после перезагрузки страницы
- [ ] Состояние синхронизируется между компонентами

**Взаимодействие:**
- [ ] Клик по label переключает чекбокс
- [ ] Клик по самому чекбоксу переключает его
- [ ] Изменение вызывает onChange с правильным значением
- [ ] Чекбокс правильно обрабатывает disabled состояние

**Доступность:**
- [ ] Чекбокс доступен через клавиатуру (Tab, Space)
- [ ] Чекбокс имеет правильный role="checkbox"
- [ ] Чекбокс имеет правильный aria-checked атрибут

#### Пример тестирования через MCP Playwright

```typescript
// TC-SWITCH-001: Тестирование чекбокса "Включить уведомления"

// Шаг 1: Проверка отображения и начального состояния
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Чекбокс "Включить уведомления" виден
// Проверка: Начальное состояние (включен/выключен)

// Шаг 2: Проверка консоли на ошибки загрузки
browser_console_messages()
// Проверка: Нет ошибок при загрузке

// Шаг 3: Переключение чекбокса (выключение)
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_wait_for({ time: 0.5 })
browser_console_messages()
// Проверка: "Switch changed: false" в консоли
browser_snapshot()
// Проверка: Чекбокс выключен (checked=false)

// Шаг 4: Переключение обратно (включение)
browser_click({ element: 'Label Включить уведомления', ref: 'e58' })
browser_wait_for({ time: 0.5 })
browser_console_messages()
// Проверка: "Switch changed: true" в консоли
browser_snapshot()
// Проверка: Чекбокс включен (checked=true)

// Шаг 5: Сохранение с выключенным чекбоксом
browser_click({ element: 'Label Включить уведомления', ref: 'e58' }) // Выключаем
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })

// Шаг 6: Проверка сохранения
browser_console_messages()
// Проверка: Нет ошибок 500
browser_network_requests()
// Проверка: POST запрос отправлен с enabled: false
// Проверка: Ответ 200 OK

// Шаг 7: Проверка персистентности
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// КРИТИЧНО: Чекбокс должен быть выключен (состояние сохранилось)
browser_console_messages()
// Проверка: Нет ошибок при загрузке

// Шаг 8: Повторная проверка (сохранение с включенным)
browser_click({ element: 'Label Включить уведомления', ref: 'e58' }) // Включаем
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Чекбокс включен (состояние сохранилось)
```

---

### 4. Вкладки (Tabs)

#### Чек-лист тестирования вкладок

**Базовые проверки:**
- [ ] Вкладки отображаются на странице
- [ ] Вкладки имеют правильные названия
- [ ] Правильная вкладка активна по умолчанию
- [ ] Правильный контент отображается для активной вкладки

**Переключение:**
- [ ] Клик по вкладке переключает активную вкладку
- [ ] Контент обновляется при переключении
- [ ] Предыдущая вкладка скрывается
- [ ] Новая вкладка показывается

**Состояние:**
- [ ] Активная вкладка имеет правильный стиль
- [ ] Неактивные вкладки имеют правильный стиль
- [ ] Состояние формы сохраняется при переключении (если нужно)

**Доступность:**
- [ ] Вкладки доступны через клавиатуру (Tab, Arrow keys)
- [ ] Вкладки имеют правильный role="tablist"/"tab"

#### Пример тестирования через MCP Playwright

```typescript
// TC-TABS-001: Тестирование вкладок "Интеграция" и "Тестирование"

// Шаг 1: Проверка начального состояния
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Вкладка "Интеграция" активна
// Проверка: Форма подключения Telegram видна
// Проверка: Вкладка "Тестирование" неактивна

// Шаг 2: Переключение на вкладку "Тестирование"
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Вкладка "Тестирование" активна
// Проверка: Форма создания тестового заказа видна
// Проверка: Вкладка "Интеграция" неактивна
// Проверка: Форма подключения Telegram скрыта

// Шаг 3: Переключение обратно
browser_click({ element: 'Вкладка Интеграция', ref: 'e95' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Вкладка "Интеграция" снова активна
// Проверка: Форма подключения Telegram снова видна

// Шаг 4: Проверка сохранения состояния формы
// Заполняем форму на вкладке "Интеграция"
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })
// Переключаемся на другую вкладку
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_wait_for({ time: 1 })
// Переключаемся обратно
browser_click({ element: 'Вкладка Интеграция', ref: 'e95' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Данные в форме сохранились (или очистились, в зависимости от требований)
```

---

### 5. Индикатор загрузки (Loader)

#### Чек-лист тестирования индикатора загрузки

**Базовые проверки:**
- [ ] Индикатор показывается во время загрузки
- [ ] Индикатор скрывается после завершения загрузки
- [ ] Индикатор имеет правильную анимацию
- [ ] Индикатор имеет правильный текст (если есть)

**Состояния:**
- [ ] Индикатор показывается при `isLoading: true`
- [ ] Индикатор скрывается при `isLoading: false`
- [ ] Индикатор правильно обрабатывает ошибки загрузки

**Визуальные проверки:**
- [ ] Индикатор виден и читаем
- [ ] Индикатор не перекрывает важный контент
- [ ] Индикатор имеет правильный размер и позицию

#### Пример тестирования через MCP Playwright

```typescript
// TC-LOADER-001: Тестирование индикатора загрузки

// Шаг 1: Проверка индикатора при загрузке страницы
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Индикатор загрузки виден (если данные загружаются)

// Шаг 2: Ожидание завершения загрузки
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Индикатор загрузки скрыт
// Проверка: Контент страницы отображается

// Шаг 3: Проверка индикатора при сохранении формы
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 0.5 })
browser_snapshot()
// Проверка: Кнопка показывает "Сохранение..." с индикатором
// Проверка: Индикатор анимируется

// Шаг 4: Проверка после завершения
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Индикатор скрыт
// Проверка: Кнопка вернулась в нормальное состояние
```

---

### 6. Карточка (Card)

#### Чек-лист тестирования карточки

**Базовые проверки:**
- [ ] Карточка отображается на странице
- [ ] Карточка имеет правильный заголовок
- [ ] Карточка имеет правильное описание
- [ ] Карточка имеет правильный контент

**Визуальные проверки:**
- [ ] Карточка имеет правильные отступы
- [ ] Карточка имеет правильные границы
- [ ] Карточка правильно отображается на разных размерах экрана

#### Пример тестирования через MCP Playwright

```typescript
// TC-CARD-001: Тестирование карточки "Подключение Telegram"

// Шаг 1: Проверка отображения
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Карточка "Подключение Telegram" видна
// Проверка: Заголовок "Подключение Telegram" виден
// Проверка: Описание видно
// Проверка: Форма внутри карточки видна

// Шаг 2: Проверка контента
browser_snapshot()
// Проверка: Все поля формы видны
// Проверка: Кнопки видны
```

---

## 🔗 Тестирование интеграций

### Принцип: Тестируй взаимодействие элементов

После тестирования отдельных элементов, тестируй их взаимодействие.

### 1. Интеграция: Форма с несколькими полями

#### Чек-лист тестирования формы

**Базовые проверки:**
- [ ] Все поля формы отображаются
- [ ] Все поля правильно связаны с labels
- [ ] Кнопка отправки формы видна

**Валидация:**
- [ ] Форма не отправляется с пустыми обязательными полями
- [ ] Форма показывает ошибки валидации
- [ ] Форма отправляется при валидных данных

**Взаимодействие:**
- [ ] Заполнение полей обновляет состояние формы
- [ ] Изменение одного поля не влияет на другие
- [ ] Очистка полей работает корректно

**Отправка:**
- [ ] Форма отправляется при нажатии Enter (если настроено)
- [ ] Форма отправляется при клике на кнопку
- [ ] Форма показывает индикатор загрузки при отправке
- [ ] Форма показывает результат после отправки

#### Пример тестирования через MCP Playwright

```typescript
// TC-FORM-001: Тестирование формы подключения Telegram

// Шаг 1: Проверка начального состояния формы
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Все поля формы видны
// Проверка: Кнопка "Сохранить" видна
// Проверка: Кнопка "Проверить" видна

// Шаг 2: Проверка валидации (пустая форма)
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Показаны ошибки валидации (если есть)
// Проверка: Форма не отправлена
browser_network_requests()
// Проверка: Нет POST запросов

// Шаг 3: Заполнение формы частично
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Ошибка валидации для Chat ID (если поле обязательное)

// Шаг 4: Заполнение всех полей
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })

// Шаг 5: Проверка отправки
browser_console_messages()
// Проверка: Нет ошибок
browser_network_requests()
// Проверка: POST запрос отправлен
// Проверка: Запрос содержит все данные формы
// Проверка: Ответ 200 OK

// Шаг 6: Проверка результата
browser_wait_for({ time: 2 })
browser_snapshot()
// Проверка: Показано сообщение об успехе (если есть)
// Проверка: Форма обновлена данными из ответа
```

---

### 2. Интеграция: Взаимодействие между вкладками

#### Чек-лист тестирования взаимодействия вкладок

**Базовые проверки:**
- [ ] Переключение между вкладками работает
- [ ] Контент каждой вкладки независим
- [ ] Состояние форм сохраняется (или очищается) при переключении

**Взаимодействие:**
- [ ] Данные из одной вкладки не влияют на другую
- [ ] Обновление данных в одной вкладке не ломает другую
- [ ] Ошибки в одной вкладке не влияют на другую

#### Пример тестирования через MCP Playwright

```typescript
// TC-TABS-INTEGRATION-001: Тестирование взаимодействия вкладок

// Шаг 1: Заполнение формы на вкладке "Интеграция"
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'test-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '123456789' })

// Шаг 2: Переключение на вкладку "Тестирование"
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Форма тестового заказа видна
// Проверка: Поля формы пустые (или заполнены независимо)

// Шаг 3: Заполнение формы на вкладке "Тестирование"
browser_type({ element: 'Поле Номер заказа', ref: 'e177', text: 'TEST-001' })
browser_type({ element: 'Поле Сумма', ref: 'e179', text: '1500' })
browser_type({ element: 'Поле Имя клиента', ref: 'e181', text: 'Тестовый клиент' })

// Шаг 4: Переключение обратно
browser_click({ element: 'Вкладка Интеграция', ref: 'e95' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Данные в форме "Интеграция" сохранились (или очистились)
// Проверка: Нет ошибок

// Шаг 5: Переключение обратно на "Тестирование"
browser_click({ element: 'Вкладка Тестирование', ref: 'e97' })
browser_wait_for({ time: 1 })
browser_snapshot()
// Проверка: Данные в форме "Тестирование" сохранились (или очистились)
```

---

### 3. Интеграция: Взаимодействие формы и статуса

#### Чек-лист тестирования взаимодействия формы и статуса

**Базовые проверки:**
- [ ] Сохранение формы обновляет статус интеграции
- [ ] Статус интеграции правильно отображается
- [ ] Изменение статуса не ломает форму

**Синхронизация:**
- [ ] Форма инициализируется данными из статуса
- [ ] Обновление статуса обновляет форму
- [ ] Ошибки при сохранении не ломают статус

#### Пример тестирования через MCP Playwright

```typescript
// TC-FORM-STATUS-INTEGRATION-001: Тестирование взаимодействия формы и статуса

// Шаг 1: Проверка начального состояния
browser_navigate({ url: 'http://posiflora-frontend:4173/shops/{shopId}/growth/telegram' })
browser_wait_for({ time: 3 })
browser_snapshot()
// Проверка: Форма инициализирована данными из статуса (если есть)
// Проверка: Статус интеграции отображается

// Шаг 2: Сохранение формы
browser_type({ element: 'Поле Bot Token', ref: 'e45', text: 'new-token' })
browser_type({ element: 'Поле Chat ID', ref: 'e48', text: '987654321' })
browser_click({ element: 'Кнопка Сохранить', ref: 'e60' })
browser_wait_for({ time: 3 })

// Шаг 3: Проверка обновления статуса
browser_wait_for({ time: 2 })
browser_snapshot()
// Проверка: Статус интеграции обновлен
// Проверка: Показано "Подключено" (если было "Не подключено")
// Проверка: Данные в статусе соответствуют сохраненным

// Шаг 4: Проверка обновления формы
browser_snapshot()
// Проверка: Форма обновлена данными из ответа
// Проверка: Нет конфликтов между формой и статусом
```

---

## 🔧 Процесс исправления найденных проблем

### Шаг 1: Воспроизведение проблемы

```typescript
// Детальное воспроизведение через MCP Playwright
browser_navigate({ url: '...' })
browser_wait_for({ time: 3 })
browser_snapshot() // Фиксируем начальное состояние

// Выполняем действия, приводящие к проблеме
browser_click({ element: '...', ref: '...' })
browser_wait_for({ time: 1 })
browser_snapshot() // Фиксируем проблемное состояние

// Собираем диагностическую информацию
browser_console_messages() // Ошибки в консоли
browser_network_requests() // Проблемы с API
browser_snapshot() // Визуальное состояние
```

### Шаг 2: Анализ проблемы

**Проверяй все слои:**

1. **UI слой:**
   - Проверка через `browser_snapshot()` - визуальное состояние
   - Проверка через `browser_console_messages()` - ошибки JavaScript

2. **API слой:**
   - Проверка через `browser_network_requests()` - запросы и ответы
   - Проверка payload и status codes

3. **Backend слой:**
   - Проверка логов: `docker-compose logs backend | grep ERROR`
   - Проверка исключений и stack traces

4. **БД слой:**
   - Проверка сохраненных данных
   - Проверка транзакций

### Шаг 3: Изоляция проблемы

**Используй метод "разделяй и властвуй":**

```typescript
// Тестируй каждый элемент отдельно
// 1. Тестируй кнопку отдельно
browser_click({ element: 'Кнопка', ref: '...' })
// Проверяй: Работает ли кнопка?

// 2. Тестируй поле отдельно
browser_type({ element: 'Поле', ref: '...', text: 'test' })
// Проверяй: Работает ли поле?

// 3. Тестируй форму отдельно
// Заполняй все поля и отправляй
// Проверяй: Работает ли форма?

// 4. Тестируй интеграцию
// Проверяй взаимодействие элементов
```

### Шаг 4: Исправление проблемы

**После исправления:**

1. **Обнови код**
2. **Перезапусти сервисы** (если нужно)
3. **Повтори тест** - проверь, что проблема решена

### Шаг 5: Верификация исправления

```typescript
// Повторный тест после исправления
browser_navigate({ url: '...' })
browser_wait_for({ time: 3 })

// Выполняем те же действия
browser_click({ element: '...', ref: '...' })
browser_wait_for({ time: 3 })

// Проверяем результат
browser_console_messages()
// Проверка: Нет ошибок

browser_network_requests()
// Проверка: Запросы успешны

browser_snapshot()
// Проверка: Визуальное состояние корректно
```

---

## 📊 Структурированный подход к тестированию

### Этап 1: Тестирование отдельных элементов (1-2 часа)

**Порядок тестирования:**

1. **Кнопки** (Button)
   - [ ] Все кнопки на странице
   - [ ] Состояния (active/disabled/loading)
   - [ ] Обработчики кликов

2. **Поля ввода** (Input)
   - [ ] Все поля на странице
   - [ ] Валидация
   - [ ] Граничные значения

3. **Чекбоксы/Switch**
   - [ ] Все чекбоксы на странице
   - [ ] Переключение
   - [ ] Персистентность

4. **Вкладки** (Tabs)
   - [ ] Все вкладки
   - [ ] Переключение
   - [ ] Контент

5. **Индикаторы загрузки** (Loader)
   - [ ] Появление/исчезновение
   - [ ] Анимация

6. **Карточки** (Card)
   - [ ] Отображение
   - [ ] Контент

### Этап 2: Тестирование простых интеграций (1-2 часа)

1. **Формы**
   - [ ] Валидация всех полей
   - [ ] Отправка формы
   - [ ] Обработка результатов

2. **Взаимодействие элементов в форме**
   - [ ] Зависимости между полями
   - [ ] Условная валидация

### Этап 3: Тестирование сложных интеграций (2-3 часа)

1. **Взаимодействие между компонентами**
   - [ ] Форма ↔ Статус
   - [ ] Вкладки ↔ Формы
   - [ ] Список ↔ Детали

2. **Синхронизация данных**
   - [ ] Обновление после сохранения
   - [ ] Обновление после удаления
   - [ ] Обновление после создания

### Этап 4: E2E тестирование (1-2 часа)

1. **Полные пользовательские сценарии**
   - [ ] Создание магазина → Настройка интеграции → Тестирование
   - [ ] Обновление настроек → Проверка работы
   - [ ] Обработка ошибок

---

## ✅ Чек-лист полного тестирования страницы

### Подготовка
- [ ] Приложение запущено и доступно
- [ ] **ОБЯЗАТЕЛЬНО**: Docker сеть настроена для MCP Playwright (см. раздел "Настройка Docker сети")
- [ ] **ОБЯЗАТЕЛЬНО**: `.cursor/mcp.json` содержит `--network=[network-name]` для playwright
- [ ] **ОБЯЗАТЕЛЬНО**: `frontend/vite.config.ts` содержит `allowedHosts` в секции `server`
- [ ] MCP Playwright настроен и работает (перезапущен после изменения конфигурации)
- [ ] Backend логи доступны

### Этап 1: Элементы по отдельности
- [ ] Все кнопки протестированы
- [ ] Все поля ввода протестированы
- [ ] Все чекбоксы протестированы
- [ ] Все вкладки протестированы
- [ ] Все индикаторы загрузки протестированы
- [ ] Все карточки протестированы

### Этап 2: Простые интеграции
- [ ] Все формы протестированы
- [ ] Взаимодействие элементов в формах протестировано

### Этап 3: Сложные интеграции
- [ ] Взаимодействие между компонентами протестировано
- [ ] Синхронизация данных протестирована

### Этап 4: E2E сценарии
- [ ] Критичные пользовательские сценарии протестированы

### Исправление проблем
- [ ] Все найденные проблемы задокументированы
- [ ] Все проблемы исправлены
- [ ] Все исправления верифицированы

### Документирование
- [ ] Все тест-кейсы задокументированы в `test-cases.qmd`
- [ ] Все найденные баги задокументированы
- [ ] Все исправления задокументированы
- [ ] Создана структура в `info/autotests/sprint*/task-name/`
- [ ] Unit тесты задокументированы в `components/*.test.qmd`
- [ ] Integration тесты задокументированы в `integrations/*.test.qmd`
- [ ] E2E тесты задокументированы в `e2e/*.test.qmd`
- [ ] MCP тесты задокументированы в `mcp/*.test.qmd`
- [ ] Результаты зафиксированы в `results/TestResults.qmd`
- [ ] Баги зафиксированы в `results/BugsFound.qmd`

---

## 📁 Структура документирования автотестов

### ОБЯЗАТЕЛЬНАЯ структура для автотестов

**КРИТИЧЕСКИ ВАЖНО**: 
- Все автотесты должны быть задокументированы в `info/autotests/`
- **ВСЕ инструкции по работе с автотестами находятся ТОЛЬКО в этом файле (`testing.mdc`)**
- Папка `info/autotests/` может быть удалена или отсутствовать в новом проекте
- **НЕ создавай README файлы в `info/autotests/`** - вся документация здесь

**Что можно удалять:**
- ✅ Всю папку `info/autotests/` - можно удалить полностью, структура воссоздается по инструкциям
- ✅ Папку `runs/` - история запусков (опционально, можно удалить, если не нужна)
- ✅ Папку `runs/YYYY-MM-DD-HH-MM/` - конкретный запуск из истории (можно удалить)

**Что НЕЛЬЗЯ удалять (во время активного запуска):**
- ❌ Папки `test-cases/group-XXX-name/`, `test-cases/`, `results/` - результаты текущего тестирования
- ❌ Файлы в этих папках - данные текущего запуска

**Когда можно удалять:**
- ✅ После завершения запуска и сохранения результатов (если нужно)
- ✅ При ротации папок (автоматически перемещаются в `runs/YYYY-MM-DD-HH-MM/`)
- ✅ Если запуск не был завершен и результаты не важны

**Примечание**: История запусков хранится в git, поэтому папка `runs/` опциональна. Можно просто коммитить `test-cases/group-XXX-name/`, `test-cases/`, `results/` в git при завершении запуска.

#### Полная структура папок

```
info/autotests/
├── templates/                    # Шаблоны (ОБЯЗАТЕЛЬНАЯ папка, только шаблоны)
│   ├── unit-test-template.qmd    # Шаблон для unit тестов
│   ├── integration-test-template.qmd  # Шаблон для integration тестов
│   ├── e2e-test-template.qmd    # Шаблон для E2E тестов
│   ├── mcp-test-template.qmd    # Шаблон для MCP Playwright тестов
│   ├── test-results-template.qmd  # Шаблон для результатов
│   ├── bugs-found-template.qmd  # Шаблон для списка багов
│   ├── group-template.qmd       # Шаблон для группы тестов
│   ├── launchlog-template.qmd   # Шаблон для launch log группы
│   └── test-case-template.qmd   # Шаблон для отдельного тест-кейса
├── test-cases/                  # Тест-кейсы, организованные по группам
│   ├── group-001-ui-elements/   # Группа: UI элементы
│   │   ├── group-001-ui-elements.qmd  # Файл группы (описание, статистика)
│   │   ├── launchlog.qmd              # Полный отчет последнего запуска группы
│   │   ├── TC-001-button-save.qmd     # Тест-кейс: Кнопка "Сохранить"
│   │   ├── TC-002-input-bot-token.qmd # Тест-кейс: Поле "Bot Token"
│   │   └── ...                         # Другие тест-кейсы группы
│   ├── group-002-forms/          # Группа: Формы
│   │   ├── group-002-forms.qmd         # Файл группы
│   │   ├── launchlog.qmd               # Полный отчет последнего запуска группы
│   │   ├── TC-008-form-connect.qmd     # Тест-кейс: Форма подключения
│   │   └── ...                         # Другие тест-кейсы группы
│   ├── group-003-integrations/   # Группа: Интеграции
│   │   ├── group-003-integrations.qmd  # Файл группы
│   │   ├── launchlog.qmd               # Полный отчет последнего запуска группы
│   │   └── ...                         # Тест-кейсы группы
│   ├── group-004-e2e-scenarios/  # Группа: E2E сценарии
│   │   ├── group-004-e2e-scenarios.qmd # Файл группы
│   │   ├── launchlog.qmd               # Полный отчет последнего запуска группы
│   │   └── ...                         # Тест-кейсы группы
│   └── group-005-api-tests/      # Группа: API тесты
│       ├── group-005-api-tests.qmd     # Файл группы
│       ├── launchlog.qmd               # Полный отчет последнего запуска группы
│       └── ...                         # Тест-кейсы группы
└── results/                      # Результаты выполнения
    ├── summary.qmd         # Сводка результатов (статистика)
    ├── details.qmd         # Детальные результаты по группам
    └── bugs-found.qmd      # Найденные баги
```

#### Структура группы тестов (`test-cases/group-XXX-name/`)

**Каждая группа содержит:**

1. **`group-XXX-name.qmd`** - Файл группы (описание, статистика, список тест-кейсов)
2. **`launchlog.qmd`** - Полный отчет последнего запуска группы (ОБЯЗАТЕЛЬНО обновляется при каждом запуске)
3. **`TC-XXX-name.qmd`** - Файлы тест-кейсов группы

**Структура файла группы:**

```markdown
---
title: "Группа тестов: [Название группы]"
group-id: "group-001"
group-name: "ui-elements"
date: "YYYY-MM-DD"
---

# Группа тестов: [Название группы]

## 📋 Описание группы
[Краткое описание области тестирования группы]

## 🎯 Область покрытия
- [Список компонентов/функций, которые тестируются в этой группе]

## 📊 Статистика группы
- **Всего тест-кейсов**: [N]
- **Пройдено**: [N]
- **Провалено**: [N]
- **В процессе**: [N]
- **Процент прохождения**: [X]%

## 🧪 Тест-кейсы группы

### TC-001: [Название тест-кейса]
**Ссылка на детали**: `test-cases/TC-001-name.qmd`
**Статус**: ✅ Пройден / ❌ Провален / 🔄 В процессе
**Приоритет**: High / Medium / Low
**Время выполнения**: [X] сек

### TC-002: [Название тест-кейса]
[Аналогично]

## 📈 Результаты группы
[Сводка результатов всех тест-кейсов группы]

## 🐛 Найденные баги
[Список багов, найденных в этой группе, со ссылками на bugs-found.qmd]
```

#### Структура тест-кейса (`test-cases/TC-XXX-name.qmd`)

**Каждый тест-кейс содержит:**

```markdown
---
title: "Тест-кейс: [Название]"
test-case-id: "TC-001"
group-id: "group-001"
date: "YYYY-MM-DD"
---

# Тест-кейс: [Название]

## 📋 Общая информация
- **ID**: TC-001
- **Группа**: group-001-ui-elements
- **Тип**: Unit / Integration / E2E / MCP
- **Приоритет**: High / Medium / Low
- **Статус**: ✅ Пройден / ❌ Провален / 🔄 В процессе

## 🎯 Цель
[Что тестируется и зачем]

## 📝 Предусловия
- [Условие 1]
- [Условие 2]

## 🔧 Шаги выполнения
1. [Действие 1]
2. [Действие 2]
3. [Действие 3]

## ✅ Ожидаемый результат
- [Результат 1]
- [Результат 2]

## 📊 Фактический результат
- [Что произошло на самом деле]

## 🔍 Детали выполнения

### MCP команды (если использовались)
```typescript
browser_navigate({ url: '...' })
browser_click({ element: '...', ref: '...' })
```

### Код теста (если есть)
```typescript
// Unit/Integration тест
it('should ...', async () => {
  // ...
})
```

### Логи
- **Консоль**: [Вывод browser_console_messages()]
- **Сеть**: [Вывод browser_network_requests()]
- **Backend**: [Логи backend, если применимо]

### Скриншоты
- [Ссылки на скриншоты, если создавались через browser_take_screenshot()]

## 🐛 Найденные проблемы
[Если есть проблемы, опиши их здесь]

## 📝 Заметки
[Дополнительные заметки]
```

#### Структура результатов (`results/`)

**`summary.qmd`** - Сводка всех результатов:

```markdown
---
title: "Сводка результатов тестирования"
date: "YYYY-MM-DD HH:MM"
---

# Сводка результатов тестирования

## 📊 Общая статистика
- **Дата запуска**: YYYY-MM-DD HH:MM
- **Длительность**: [X] мин [Y] сек
- **Всего тест-кейсов**: [N]
- **Пройдено**: [N] ([X]%)
- **Провалено**: [N] ([X]%)
- **В процессе**: [N] ([X]%)

## 📈 Статистика по группам

| Группа | Всего | Пройдено | Провалено | % |
|--------|-------|----------|-----------|---|
| group-001-ui-elements | [N] | [N] | [N] | [X]% |
| group-002-forms | [N] | [N] | [N] | [X]% |
| ... | ... | ... | ... | ... |

## ❌ Проваленные тесты
- [Список проваленных тест-кейсов со ссылками]

## 🐛 Найденные баги
- [Список багов со ссылками на bugs-found.qmd]

## ✅ Критерии приемки
- [ ] Все критичные тесты пройдены
- [ ] Нет критичных багов
- [ ] [Другие критерии]
```

**`details.qmd`** - Детальные результаты по группам:

```markdown
---
title: "Детальные результаты тестирования"
date: "YYYY-MM-DD HH:MM"
---

# Детальные результаты тестирования

## group-001-ui-elements
[Детальные результаты группы]

## group-002-forms
[Детальные результаты группы]

## ...
```

**`bugs-found.qmd`** - Список найденных багов (используй шаблон `bugs-found-template.qmd`)

### Полное описание файлов и их назначения

**КРИТИЧЕСКИ ВАЖНО**: Все инструкции по работе с файлами находятся здесь. README файлы в `info/autotests/` НЕ создаются.

#### Файлы в `templates/` (шаблоны)

**Назначение**: Шаблоны для быстрого создания файлов тестов.

**Файлы:**

1. **`group-template.qmd`** - Шаблон для создания группы тестов
   - Используй для создания файлов в `test-cases/group-XXX-name/`
   - Формат имени папки: `group-XXX-name/`
   - Формат имени файла группы: `group-XXX-name.qmd` (внутри папки группы)
   - Содержит: описание группы, статистику, список тест-кейсов

2. **`test-case-template.qmd`** - Шаблон для создания тест-кейса
   - Используй для создания файлов в `test-cases/`
   - Формат имени: `TC-XXX-name.qmd`
   - Содержит: цель, предусловия, шаги, ожидаемый/фактический результат, логи

3. **`test-results-template.qmd`** - Шаблон для сводки результатов
   - Используй для создания `results/summary.qmd`
   - Содержит: общую статистику, статистику по группам, проваленные тесты, баги

4. **`bugs-found-template.qmd`** - Шаблон для списка багов
   - Используй для создания `results/bugs-found.qmd`
   - Содержит: список багов с приоритетом, статусом, шагами воспроизведения

5. **`unit-test-template.qmd`** - Шаблон для unit тестов (legacy, опционально)
6. **`integration-test-template.qmd`** - Шаблон для integration тестов (legacy, опционально)
7. **`e2e-test-template.qmd`** - Шаблон для E2E тестов (legacy, опционально)
8. **`mcp-test-template.qmd`** - Шаблон для MCP тестов (legacy, опционально)

#### Файлы в `info/autotests/` (текущий запуск)

**Назначение**: Активные папки для текущего запуска тестов.

**Структура:**

1. **`test-cases/group-XXX-name/group-XXX-name.qmd`** - Файл группы тестов (внутри папки группы)
   - Создавай по мере необходимости
   - Каждая группа содержит список тест-кейсов и статистику
   - Обновляй статистику после выполнения каждого теста

2. **`test-cases/group-XXX-name/launchlog.qmd`** - Полный отчет последнего запуска группы (ОБЯЗАТЕЛЬНО)
   - Создавай при создании группы (из шаблона `launchlog-template.qmd`)
   - **ОБЯЗАТЕЛЬНО обновляй при каждом запуске группы**:
     * Обновляй дату и время запуска
     * Обновляй статус запуска (В процессе → Завершен/Провален)
     * Обновляй статистику (всего, пройдено, провалено, пропущено)
     * Добавляй результаты каждого выполненного тест-кейса
     * Фиксируй найденные баги
     * Добавляй примечания и наблюдения
   - Содержит: информацию о запуске, статистику, результаты всех тест-кейсов, найденные баги, примечания

3. **`test-cases/group-XXX-name/TC-XXX-name.qmd`** - Файлы тест-кейсов (внутри папки группы)
   - Создавай при начале тестирования элемента/функции
   - Заполняй по мере выполнения теста
   - Обновляй статус после выполнения

3. **`results/summary.qmd`** - Сводка результатов
   - Создавай при ротации папок (копируй из шаблона)
   - Обновляй после каждого теста
   - Содержит финальную статистику запуска

4. **`results/details.qmd`** - Детальные результаты
   - Создавай при ротации папок (пустой файл)
   - Заполняй по мере необходимости
   - Содержит детальные результаты по группам

5. **`results/bugs-found.qmd`** - Список багов
   - Создавай при ротации папок (копируй из шаблона)
   - Добавляй записи при обнаружении багов
   - Обновляй статус после исправления

6. **Логи и скриншоты** (опционально)
   - Логи сохраняются через `browser_console_messages()` и `browser_network_requests()` в тест-кейсах
   - Скриншоты создаются через `browser_take_screenshot()` при необходимости
   - Артефакты не требуют отдельной папки - они документируются в тест-кейсах

#### Файлы в `runs/YYYY-MM-DD-HH-MM/` (опционально, история запусков)

**Назначение**: Архив конкретного запуска (опционально, для сравнения результатов).

**Структура**: Та же, что и в ``

**Использование**: 
- Сравнивай результаты между запусками
- Анализируй изменения
- **Примечание**: История также хранится в git, поэтому эта папка опциональна

### Создание структуры с нуля (если папка отсутствует)

**Если папка `info/autotests/` отсутствует или была удалена:**

#### Шаг 1: Создай базовую структуру

```bash
# Создай папку templates
mkdir -p info/autotests/templates

# Создай папки для запусков (пока пустые)
mkdir -p info/autotests/test-cases info/autotests/results
```

**Через Cursor IDE:**
1. Используй `write` для создания файлов - папки создадутся автоматически
2. Или используй `list_dir` для проверки существования

#### Шаг 2: Создай шаблоны (если отсутствуют)

**ОБЯЗАТЕЛЬНЫЕ шаблоны (минимальный набор):**

1. **`group-template.qmd`** - Шаблон группы тестов
   - Структура: см. раздел "Структура группы тестов" выше
   - Или скопируй из существующего проекта, если есть

2. **`test-case-template.qmd`** - Шаблон тест-кейса
   - Структура: см. раздел "Структура тест-кейса" выше
   - Или скопируй из существующего проекта, если есть

3. **`test-results-template.qmd`** - Шаблон сводки результатов
   - Структура: см. раздел "Структура результатов" выше
   - Или скопируй из существующего проекта, если есть

4. **`bugs-found-template.qmd`** - Шаблон списка багов
   - Структура: см. раздел "Структура результатов" выше
   - Или скопируй из существующего проекта, если есть

**Создание шаблонов через Cursor IDE:**

1. Прочитай структуру шаблона из этого файла (разделы выше)
2. Создай файл: `write("info/autotests/templates/group-template.qmd", content)`
3. Повтори для всех шаблонов

**Или скопируй из существующего проекта:**

Если у тебя есть доступ к другому проекту с такой же структурой:
```bash
# Скопируй все шаблоны
cp -r /path/to/other/project/info/autotests/templates/*.qmd info/autotests/templates/
```

#### Шаг 3: Выполни ротацию папок

Следуй инструкциям в разделе "Workflow управления запусками тестов" выше.

**Важно**: После создания структуры с нуля, сразу создай папки `test-cases/group-XXX-name/`, `test-cases/`, `results/` с правильной структурой.

### Правила работы с файлами

**ОБЯЗАТЕЛЬНО:**

1. **НЕ создавай README файлы** в `info/autotests/` - вся документация в `testing.mdc`
2. **Всегда используй шаблоны** при создании новых файлов
3. **Следуй правилам именования** (см. раздел "Правила именования" выше)
4. **Обновляй статистику** в группах после каждого теста (конкретные примеры выше)
5. **Фиксируй результаты** сразу после выполнения теста
6. **Выполняй ротацию** перед каждым новым запуском

**РЕКОМЕНДУЕТСЯ:**

1. **Создавай ВСЕ стандартные группы СРАЗУ** перед началом тестирования (не по мере необходимости)
2. **Создавай примеры тест-кейсов СРАЗУ** для демонстрации структуры
3. Создавай остальные тест-кейсы при начале тестирования элемента
4. Сохраняй артефакты (скриншоты, логи) только при необходимости
5. Создавай ссылки в задачах для удобства навигации

### Примеры заполнения frontmatter

**Группа тестов:**
```yaml
---
title: "Группа тестов: UI элементы"
group-id: "group-001"
group-name: "ui-elements"
date: "2026-01-21"
---
```

**Тест-кейс:**
```yaml
---
title: "Тест-кейс: Кнопка Сохранить"
test-case-id: "TC-001"
group-id: "group-001"
date: "2026-01-21"
---
```

**Важно**: 
- `group-id` в тест-кейсе должен совпадать с `group-id` в группе
- Если тест-кейс не относится ни к одной группе, создай новую группу или используй группу "other" (`group-999-other`)
- Каждый тест-кейс должен быть привязан к группе

### Сравнение с предыдущим запуском

**Для сравнения результатов между запусками:**

1. **Открой `runs/YYYY-MM-DD-HH-MM/results/summary.qmd`** - посмотри статистику предыдущего запуска
2. **Сравни с текущим** `results/summary.qmd`:
   - Изменилось ли количество тест-кейсов
   - Улучшился ли процент прохождения
   - Появились ли новые баги
3. **Сравни группы** - открой соответствующие файлы групп в обоих запусках
4. **Анализируй изменения** - какие тесты были добавлены/удалены/изменены

#### Принципы организации

1. **Ротация запусков**: Перед каждым новым запуском выполняется ротация папок
   - `runs/YYYY-MM-DD-HH-MM/` - архив предыдущего запуска (только для чтения)
   - `` - текущий активный запуск (для записи)
   - Это позволяет сравнивать результаты между запусками и сохранять историю

2. **Группировка тестов**: Тесты организованы по группам функциональности
   - `group-001-ui-elements` - UI элементы (кнопки, поля, чекбоксы, вкладки, индикаторы, карточки)
   - `group-002-forms` - Формы (валидация, отправка, обработка результатов)
   - `group-003-integrations` - Интеграции между компонентами (форма ↔ статус, вкладки ↔ формы)
   - `group-004-e2e-scenarios` - E2E пользовательские сценарии (полные потоки)
   - `group-005-api-tests` - API тесты (позитивные, негативные, граничные)
   - **ВСЕ стандартные группы создаются СРАЗУ** перед началом тестирования

3. **Детализация тест-кейсов**: Каждый тест-кейс в отдельном файле
   - `TC-001-button-save.qmd` - детальный тест-кейс для кнопки "Сохранить"
   - `TC-002-input-bot-token.qmd` - детальный тест-кейс для поля "Bot Token"
   - Каждый тест-кейс связан с группой через `group-id`

4. **Результаты**: Все результаты фиксируются в `results/`
   - `summary.qmd` - сводка всех результатов (статистика)
   - `details.qmd` - детальные результаты по группам
   - `bugs-found.qmd` - все найденные баги

5. **Артефакты**: Логи и скриншоты документируются в тест-кейсах
   - Логи: через `browser_console_messages()` и `browser_network_requests()`
   - Скриншоты: через `browser_take_screenshot()` при необходимости

#### Правила именования

**Группы тестов:**
- Формат папки: `group-XXX-name/` (например, `group-001-ui-elements/`)
- Формат файла группы: `group-XXX-name.qmd` (внутри папки группы)
- `XXX` - порядковый номер группы (001, 002, 003, ...) - последовательно по мере создания
- `name` - краткое название группы на английском (kebab-case)
- Файл должен содержать `group-id: "group-XXX"` в frontmatter

**Тест-кейсы:**
- Формат: `TC-XXX-name.qmd` (например, `TC-001-button-save.qmd`)
- `XXX` - порядковый номер тест-кейса (001, 002, 003, ...) - **последовательно по всему запуску**, не по группам
- `name` - краткое название тест-кейса на английском (kebab-case)
- Файл должен содержать `test-case-id: "TC-XXX"` и `group-id: "group-XXX"` в frontmatter
- **Важно**: Один тест-кейс может быть только в одной группе

**Результаты:**
- `summary.qmd` - всегда одно имя (сводка)
- `details.qmd` - всегда одно имя (детали)
- `bugs-found.qmd` - всегда одно имя (баги)

**Артефакты:**
- Скриншоты: `TC-XXX-description.png` (например, `TC-001-before.png`)
- Логи: `console.log`, `network.log`, `backend.log`


### Правила документирования

#### 1. При создании тест-кейсов

**ОБЯЗАТЕЛЬНО**:
- Создай папку `info/autotests/sprint[номер]/[номер]_task-name/`
- Используй шаблоны из `info/autotests/templates/`
- Создай файлы для каждого типа тестов

**Пример создания структуры:**

```bash
# Создание структуры для задачи
mkdir -p info/autotests/sprint1/001_telegram-integration-mvp/{components,integrations,e2e,mcp,results}

# Копирование шаблонов
cp info/autotests/templates/unit-test-template.qmd info/autotests/sprint1/001_telegram-integration-mvp/components/Button.test.qmd
cp info/autotests/templates/mcp-test-template.qmd info/autotests/sprint1/001_telegram-integration-mvp/mcp/ElementTests.qmd
```

#### 2. При тестировании элементов

**ОБЯЗАТЕЛЬНО**:
- Создай файл `components/[ComponentName].test.qmd` для каждого компонента
- Записывай все тест-кейсы с результатами
- Фиксируй найденные проблемы

**Пример:**

```markdown
# Unit тест: Button

## TC-UNIT-001: Тестирование кнопки "Сохранить"
**Результат**: ✅ Пройдено
**Дата**: 2026-01-21
**Код теста**: [код]
```

#### 3. При тестировании через MCP Playwright

**ОБЯЗАТЕЛЬНО**:
- Создай файл `mcp/[TestName].qmd`
- Записывай все MCP команды с результатами
- Фиксируй скриншоты и логи

**Пример:**

```markdown
# MCP тест: Button Element

## TC-MCP-001: Тестирование кнопки через браузер
**MCP команды**:
```typescript
browser_navigate({ url: '...' })
browser_click({ element: 'Кнопка', ref: 'e60' })
```
**Результаты**:
- ✅ Отображение: Кнопка видна
- ✅ Взаимодействие: Клик работает
- ✅ Консоль: Нет ошибок
```

#### 4. При фиксации результатов

**ОБЯЗАТЕЛЬНО**:
- Обновляй `results/TestResults.qmd` после каждого прогона
- Фиксируй статистику (пройдено/провалено)
- Обновляй `results/BugsFound.qmd` при обнаружении багов

**Пример:**

```markdown
# Результаты тестирования

## Сводка
- Unit тесты: 15/15 пройдено ✅
- Integration тесты: 8/8 пройдено ✅
- E2E тесты: 3/3 пройдено ✅
- MCP тесты: 12/12 пройдено ✅
```

#### 5. При обнаружении багов

**ОБЯЗАТЕЛЬНО**:
- Создай запись в `results/BugsFound.qmd`
- Укажи приоритет, статус, шаги воспроизведения
- Зафиксируй решение после исправления

**Пример:**

```markdown
### BUG-001: Чекбокс не сохраняет состояние
**Приоритет**: High
**Статус**: Исправлен
**Дата обнаружения**: 2026-01-21
**Решение**: Убрали registerManaged(), используем стандартный find()
**Верификация**: Повторный тест пройден ✅
```

### Связь с основной документацией

**Три уровня документирования:**

1. **Высокоуровневые тест-кейсы** (`info/sprint*/task-name/test-cases.qmd`)
   - Общие тест-кейсы из ТЗ
   - Позитивные и негативные сценарии
   - Критерии приемки
   - Используется для планирования

2. **Детальные автотесты** (`info/autotests/sprint*/task-name/`)
   - Конкретные тесты компонентов
   - Код тестов
   - MCP команды
   - Детальные результаты
   - Используется для выполнения

3. **Результаты выполнения** (`info/autotests/sprint*/task-name/results/`)
   - Сводка всех прогонов
   - Статистика
   - Найденные баги
   - Используется для отчетности

**Workflow синхронизации:**

1. **Планирование** (в `test-cases.qmd`):
   - Создай общие тест-кейсы на основе ТЗ
   - Определи критерии приемки
   - Спланируй области тестирования

2. **Выполнение** (в `info/autotests/`):
   - Создай структуру для задачи
   - Детализируй тесты в соответствующих файлах
   - Выполняй тесты и фиксируй результаты

3. **Отчетность** (в `results/`):
   - Обновляй `TestResults.qmd` после каждого прогона
   - Фиксируй баги в `BugsFound.qmd`
   - Обновляй общий `test-cases.qmd` при необходимости

4. **Синхронизация**:
   - Обновляй статус тестов в `test-cases.qmd`
   - Связывай найденные баги с тест-кейсами
   - Обновляй критерии приемки на основе результатов

### Шаблоны для быстрого старта

**Используй шаблоны из `info/autotests/templates/`:**

- `unit-test-template.qmd` - для unit тестов компонентов
- `integration-test-template.qmd` - для integration тестов
- `e2e-test-template.qmd` - для E2E тестов
- `mcp-test-template.qmd` - для MCP Playwright тестов
- `test-results-template.qmd` - для сводки результатов
- `bugs-found-template.qmd` - для списка багов

**Как использовать:**
1. Скопируй нужный шаблон
2. Замени `[Название]` на реальные значения
3. Заполни тест-кейсы
4. Обновляй результаты по мере тестирования

### Workflow управления запусками тестов

**КРИТИЧЕСКИ ВАЖНО**: Перед каждым новым запуском тестов выполняется ротация папок.

#### Шаг 1: Подготовка к новому запуску (ОБЯЗАТЕЛЬНО перед началом тестирования)

**🚨 КРИТИЧЕСКИ ВАЖНО**: Структура должна быть создана ПОЛНОСТЬЮ и СРАЗУ перед началом тестирования!

**Алгоритм ротации и создания полной структуры:**

1. **Сохранить текущий запуск в историю** (опционально, если нужна история):
   ```bash
   # Создать папку runs/ если её нет
   mkdir -p info/autotests/runs
   # Создать папку для этого запуска
   RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
   mkdir -p "$RUN_DIR"
   # Переместить папки в историю
   mv info/autotests/test-cases "$RUN_DIR/"
   mv info/autotests/results "$RUN_DIR/"
   ```
   
   **Примечание**: Если история не нужна, можно просто коммитить `test-cases/group-XXX-name/`, `test-cases/`, `results/` в git при завершении запуска.

2. **Если история не нужна, просто удалить папки** (если нужно):
   ```bash
   rm -rf info/autotests/{test-cases,results}
   ```

3. **Создать ПОЛНУЮ структуру последнего запуска СРАЗУ**:
   ```bash
   # Создать структуру папок
   mkdir -p info/autotests/test-cases info/autotests/results
   
   # Создать файлы результатов с правильным содержимым
   cp info/autotests/templates/test-results-template.qmd info/autotests/results/summary.qmd
   # Отредактируй summary.qmd: укажи дату, задачу, спринт, начальную статистику (0 тест-кейсов)
   
   touch info/autotests/results/details.qmd
   # Заполни details.qmd: создай разделы для каждой группы
   
   cp info/autotests/templates/bugs-found-template.qmd info/autotests/results/bugs-found.qmd
   # Отредактируй bugs-found.qmd: укажи задачу, спринт, дату
   
   ```

**Через Cursor IDE:**

1. Проверь существование папок: `list_dir("info/autotests/templates/")`
2. Сохрани текущий запуск в историю (опционально): перемести `test-cases/group-XXX-name/`, `test-cases/`, `results/` в `runs/YYYY-MM-DD-HH-MM/`
3. **СОЗДАЙ ПОЛНУЮ СТРУКТУРУ СРАЗУ**:
   - Создай папки `test-cases/group-XXX-name/`, `test-cases/`, `results/` через `write` файлов
   - Создай файлы результатов с правильным содержимым (не пустые!)
   - Создай все стандартные группы (см. Шаг 2)
   - Создай примеры тест-кейсов (см. Шаг 3)

#### Шаг 2: Создание ВСЕХ стандартных групп тестов (ОБЯЗАТЕЛЬНО СРАЗУ)

**🚨 КРИТИЧЕСКИ ВАЖНО**: Создай ВСЕ стандартные группы СРАЗУ, не "по мере необходимости"!

**ОБЯЗАТЕЛЬНЫЕ стандартные группы (создай все сразу):**

1. **group-001-ui-elements** - Тестирование UI элементов (кнопки, поля, чекбоксы, вкладки, индикаторы, карточки)
2. **group-002-forms** - Тестирование форм (валидация, отправка, обработка результатов)
3. **group-003-integrations** - Тестирование интеграций между компонентами (форма ↔ статус, вкладки ↔ формы)
4. **group-004-e2e-scenarios** - E2E пользовательские сценарии (полные потоки)
5. **group-005-api-tests** - Тестирование API endpoints (позитивные, негативные, граничные)

**Создание ВСЕХ групп сразу:**

```bash
# Создай все 5 стандартных групп (каждая группа = папка)
mkdir -p info/autotests/test-cases/group-001-ui-elements
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-001-ui-elements/group-001-ui-elements.qmd
mkdir -p info/autotests/test-cases/group-002-forms
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-002-forms/group-002-forms.qmd
mkdir -p info/autotests/test-cases/group-003-integrations
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-003-integrations/group-003-integrations.qmd
mkdir -p info/autotests/test-cases/group-004-e2e-scenarios
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-004-e2e-scenarios/group-004-e2e-scenarios.qmd
mkdir -p info/autotests/test-cases/group-005-api-tests
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-005-api-tests/group-005-api-tests.qmd

# Отредактируй каждый файл:
# - Замени [Название группы] на реальное название
# - Замени group-XXX на реальный ID группы
# - Заполни описание группы
# - Укажи область покрытия
# - Установи начальную статистику: 0 тест-кейсов, 0 пройдено, 0 провалено
```

**Через Cursor IDE:**

1. Прочитай шаблон: `read_file("info/autotests/templates/group-template.qmd")`
2. **Для каждой из 5 групп**:
   - Замени все `[Название]` на реальные значения
   - Укажи правильный `group-id` (group-001, group-002, и т.д.)
   - Заполни описание группы и область покрытия
   - Установи начальную статистику (0 тест-кейсов)
   - Создай файл: `write("info/autotests/test-cases/group-XXX-name/group-XXX-name.qmd", content)`

**Важно**: 
- Все группы должны быть созданы СРАЗУ перед началом тестирования
- Каждая группа должна иметь правильную структуру с начальной статистикой
- Группы должны быть готовы к добавлению тест-кейсов

#### Шаг 3: Создание примеров тест-кейсов (РЕКОМЕНДУЕТСЯ СРАЗУ)

**🚨 РЕКОМЕНДУЕТСЯ**: Создай примеры тест-кейсов для каждой группы СРАЗУ, чтобы показать структуру.

**Примеры тест-кейсов для каждой группы:**

1. **group-001-ui-elements**: 
   - `TC-001-button-save.qmd` - Пример тест-кейса для кнопки
   - `TC-004-switch-enabled.qmd` - Пример тест-кейса для чекбокса

2. **group-002-forms**: 
   - `TC-008-form-connect.qmd` - Пример тест-кейса для формы

3. **group-003-integrations**: 
   - Примеры создаются по мере необходимости

4. **group-004-e2e-scenarios**: 
   - Примеры создаются по мере необходимости

5. **group-005-api-tests**: 
   - Примеры создаются по мере необходимости

**Создание примера тест-кейса:**

```bash
# Скопируй шаблон
cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-001-button-save.qmd

# Отредактируй файл:
# - Замени TC-001 на реальный ID
# - Укажи group-id (например, group-001)
# - Заполни все обязательные поля (цель, предусловия, шаги, ожидаемый результат)
# - Установи статус: ⏳ Не начат
```

**Через Cursor IDE:**

1. Прочитай шаблон: `read_file("info/autotests/templates/test-case-template.qmd")`
2. Замени все `[Название]` на реальные значения
3. Укажи правильный `test-case-id` и `group-id` в frontmatter
4. Заполни все обязательные поля (цель, предусловия, шаги, ожидаемый результат)
5. Установи статус: ⏳ Не начат
6. Создай файл: `write("info/autotests/test-cases/TC-XXX-name.qmd", content)`
7. **ОБЯЗАТЕЛЬНО обнови группу**, добавив ссылку на тест-кейс:

```markdown
### TC-001: Тестирование кнопки "Сохранить"
**Ссылка на детали**: `test-cases/TC-001-button-save.qmd`
**Статус**: ⏳ Не начат
**Приоритет**: High
**Время выполнения**: - сек
```

**Важно**: 
- Примеры тест-кейсов создаются для демонстрации структуры
- Остальные тест-кейсы создаются по мере тестирования элементов
- Каждый новый тест-кейс должен быть добавлен в соответствующую группу

#### Шаг 4: Выполнение тестов

**При выполнении теста:**

1. **Выполни тест** (через MCP Playwright, Unit тесты, и т.д.)
2. **Зафиксируй результаты** в файле тест-кейса:
   - Фактический результат
   - MCP команды
   - Логи
   - Скриншоты (если нужны)
3. **Обнови статус** в файле тест-кейса: ✅ Пройден / ❌ Провален
4. **Обнови статистику группы** в файле группы:
   - Увеличь счетчик "Пройдено" или "Провалено" на 1
   - Уменьшь "В процессе" на 1 (если был)
   - Пересчитай "Процент прохождения": `(Пройдено / Всего) * 100`
   - Обнови статус тест-кейса в списке группы (✅/❌)
   
   **Пример обновления:**
   ```markdown
   ## 📊 Статистика группы
   - **Всего тест-кейсов**: 5
   - **Пройдено**: 4  (было 3, стало 4)
   - **Провалено**: 1
   - **В процессе**: 0  (было 1, стало 0)
   - **Процент прохождения**: 80%  (4/5 * 100)
   
   ### TC-001: Тестирование кнопки "Сохранить"
   **Статус**: ✅ Пройден  (было 🔄 В процессе)
   ```
5. **Обнови сводку** в `results/summary.qmd`:
   - Обнови общую статистику (Всего/Пройдено/Провалено)
   - Обнови статистику по группам в таблице
   - Добавь проваленный тест в список "Проваленные тесты" (если провален)
   
   **Пример обновления:**
   ```markdown
   ## 📊 Общая статистика
   - **Всего тест-кейсов**: 15  (было 14, стало 15)
   - **Пройдено**: 12  (было 11, стало 12)
   - **Провалено**: 1
   
   | Группа | Всего | Пройдено | Провалено | % |
   |--------|-------|----------|-----------|---|
   | group-001-ui-elements | 5 | 4 | 1 | 80% |
   ```

#### Шаг 5: Фиксация багов

**При обнаружении бага:**

1. **Создай запись** в `results/bugs-found.qmd`
2. **Свяжи баг с тест-кейсом** (укажи TC-XXX в описании бага)
3. **Обнови группу** (добавь ссылку на баг в раздел "Найденные баги")
4. **Обнови `results/summary.qmd`** (добавь баг в список найденных багов)

#### Шаг 6: Завершение запуска

**После завершения всех тестов:**

1. **Обнови `results/summary.qmd`**:
   - Финальная статистика
   - Список проваленных тестов
   - Список найденных багов
   - Критерии приемки

2. **Обнови `results/details.qmd`**:
   - Детальные результаты по каждой группе

3. **Коммит в git** (рекомендуется):
   ```bash
   # После завершения запуска закоммить результаты
   git add info/autotests/
   git commit -m "test: результаты тестирования $(date +%Y-%m-%d-%H-%M)"
   ```
   
   **Примечание**: История запусков хранится в git, поэтому папка `runs/` опциональна.

### Пошаговое создание структуры для новой задачи

**ОБЯЗАТЕЛЬНО** при начале тестирования новой задачи:

#### Шаг 1: Определи путь к задаче

Убедись, что знаешь:
- Номер спринта: `sprint1`, `sprint2`, и т.д.
- Номер и название задачи: `001_task-name`, `002_task-name`, и т.д.

**Пример**: Задача находится в `info/sprint1/001_telegram-integration-mvp/`
→ Тесты будут в `info/autotests/sprint1/001_telegram-integration-mvp/`

#### Шаг 2: Подготовь запуск тестов (ротация папок)

**ОБЯЗАТЕЛЬНО** выполни ротацию перед началом тестирования:

**Через терминал:**
```bash
# 1. Удали предыдущий запуск
rm -rf info/autotests/runs/YYYY-MM-DD-HH-MM

# 2. Переименуй последний в предыдущий (если существует)
if [ -d "info/autotests/groups" ] || [ -d "info/autotests/test-cases" ] || [ -d "info/autotests/results" ]; then
  # Переместить папки в историю
  RUN_DIR="info/autotests/runs/YYYY-MM-DD-HH-MM"
  mkdir -p "$RUN_DIR"
  [ -d "info/autotests/groups" ] && # groups больше нет "$RUN_DIR/"
  [ -d "info/autotests/test-cases" ] && mv info/autotests/test-cases "$RUN_DIR/"
  [ -d "info/autotests/results" ] && mv info/autotests/results "$RUN_DIR/"
  RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
  mkdir -p "$RUN_DIR"
  # groups больше нет "$RUN_DIR/"
  mv info/autotests/test-cases "$RUN_DIR/"
  mv info/autotests/results "$RUN_DIR/" info/autotests/runs/YYYY-MM-DD-HH-MM
fi

# 3. Создай новую структуру последнего запуска
mkdir -p info/autotests/test-cases info/autotests/results

# 4. Создай файлы результатов (README не нужен, но можно создать для удобства)
cat > info/autotests/results/summary.qmd << 'EOF'
---
title: "Запуск тестов"
date: "$(date +%Y-%m-%d)"
time: "$(date +%H:%M)"
task: "001_telegram-integration-mvp"
sprint: "sprint1"
---

# Запуск тестов

**Дата**: $(date +%Y-%m-%d %H:%M)
**Задача**: 001_telegram-integration-mvp
**Спринт**: sprint1

## 📋 Описание запуска
[Опиши цель этого запуска тестов]

## 📁 Структура
- `test-cases/group-XXX-name/` - Группы тестов
- `test-cases/` - Детальные тест-кейсы
- `results/` - Результаты выполнения
EOF

# 5. Создай файлы результатов
cp info/autotests/templates/test-results-template.qmd info/autotests/results/summary.qmd
touch info/autotests/results/details.qmd
cp info/autotests/templates/bugs-found-template.qmd info/autotests/results/bugs-found.qmd
```

**Через Cursor IDE:**
1. Проверь существование: `list_dir("info/autotests/templates/")`
2. Удали предыдущий запуск (если есть)
3. Переименуй последний в предыдущий (если есть)
4. Создай новую структуру через `write` файлов

#### Шаг 3: Создай группы тестов

**ОБЯЗАТЕЛЬНО** создай группы перед началом тестирования:

**Через терминал:**
```bash
# Создай стандартные группы
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-001-ui-elements.qmd
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-002-forms.qmd
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-003-integrations.qmd

# Отредактируй каждый файл, заменив [Название] на реальные значения
```

**Через Cursor IDE:**
1. Прочитай шаблон: `read_file("info/autotests/templates/group-template.qmd")`
2. Для каждой группы:
   - Замени `[Название группы]` на реальное название
   - Замени `group-001` на реальный ID
   - Создай файл: `write("info/autotests/test-cases/group-XXX-name/group-XXX-name.qmd", content)`

#### Шаг 4: Создай ссылку в задаче (опционально, для истории)

```markdown
---
title: "Автотесты: [Название задачи]"
description: "Описание автотестов для задачи [Название]"
date: "YYYY-MM-DD"
task: "[номер]_task-name"
sprint: "[номер спринта]"
---

# Автотесты: [Название задачи]

## 📋 Общая информация
- **Задача**: [Название задачи]
- **Спринт**: [Номер спринта]
- **Дата создания**: YYYY-MM-DD

## 🎯 Область тестирования
[Список компонентов, интеграций, сценариев для тестирования]

## 📁 Структура тестов
[Описание структуры папок]

## 🔗 Связь с документацией
- **Общие тест-кейсы**: `info/sprint*/task-name/test-cases.qmd`
- **Детальные автотесты**: `info/autotests/sprint*/task-name/`
```

**Создай папку для истории запусков задачи (опционально):**

```bash
# История запусков хранится в git, поэтому папка runs/ опциональна
# Если нужна история в файловой системе:
# mkdir -p info/autotests/runs
# # Переместить папки в историю
  RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
  mkdir -p "$RUN_DIR"
  # groups больше нет "$RUN_DIR/"
  mv info/autotests/test-cases "$RUN_DIR/"
  mv info/autotests/results "$RUN_DIR/" info/autotests/runs/$(date +%Y-%m-%d-%H-%M)

# Рекомендуется просто коммитить  в git:
# git add info/autotests/
# git commit -m "test: результаты тестирования $(date +%Y-%m-%d-%H-%M)"
---
title: "Запуск тестов: $(date +%Y-%m-%d-%H-%M)"
date: "$(date +%Y-%m-%d)"
time: "$(date +%H:%M)"
---

# Запуск тестов: $(date +%Y-%m-%d-%H-%M)

**Ссылка на результаты**: \`../../../\`

## 📋 Описание
[Краткое описание запуска]
EOF
```

#### Шаг 5: Создавай тест-кейсы по мере тестирования

**При тестировании элемента/функции:**

1. **Определи группу** для тест-кейса (например, `group-001-ui-elements`)

2. **Создай файл тест-кейса:**

**Через терминал:**
```bash
# Создай тест-кейс для кнопки
cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-001-button-save.qmd

# Отредактируй файл:
# - Замени TC-001 на реальный ID
# - Укажи group-id: group-001-ui-elements
# - Заполни все поля
```

**Через Cursor IDE:**
1. Прочитай шаблон: `read_file("info/autotests/templates/test-case-template.qmd")`
2. Замени все `[Название]` на реальные значения
3. Укажи `group-id` в frontmatter
4. Создай файл: `write("info/autotests/test-cases/TC-XXX-name.qmd", content)`

3. **Обнови группу**, добавив ссылку на тест-кейс:

```markdown
### TC-001: Тестирование кнопки "Сохранить"
**Ссылка на детали**: `test-cases/TC-001-button-save.qmd`
**Статус**: 🔄 В процессе
**Приоритет**: High
```

4. **Выполни тест** и зафиксируй результаты в файле тест-кейса

5. **Обнови статус** в группе после выполнения теста

#### Шаг 6: Обновляй результаты по мере тестирования

**После выполнения каждого теста:**

1. **Обнови файл тест-кейса**:
   - Зафиксируй фактический результат
   - Добавь MCP команды (если использовались)
   - Добавь логи (консоль, сеть, backend)
   - Обнови статус: ✅ Пройден / ❌ Провален

2. **Обнови группу**:
   - Обнови статистику группы (пройдено/провалено)
   - Обнови статус тест-кейса в списке группы

3. **Обнови `results/summary.qmd`**:
   - Обнови общую статистику
   - Добавь проваленные тесты в список (если есть)

4. **Обнови `results/bugs-found.qmd`** при обнаружении багов:
   - Создай новую запись BUG-XXX
   - Заполни все поля (приоритет, статус, описание, шаги воспроизведения)
   - Свяжи баг с тест-кейсом (укажи TC-XXX)

5. **Обнови `results/details.qmd`**:
   - Добавь детальные результаты по группе (если нужно)

#### Пример полного workflow

```bash
# ============================================
# ШАГ 1: Подготовка к новому запуску (ОБЯЗАТЕЛЬНО)
# ============================================

# 1.1. Удали предыдущий запуск
rm -rf info/autotests/runs/YYYY-MM-DD-HH-MM

# 1.2. Переименуй последний в предыдущий (если существует)
if [ -d "info/autotests/groups" ] || [ -d "info/autotests/test-cases" ] || [ -d "info/autotests/results" ]; then
  # Переместить папки в историю
  RUN_DIR="info/autotests/runs/YYYY-MM-DD-HH-MM"
  mkdir -p "$RUN_DIR"
  [ -d "info/autotests/groups" ] && # groups больше нет "$RUN_DIR/"
  [ -d "info/autotests/test-cases" ] && mv info/autotests/test-cases "$RUN_DIR/"
  [ -d "info/autotests/results" ] && mv info/autotests/results "$RUN_DIR/"
  RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
  mkdir -p "$RUN_DIR"
  # groups больше нет "$RUN_DIR/"
  mv info/autotests/test-cases "$RUN_DIR/"
  mv info/autotests/results "$RUN_DIR/" info/autotests/runs/YYYY-MM-DD-HH-MM
fi

# 1.3. Создай новую структуру последнего запуска
mkdir -p info/autotests/test-cases info/autotests/results

# 1.4. Создай файлы результатов
cat > info/autotests/results/summary.qmd << 'EOF'
---
title: "Запуск тестов"
date: "2026-01-21"
time: "14:30"
task: "001_telegram-integration-mvp"
sprint: "sprint1"
---

# Запуск тестов

**Дата**: 2026-01-21 14:30
**Задача**: 001_telegram-integration-mvp
**Спринт**: sprint1

## 📋 Описание запуска
Полное тестирование Telegram интеграции: UI элементы, формы, интеграции, E2E сценарии.
EOF

# 1.5. Создай файлы результатов
cp info/autotests/templates/test-results-template.qmd info/autotests/results/summary.qmd
touch info/autotests/results/details.qmd
cp info/autotests/templates/bugs-found-template.qmd info/autotests/results/bugs-found.qmd

# ============================================
# ШАГ 2: Создание групп тестов
# ============================================

# 2.1. Создай группы
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-001-ui-elements.qmd
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-002-forms.qmd
cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-003-integrations.qmd

# 2.2. Отредактируй каждый файл группы (замени [Название] на реальные значения)

# ============================================
# ШАГ 3: Создание тест-кейсов
# ============================================

# 3.1. Создай тест-кейс для кнопки
cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-001-button-save.qmd
# Отредактируй: укажи group-id: group-001-ui-elements

# 3.2. Создай тест-кейс для поля ввода
cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-002-input-bot-token.qmd
# Отредактируй: укажи group-id: group-001-ui-elements

# 3.3. Обнови группы, добавив ссылки на тест-кейсы

# ============================================
# ШАГ 4: Выполнение тестов
# ============================================

# 4.1. Выполни тесты (через MCP Playwright, Unit тесты, и т.д.)
# 4.2. Зафиксируй результаты в файлах тест-кейсов
# 4.3. Обнови статусы в группах
# 4.4. Обнови results/summary.qmd

# ============================================
# ШАГ 5: Фиксация багов (если есть)
# ============================================

# 5.1. Создай запись в results/bugs-found.qmd
# 5.2. Свяжи баг с тест-кейсом

# ============================================
# ШАГ 6: Завершение запуска
# ============================================

# 6.1. Обнови results/summary.qmd (финальная статистика)
# 6.2. Обнови results/details.qmd (детальные результаты)
# 6.3. Коммит в git (рекомендуется)
git add info/autotests/
git commit -m "test: результаты тестирования $(date +%Y-%m-%d-%H-%M)"
```

### Краткое резюме: Быстрый старт нового запуска тестов

**ОБЯЗАТЕЛЬНАЯ последовательность действий:**

0. **Настройка Docker (ОБЯЗАТЕЛЬНО ПЕРЕД ПЕРВЫМ ЗАПУСКОМ)**:
   - [ ] Настроен `.cursor/mcp.json` с `--network=[network-name]`
   - [ ] Настроен `frontend/vite.config.ts` с `allowedHosts`
   - [ ] Пересобран frontend контейнер
   - [ ] Перезапущен Cursor IDE
   - [ ] Проверена доступность через MCP Playwright

1. **Ротация папок** (перед каждым запуском):
   ```bash
   rm -rf info/autotests/runs/YYYY-MM-DD-HH-MM
   # Переместить папки в историю
  RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
  mkdir -p "$RUN_DIR"
  # groups больше нет "$RUN_DIR/"
  mv info/autotests/test-cases "$RUN_DIR/"
  mv info/autotests/results "$RUN_DIR/" info/autotests/runs/YYYY-MM-DD-HH-MM 2>/dev/null || true
   mkdir -p info/autotests/test-cases info/autotests/results
   ```

2. **Создание файлов результатов**:
   ```bash
   cp info/autotests/templates/test-results-template.qmd info/autotests/results/summary.qmd
   touch info/autotests/results/details.qmd
   cp info/autotests/templates/bugs-found-template.qmd info/autotests/results/bugs-found.qmd
   ```

3. **Создание ВСЕХ стандартных групп СРАЗУ** (ОБЯЗАТЕЛЬНО):
   ```bash
   # Создай все 5 стандартных групп
   cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-001-ui-elements.qmd
   cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-002-forms.qmd
   cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-003-integrations.qmd
   cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-004-e2e-scenarios.qmd
   cp info/autotests/templates/group-template.qmd info/autotests/test-cases/group-XXX-name/group-005-api-tests.qmd
   # Отредактируй каждый файл: замени [Название группы], group-XXX, заполни описание и область покрытия
   ```

4. **Создание примеров тест-кейсов СРАЗУ** (РЕКОМЕНДУЕТСЯ):
   ```bash
   # Создай примеры для каждой группы
   cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-001-button-save.qmd
   cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-004-switch-enabled.qmd
   cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-008-form-connect.qmd
   # Отредактируй каждый файл: укажи group-id, заполни все поля, установи статус "⏳ Не начат"
   # ОБЯЗАТЕЛЬНО обнови группы, добавив ссылки на тест-кейсы
   ```
   
   **Остальные тест-кейсы создаются при тестировании элементов:**
   ```bash
   cp info/autotests/templates/test-case-template.qmd info/autotests/test-cases/TC-XXX-name.qmd
   # Отредактируй файл: укажи group-id, заполни все поля
   # ОБЯЗАТЕЛЬНО обнови группу, добавив ссылку на тест-кейс
   ```

5. **Выполнение тестов**:
   - Выполни тест (MCP Playwright, Unit тесты, и т.д.)
   - Зафиксируй результаты в файле тест-кейса
   - Обнови статус в группе
   - Обнови `results/summary.qmd`

6. **Завершение запуска**:
   - Обнови финальную статистику в `results/summary.qmd`
   - Обнови детальные результаты в `results/details.qmd`
   - Создай ссылку в задаче (опционально)

**✅ Чек-лист полной инициализации структуры:**

Перед началом тестирования проверь, что создано:

- [ ] Папка `` с полной структурой папок (`test-cases/group-XXX-name/`, `test-cases/`, `results/`)
- [ ] Файл `results/summary.qmd` с правильным содержимым (дата, задача, спринт, начальная статистика: 0 тест-кейсов)
- [ ] Файл `results/details.qmd` с разделами для всех 5 групп
- [ ] Файл `results/bugs-found.qmd` с правильным содержимым (задача, спринт, дата, начальная статистика: 0 багов)
- [ ] Все 5 стандартных групп созданы:
  - [ ] `group-001-ui-elements.qmd` с правильной структурой
  - [ ] `group-002-forms.qmd` с правильной структурой
  - [ ] `group-003-integrations.qmd` с правильной структурой
  - [ ] `group-004-e2e-scenarios.qmd` с правильной структурой
  - [ ] `group-005-api-tests.qmd` с правильной структурой
- [ ] Каждая группа имеет правильную структуру:
  - [ ] Описание группы заполнено
  - [ ] Область покрытия указана
  - [ ] Начальная статистика установлена (0 тест-кейсов, 0 пройдено, 0 провалено)
  - [ ] Файл `launchlog.qmd` создан из шаблона
- [ ] Примеры тест-кейсов созданы (минимум по 1 для каждой группы):
  - [ ] `TC-001-button-save.qmd` (group-001)
  - [ ] `TC-004-switch-enabled.qmd` (group-001)
  - [ ] `TC-008-form-connect.qmd` (group-002)
- [ ] Каждый тест-кейс имеет правильную структуру:
  - [ ] Frontmatter заполнен (test-case-id, group-id, date)
  - [ ] Цель, предусловия, шаги, ожидаемый результат заполнены
  - [ ] Статус установлен: ⏳ Не начат
- [ ] Каждый тест-кейс добавлен в соответствующую группу (ссылка в списке группы)

**🚨 Только после выполнения ВСЕХ пунктов чек-листа можно начинать тестирование!**

### Автоматизация через скрипт (опционально)

Можешь создать скрипт для быстрого создания структуры нового запуска:

```bash
#!/bin/bash
# prepare-test-run.sh

# Шаг 1: Ротация папок
rm -rf info/autotests/runs/YYYY-MM-DD-HH-MM
if [ -d "info/autotests/groups" ] || [ -d "info/autotests/test-cases" ] || [ -d "info/autotests/results" ]; then
  # Переместить папки в историю
  RUN_DIR="info/autotests/runs/YYYY-MM-DD-HH-MM"
  mkdir -p "$RUN_DIR"
  [ -d "info/autotests/groups" ] && # groups больше нет "$RUN_DIR/"
  [ -d "info/autotests/test-cases" ] && mv info/autotests/test-cases "$RUN_DIR/"
  [ -d "info/autotests/results" ] && mv info/autotests/results "$RUN_DIR/"
  RUN_DIR="info/autotests/runs/$(date +%Y-%m-%d-%H-%M)"
  mkdir -p "$RUN_DIR"
  # groups больше нет "$RUN_DIR/"
  mv info/autotests/test-cases "$RUN_DIR/"
  mv info/autotests/results "$RUN_DIR/" info/autotests/runs/YYYY-MM-DD-HH-MM
fi

# Шаг 2: Создание новой структуры
mkdir -p info/autotests/test-cases info/autotests/results

# Шаг 3: Создание файлов результатов
cat > info/autotests/results/summary.qmd << EOF
---
title: "Запуск тестов"
date: "$(date +%Y-%m-%d)"
time: "$(date +%H:%M)"
---

# Запуск тестов

**Дата**: $(date +%Y-%m-%d %H:%M)

## 📋 Описание запуска
[Опиши цель этого запуска тестов]
EOF

# Шаг 4: Создание файлов результатов
cp info/autotests/templates/test-results-template.qmd info/autotests/results/summary.qmd
touch info/autotests/results/details.qmd
cp info/autotests/templates/bugs-found-template.qmd info/autotests/results/bugs-found.qmd

echo "✅ Структура нового запуска создана: info/autotests/"
echo "📝 Создай группы и тест-кейсы по мере необходимости"
```

---

**Эти правила основаны на современных методологиях тестирования (ISTQB, Agile Testing, Test-Driven Development) и интегрированы с существующим workflow проекта для максимальной эффективности тестирования.**

# 🔐 Jenkins Credentials - Объяснение

## Что это?

**Jenkins Credentials** - это система хранения секретов (пароли, токены, ключи) в Jenkins. Это **общие настройки Jenkins**, доступные всем job'ам и pipeline'ам.

## Типы Credentials:

### 1. **Global Credentials** (Глобальные)
- Доступны **всем** job'ам и pipeline'ам в Jenkins
- Хранятся на уровне системы
- **Это то, что мы используем!**

### 2. **User Credentials** (Пользовательские)
- Доступны только конкретному пользователю
- Привязаны к аккаунту пользователя

### 3. **System Credentials** (Системные)
- На уровне системы Jenkins
- Обычно для внутренних нужд Jenkins

## Где находятся?

**Путь в Jenkins UI:**
```
Manage Jenkins → Credentials → System → Global credentials (unrestricted)
```

**URL:**
```
http://192.168.1.49:32768/credentials/store/system/domain/_/
```

## Как используются?

### В Jenkinsfile:
```groovy
withCredentials([string(credentialsId: 'telegram-bot-token', variable: 'TELEGRAM_BOT_TOKEN')]) {
    // Токен доступен как переменная окружения $TELEGRAM_BOT_TOKEN
    telegramBotToken = env.TELEGRAM_BOT_TOKEN
}
```

### Преимущества:
1. ✅ **Безопасность** - токены не хранятся в Git
2. ✅ **Централизованное управление** - один раз создал, используешь везде
3. ✅ **Маскирование в логах** - Jenkins автоматически скрывает секреты
4. ✅ **Переиспользование** - один credential можно использовать в разных job'ах

## Наши Credentials:

1. **telegram-bot-token** (Secret text)
   - ID: `telegram-bot-token`
   - Значение: `8243118630:AAF-_fBdgaHgclVab_7vS_X9k4oEZFpjkcM`
   - Используется в: `personal-page-deploy` pipeline

2. **telegram-user-id** (Secret text)
   - ID: `telegram-user-id`
   - Значение: `754334329`
   - Используется в: `personal-page-deploy` pipeline

## Важно!

- Credentials создаются **один раз** в Jenkins UI
- После создания они доступны **всем** pipeline'ам, которые знают их ID
- Если нужно изменить токен - просто обновите credential в UI
- **НЕ нужно** менять код в Jenkinsfile или Git

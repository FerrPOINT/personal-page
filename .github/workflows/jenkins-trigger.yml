name: Trigger Jenkins Build

on:
  push:
    branches:
      - main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Build
        run: |
          curl -X POST \
            "http://192.168.1.49:32768/job/personal-page-deploy/build" \
            --user "ferrpoint:1191a9f019fc3989d7a5ff30d456fb9cf3"
        env:
          JENKINS_URL: http://192.168.1.49:32768/
          JENKINS_USER: ferrpoint
          JENKINS_TOKEN: ${{ secrets.JENKINS_TOKEN }}

